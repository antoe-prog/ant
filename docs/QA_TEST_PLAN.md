# 파이널 유도 멀티짐 MVP QA 테스트 계획

작성일: 2026-06-13
대상: `/app/dashboard`, `/app/classes`, `/app/members`, `/app/payments`, `/app/notices`, `/app/notifications`, `/app/account`, `/app/owner/branches`, `/app/owner/reports`, `/app/admin/users`, `/app/admin/roles`, `/app/admin/audit-logs`, `/app/admin/settings`, `/select-role`, `/login`, `/signup`, `/reset-password`, `/invite/:token`
기준: `docs/FINAL_MVP_PRODUCT_SPEC.md`, `docs/API_CONTRACT.md`, `docs/IMPLEMENTATION_BACKLOG.md`

## 1. 통과 기준

- `npm run lint` 통과
- `npm run test:production-runtime-environment` 및 `npm run test:postgres-runtime-identity` 통과. Vercel production이 PostgreSQL driver·연결 정보·설치 식별자와 고정 상태 key `mvp`·테이블 `app_runtime_state` 없이 빌드 또는 서버 실행되지 않고, 다른 설치 식별자·빈 DB에서는 DDL/시드 생성 없이 차단되며 출력에 비밀정보가 포함되지 않는지 확인
- `npm run test:live-production-runtime` 통과. 운영 사전 점검이 읽기 전용 트랜잭션으로 revision·설치 식별자·DB identity·최소 데이터 수·관리자 자격증명 형태를 확인하고 비밀정보를 출력하거나 상태를 변경하지 않는지 검증
- `npm run test:production-recovery-manifest` 통과. 복구 manifest가 승인된 배포·소스·빌드·Neon·런타임·스모크 식별자만 기록하고 연결 문자열·자격증명·토큰을 포함하지 않는지 검증
- `npm run test:admin-credential-recovery` 통과. 관리자 자격증명 복구가 기본 검증 전용이며 명시적 승인, 기대 revision, 설치 식별자, advisory lock을 갖춘 경우에만 비밀번호 변경·세션 폐기·감사 기록을 하나의 트랜잭션으로 수행하는지 검증
- `npm run build` 통과
- `npm run lint`와 `npm run build`가 `node_modules/.bin` shim 없이도 직접 package entrypoint로 실행 가능
- Next production build trace에서 `.data` 런타임/증빙 산출물을 제외
- `npm audit --audit-level=moderate` 통과
- `npm run test:unit` 통과
- `npm run test:adversarial-design-fixes` 통과. 역할별 모바일 화면의 우선순위, 지점 식별, 공지 본문, 출석 명단, 학부모 행동, 대표 리포트와 내비게이션 회귀를 확인
- `npm run test:final-common-fee-policy` 통과. 공통 회비 상품·할인·1+1 연장 규칙과 유효 날짜 경계를 확인
- `npm run test:final-main-schedule-policy` 통과. 본관 전용 수업 시간표·훈련 프로그램과 전 지점 공통 정책의 분리를 확인
- `npm run test:final-common-promotion-policy` 통과. 전 지점 공통 승급 기간·월 수련시간·1일 최대 인정시간·심사일·정확한 다음 띠·코치 범위를 확인
- `npm run test:promotion-api-integrity` 통과. 격리 저장소와 서버에서 잘못된 생성·결과 본문 및 201자 회원 ID·501자 공개 메모의 무변경 차단, 비심사일 등록, 미래 결과, 띠 건너뛰기, 지점·코치 범위, 동시 등록 고유 ID와 같은 대상 중복 반영 차단을 확인
- `npm run test:final-policy-ui` 통과. 회비·본관 시간표·승급 기준 UI가 역할별 화면에 연결되고 정책 원문과 어긋나지 않는지 확인
- `npm run test:role-csv-export-gates` 통과. 회원/학부모 차단, 대표/총괄 최신 계정·지점 범위 재검사, 동시 결제·운영 CSV 4건과 고유 감사 기록 4건 보존을 확인
- `npm run test:deleted-request-surface` 통과
- `npm run test:store` 통과. JSON runtime store의 동일 키 작업이 FIFO 순서로 직렬화되고 같은 base revision의 추가·서로 다른 필드 수정은 병합되며 동일 필드 충돌은 차단되고 revision/base 메타가 파일에 직렬화되지 않는지 확인
- `npm run test:store`에서 서로 다른 JSON store 인스턴스와 독립 Node 프로세스가 같은 stale 파일을 동시에 써도 두 변경이 보존되고, 쓰지 않은 캐시 보유 인스턴스도 다음 조회에서 최신 파일을 읽는지 확인한다. 정규화 휴대폰·이메일 중복과 새로 생성되는 고아 참조는 저장 경계에서 차단하되, 기존 고아 참조는 읽기 중 자동 변경하지 않고 명시적 복구 대상으로 진단해야 한다.
- `npm run test:store-write-validation`, `npm run test:runtime-state-integrity`, `npm run test:runtime-state-tools` 통과. JSON/PostgreSQL 쓰기 전후 검증 훅, 비파괴 진단, 기존 결함 호환·새 결함 차단, `system.integrity.repair` 감사 기록을 남기는 명시적 복구, 출석·결제 원본 보존, 운영 복제본 리포트 비식별화를 확인한다.
- `npm run test:next-build-readiness` 통과. 현재 Next dist의 `BUILD_ID`·빌드 입력 SHA-256 지문 누락, mtime 보존 수정, 소스 삭제·이름 변경, public 추가, 검증 중 빌드 변경 차단과 최신 빌드 허용을 확인하고, `FINAL_JUDO_NEXT_DIST_DIR` 격리 빌드가 기본 `.next` manifest를 덮어쓰지 않는지 검증
- `npm run test:release-smoke-isolation` 통과. 빈 로컬 포트, run-owned 임시 JSON 디렉터리·실제 `PILOT_DB_FILE`·마커 강제, 상속 토큰 교체, 공유 경로·심볼릭 링크·원격/기존 서버 재사용 차단, 종료 후 임시 데이터 제거를 확인
- `npm run test:admin-user-management-api` 통과. 최신 production build를 확인한 뒤 임시 DB와 임시 `next start` 서버에서 공개 가입 지점 최소 필드 목록, 복수 지점 선택 필수, 위조 지점 차단, 선택 지점 귀속, 동시 휴대폰 가입 유일성, 총괄 사용자 수정/삭제/역할 변경의 본문 타입·계정 필드·연결 배열·사유·비밀번호 길이 무변경, 초대 본문 타입·계정 필드/원본 지점 배열 길이·동시 ID/휴대폰 유일성, 역할 변경 사유/원본 지점 배열 길이 무변경, 지점 생성/수정/대표 배정 API, 인증/권한 선확인, 계정 단위 로그인 제한 중 정상 비밀번호 복구, 코치 역할 제거 시 수업·회원 자동 인계, 단독 대표 보호, 선택 비밀번호 변경, 전용 비밀번호 재발급, 원문 비밀번호/password hash 미노출과 변경 기록을 확인
- `npm run test:dashboard-priority-kpi` 통과. 총괄 대시보드 첫 화면 KPI가 내부 변경 기록 요약으로 회귀하지 않고 대기 초대와 사용자 관리 액션을 우선 표시하는지 확인
- `npm run test:member-profile-guardian-edit` 통과. `/app/members` 운영자 회원 상세에서 연령 수정 저장 요약, 보호자 검색 기반 변경/해제 UI, 보호자 ID 200자 상한, 인증·지점 선검사, 공통 잠금 밖 본문 파싱과 잠금 안 최신 권한 재검사, 보호자-자녀 양방향 링크·동시 감사 ID 갱신 API와 smoke 회귀 범위를 확인
- `npm run test:guardian-age-policy-ui` 통과. 운영자 회원 상세의 연령 정책과 보호자 연결 후보가 성인/유소년/청소년 기준으로 렌더링되고, 회원 검색 지우기 컨트롤이 44px 터치 목표를 유지하는지 확인
- `npm run test:admin-user-guardian-bottom-safe-area` 통과. 관리자 사용자 목록의 44px 액션 버튼과 사용자 상세의 보호자 연결 선택/해제 목록/저장 액션이 모바일 하단 내비게이션에 가려지지 않는지 확인
- `npm run test:api-auth-order` 통과. 보호 API route handler의 `request.json()` 본문 읽기가 `requireSession()` 이후에만 실행되고, 공개 body route allowlist가 로그인/비밀번호 재설정/초대 수락/결제 webhook으로 유지되는지 확인
- `npm run test:attendance-mutation-safety` 통과. 출석 등록·해제·사유 변경이 공통 서버 잠금 안에서 직렬화되고, URL 선택 지점과 수업 지점 불일치 및 수업 시작 전 변경을 차단한다. 일괄 저장은 JSON 객체·배열 항목·필드 타입·200건 상한·회원 ID 200자·메모/사유 80자·회원 중복을 검증한다. 본문은 권한 선검증 후 공통 잠금 밖에서 읽고 잠금 안에서 최신 권한·수업 상태를 재검증하며, 격리 스모크는 느린 미완성 일괄 본문을 읽는 중에도 별도 출석 사유 저장이 먼저 완료되는지 확인한다. 잘못된 요청은 기존 출석·감사 기록을 변경하지 않고 저장 실패 건은 재시도 큐에 유지되는지 확인
- `npm run test:login-keep-signed-in` 통과. 390px 로그인 화면에서 로그인 상태 유지 선택지가 44px 이상 터치 영역으로 보이고, 선택 시 30일/해제 시 8시간 세션 쿠키가 설정되는지 확인. 로그인된 상태 또는 HttpOnly 쿠키만 남은 앱 재시작 상태로 `/login`에 진입했을 때 `로그아웃하고 계정 전환` 액션도 44px 터치 높이를 유지해야 한다.
- `npm run test:auth-session-security` 통과. 사용자 ID를 세션 쿠키로 직접 넣어도 인증되지 않고, 무작위 토큰 원문은 저장소에 남지 않으며 만료·로그아웃·사용자 전체 세션 폐기와 계정 단위 실패 제한 중 정상 자격증명 복구가 적용되는지 확인
- `npm run test:invitation-token-security` 통과. 256-bit 초대 토큰의 해시 저장·7일 만료·단일 사용, 사용자별 비밀번호 실패 제한, 257자 비밀번호의 해시 전 차단·원문 미저장, 초대 수락 뒤 휴대폰·동일 비밀번호 로그인과 로그아웃 후 재로그인, 비관리자·담당 밖 지점 재발급 차단, 재발급 시 이전 링크 무효화와 bootstrap 해시 미노출을 확인
- `npm run test:local-demo-password-rotation` 통과. 공용 데모 비밀번호의 고정/임의 salt 해시를 모두 탐지하고, 명시적 격리 JSON 확인 없이는 쓰지 않으며 원본·PostgreSQL·공개 산출물에 비밀번호를 남기지 않는지 확인
- `npm run test:notification-outbox` 및 `npm run test:notification-outbox-integration` 통과. 구독별 멱등 enqueue, lease/revision 경쟁, 지수 backoff, 최대 시도, 404/410 비활성화, 수동 재발송 멱등 digest, 공지 변경/삭제 취소 경계, provider timeout·불확실 전송·stale settlement 감사, CRON_SECRET 인증과 endpoint/key 없는 시도 감사 기록을 확인
- `npm run test:phone-signup-login-flow` 통과. 고유 로컬 포트와 실행별 임시 JSON 저장소의 390px 휴대폰 회원가입에서 서버가 제공한 지점 선택기(44px, 가로 overflow 0)를 거쳐 선택 지점에 가입하고, 31자 이름·257자 비밀번호·255자 로그인/재설정 식별자를 계정 생성·비밀번호 검증 전에 400으로 차단한다. 정상 입력은 같은 휴대폰과 비밀번호로 가입 완료 안내 로그인 화면과 회원 대시보드까지 이동하며, 폼의 이름·휴대폰·비밀번호 `maxLength`와 잘못된 비밀번호 문구 비노출을 확인한다. 최신 브라우저 증빙은 `.data/mobile-builds/ios/phone-signup-branch-selection-20260719/summary.json`에 보관하며 기존 iPhone 16e Simulator 증빙은 `.data/mobile-builds/ios/phone-signup-login-flow-ios-20260705/summary.json`에 보관
- `npm run test:payment-lifecycle` 통과. 수기 결제 등록이 처리자·지점 범위의 `Idempotency-Key` 재시도를 한 건으로 유지하고 다른 payload나 삭제된 원본의 키 재사용을 차단하며, UUID 식별자를 사용하고 환불액 없는 부분 환불 직접 생성을 거부하는지 확인한다. 취소·환불 완료 상태 등록은 사유를 필수로 받고 정규화한 사유를 최초 상태 이력·감사 스냅샷·확인값에 저장하며, 일반 상태 확인값은 기존 형식과 호환되는지 검증한다. 회원권명 100자와 등록·수정·삭제·환불/취소 사유 500자 공통 상한, 존재하지 않는 달력 날짜와 납부일보다 앞선 만료일을 차단하고, 동일 결제의 수기 변경과 온라인 요청은 직렬화되며 삭제 감사 스냅샷에는 취소·상태 이력이 유지되고, 등록과 환불/취소 API가 세션과 역할/지점 권한을 요청 본문 검증보다 먼저 확인하는지 검증
- `npm run test:auth-production-guard` 통과. production 데모 로그인 차단·세션 쿠키 정책, 로그인/회원가입/초대 수락/재설정의 공통 입력 상한, 로컬 자동로그인 `next`의 같은 오리진·2,048자 상한과 안전하지 않은 경로의 대시보드 대체가 서버와 폼에 연결되는지 확인
- `npm run test:dev-reset-guard` 통과
- `npm run test:env-readiness` 통과
- `npm run test:deployment-handoff-draft` 통과
- `npm run test:deployment-handoff` 통과
- `npm run test:preflight` 통과
- `npm run test:preflight-runtime` 통과
- `npm run test:pilot-evidence` 통과
- `npm run test:pilot-field-evidence-draft` 통과
- `npm run test:pilot-field-evidence` 통과
- `npm run test:pilot-storage-receipt-draft` 통과
- `npm run test:pilot-storage-receipt` 통과
- `npm run test:pilot-final-handoff` 통과
- `npm run test:pilot-status` 통과
- `npm run test:p1-handoff-draft` 통과
- `npm run test:p1-handoff-checklist` 통과
- `npm run test:p1-handoff-bundle` 통과
- `npm run test:p1-handoff-dispatch` 통과
- `npm run test:p1-handoff-dispatch-apply-csv` 통과
- `npm run test:p1-handoff-issues` 통과
- `npm run test:p1-github-connector-readiness` 통과
- `npm run test:p1-handoff-issue-receipt` 통과
- `npm run test:p1-evidence-intake-draft` 통과
- `npm run test:p1-evidence-intake` 통과
- `npm run test:p1-readiness` 통과
- `npm run test:p1-operator-status` 통과
- `npm run test:p1-operator-status-apply-external-blockers-csv` 통과
- `npm run test:p1-completion-evidence` 통과
- `npm run test:p1-completion-evidence-apply-csv` 통과
- `npm run test:p1-release-package` 통과
- `npm run test:team-agent-prompts` 통과
- `npm run test:implementation-backlog` 통과
- `npm run test:qa-plan` 통과
- `npm run test:release-docs` 통과
- `npm run test:admin-settings-gates` 통과
- `npm run test:p3-operations` 통과. P3 운영 고도화 화면은 확인하되 P1 release blocked 상태는 유지
- `npm run test:p4-simulator-rehearsal` 통과. P4 실제 사용 환경 검증은 앱 UI에는 P4 내부 상태판을 노출하지 않고 iOS Simulator 역할별/상태별 캡처 5개를 확인하되 iOS Simulator 성공을 IPA ready로 보지 않음 상태를 유지
- `npm run p5-p10:internal-audit`와 `npm run test:p5-p10-internal-readiness` 통과. P5~P10 내부 readiness audit는 상단 safe area header 강화, 하단 모바일 내비게이션 active 메뉴 자동 정렬, 코치 모바일 저장 상태 패널 safe-area bottom inset과 하단 스크롤 여백, Next 개발 표시 배지 비활성화, 역할별 `autoLogin` deep link 실제 서비스 화면 진입, 앱 UI 내부 release/audit 카드 비노출, `.data/mobile-builds/ios/p5-p10-simulator-screenshots` 캡처 4개, `.data/mobile-builds/ios/visible-copy-expanded`의 41개 역할/경로 캡처, `.data/mobile-builds/ios/visible-text-audit-20260621-recheck`의 관리자 변경 기록/코치 회원 화면 recheck 캡처, `.data/p5-p10-internal-audit.json`, `.data/p5-p10-internal-audit.md`, P1 외부 blocker blocked 7/7을 확인하되 출시 완료, IPA ready, 운영 ready로 보지 않음 상태를 유지
- `npm run test:notice-delete-ui` 통과. 대표/총괄/코치 공지 목록의 삭제 권한과 삭제 후 목록/알림 상태, 읽음 뒤 알림 발송 시 오래된 피드백 정리, 관리자 공지 수정 뒤 읽음 초기화, 동일 내용 재저장 시 읽음 유지, 코치 대상 변경 차단, 감사 로그 본문 미저장을 확인
- `npm run test:notice-compose-safety` 통과. 공지 작성 대상·확인 단계와 중복 발행 방지 계약을 확인
- `npm run test:visible-app-copy-stability` 통과. 390px 모바일 실제 화면에서 로그인/회원가입/비밀번호 재설정/초대 수락/계정 전환 진입 화면과 역할별 앱 화면이 FINAL 벡터 로고, 필수 폼/계정 전환 버튼, 콘솔 에러 없음, 더미/샘플/테스트 계정/릴리즈 내부 문구 비노출, 가로 overflow 없음, runtime error 없음 상태를 유지하고 로그인/회원가입/초대 수락 비밀번호 표시 토글이 44px 터치 목표와 입력 우측 여백을 유지하는지 검증. 회원/학부모 계정 화면은 불필요한 활성 상태 카드 없이 계정 전환/로그아웃 44px 액션을 검증. 회원/학부모 결제 필터는 visible `보기` 제목 없이 44px 한 줄 필터로 유지하고 금액/CSV를 노출하지 않는다. 코치 회원 화면은 상태 필터와 개인 공지 액션이 44px 터치 높이를 유지하고 하단 내비게이션에 가리지 않는지 검증한다. 대표 대시보드 `오늘/7일/30일` 기간 필터, 관리자 대시보드 `오늘 수업 보기`, 사용자 관리 `회원 등록`/`초대`, 변경 기록 `새로고침`은 44px 터치 높이를 유지한다. 요청 작성 카드, 요청 목록 카드, 요청 하단 메뉴, `/app/requests?compose=1` 직행 링크는 노출하지 않는다. 관리자 사용자 목록 수정/삭제/비밀번호 재발급 액션은 compact 세로 스택으로 보이며 수정 폼의 선택 비밀번호 입력 2개와 수정/삭제/재발급 폼 접힘/토글 상태를 검증. 코치 수업 화면은 내부 P3 운영 패널과 코드형 마감 문구가 실제 앱 DOM에 노출되지 않는지 확인. 대표 리포트도 내부 P3 운영 패널과 코드형 운영 확인 문구 없이 지점별 회원/출석/위험/매출 그래프를 유지하는지 확인. 대표 내 지점 화면은 회원 유지/수업 채움/결제 위험 그래프와 44px 액션 버튼, 목록 끝 112px 하단 safe-area와 scroll-end 액션 clearance 24px 이상을 확인하고, 회원 `/app/notifications` 공지 alias는 compact 공지 필터와 읽음 액션을 확인
- 관리자 지점 운영 설정 폼을 열었을 때 지점명/지역/상태/시간대 입력과 출석 수정 정책 토글, `설정 저장` 액션은 모두 44px 터치 높이를 유지해야 한다. `npm run test:visible-app-copy-stability`가 `adminBranchSettingsOpenFieldGridMinControlHeight=44`, `adminBranchSettingsOpenPolicyControlHeight=44`, `adminBranchSettingsOpenSaveHeight=44`를 기록하고, iPhone 16e Simulator 증빙은 `.data/mobile-builds/ios/admin-branch-settings-touch-20260705/summary.json`에 보관한다.
- `/select-role` 하단의 `로그인 화면` 링크와 로그인된 `/login`의 `로그아웃하고 계정 전환` 액션은 모바일에서 44px 터치 높이를 유지해야 한다. `npm run test:visible-app-copy-stability`는 `authSelectRoleLoginLinkHeight=44` 이상을, `npm run test:login-keep-signed-in`은 같은 클라이언트 상태와 쿠키만 남은 앱 재시작 상태의 계정 전환 버튼 높이 44px 이상, 390px overflow 0을 기록한다.
- 대표 대시보드 지점 비교 카드는 390px 모바일 첫 화면에서 위험 알림보다 먼저 보이고 하단 내비게이션과 24px 이상 여백을 유지해야 하며, 위험 알림 요약도 같은 화면에서 하단 내비게이션에 가리지 않아야 한다. `npm run test:visible-app-copy-stability`가 `ownerBranchComparison*BottomNavClearance`와 `ownerDashboardRiskSummaryBottomNavOverlap` 값으로 회귀를 차단한다.
- `npm run test:postgres-doctor` 통과
- `npm run test:online-payments` 통과
- `npm run test:family-payment-checkout` 통과. 회원/학부모 결제 카드가 내부 결제 준비 화면으로 이동하고, 성인 회원 직접 결제와 학부모 자녀 결제 허용, 유소년/청소년 회원 직접 결제 차단, 납부 요청 이름·연락처 UI/서버 상한과 본문 잠금 밖 검증·잠금 안 최신 권한 재확인, 실 PG/API 미연결 상태를 검증
- `npm run test:payment-checkout-method-flow` 통과. 390px 모바일 결제 상세에서 성인 회원/학부모 결제자 정보 필수 입력, 주소·일반전화·이메일 추가 영역 기본 접힘/펼침, compact 납부 요약, 자연스러운 이메일 placeholder, 무통장입금/신용카드/가상계좌/계좌이체 선택, 카드사 선택, 카드 안내 버튼 44px 터치 목표, 우리WON페이 모달, 저장/1회성 납부 정보 확인 피드백과 polite status live region, 학부모 자녀 결제 화면이 overflow/콘솔 오류 없이 동작하고 하단 고정 내비게이션에 `납부 정보 확인` 버튼과 `납부 정보 접수` 안내가 가리지 않는지 검증
- `npm run test:tournament-access` 통과. 역할·지점별 대회 조회/관리 권한, 잘못된 본문 타입·존재하지 않는 달력 날짜 무변경 차단, 동시 등록·수정·감사 기록 보존을 확인
- `npm run test:payment-create-touch-targets` 통과. 390px 모바일 대표 결제 화면에서 상단 결제 내보내기/상태 필터/표시 건수, 결제 행 재등록/온라인 요청/정기결제 약정/환불/취소 액션, 수기 결제 등록 폼의 접힘/열림, 회원 검색 결과 선택, 취소·환불 완료 상태의 44px 조건부 사유 입력과 빈 사유 제출 차단, 등록 중 중복 제출 차단, 서버 저장 뒤 응답 유실 시 동일 `Idempotency-Key` 재시도와 정확히 1건의 목록 반영, 성공 안내, overflow 0, 콘솔 오류 없음 상태를 검증. iPhone 16e 조건부 사유 입력 증빙은 `.data/mobile-builds/ios/manual-payment-create-audit-reason-20260713/simulator-summary.json`에 보관
- `npm run test:class-management-touch-targets` 통과. 390px 모바일 대표 수업 화면에서 수업 생성 폼 기본 접힘, 생성 입력/등록 버튼, 수업 장소/정원 수정 입력/저장 버튼이 44px 이상이고 수업명 80자·레벨 40자·장소 80자 UI 상한을 유지한다. 코치 수업 화면의 출석 메모 토글/입력/빠른 메모/사유 저장도 44px 이상이고 메모 80자 UI 상한, overflow 0, 콘솔 오류 없음 상태를 검증
- `npm run test:member-management-touch-targets` 통과. 390px 모바일 대표 회원 관리 컨트롤 44px, 회원 상세 결제·회원권 요약의 가족 금액 비노출·코치 전체 비노출, 학부모 자녀·결제 딥링크와 이후 수동 자녀 선택 유지, overflow 0, 콘솔 오류 없음, iPhone 16e 네이티브 셸을 검증
- `npm run test:admin-user-management-touch-targets` 통과. 390px 모바일 총괄 사용자 관리 화면에서 초대 폼 기본 접힘, 초대 이름 80자·휴대폰 40자·이메일 254자, 수정 설명 120자·비밀번호 256자, 삭제·재발급 사유 500자 상한, 목록 액션, 초대/수정/삭제/비밀번호 재발급 입력과 저장 버튼이 44px 이상이고 하단 내비 clearance, overflow 0, 콘솔 오류 없음, iOS Simulator 앱 chrome 증빙을 검증
- `npm run test:operator-list-search` 통과. 390px 모바일 운영자 결제 목록과 공지함 `q` 검색 딥링크/검색어 지우기, 0건 검색 빈 상태, 공지 0건 상태의 읽음 처리 액션 숨김과 하단 내비 clearance, 검색 입력/인라인 지우기/빈 상태 지우기 44px 터치 목표가 표시 건수, 목록 축소, overflow 0, 콘솔 오류 없음 상태를 유지하는지 검증
- `npm run test:global-search` 통과. 대표/총괄 상단 검색이 권한 범위 안의 메뉴·회원·결제·공지·사용자를 통합 검색하고, 코치에게 결제·사용자 민감 결과를 반환하지 않으며 실제 목록 검색 딥링크로 연결되는지 검증
- `npm run test:admin-role-search` 통과. 390px 모바일 총괄 권한 관리의 `q` 검색 딥링크, 검색 입력/인라인 지우기/0건 빈 상태 `전체 보기` 44px 액션, 검색 중 부가 패널 숨김, 하단 safe-area spacer와 내비 clearance, overflow 0, 콘솔 오류 없음 상태를 검증
- `npm run test:admin-user-search` 통과. 390px 모바일 총괄 사용자 관리의 `role`+`q` 검색 딥링크, 검색어 지우기 시 역할 필터 보존, 0건 빈 상태 `전체 보기`의 q/role 복구, 검색 입력/지우기/필터 초기화/빈 상태 초기화 44px 터치 목표, 하단 내비 clearance, overflow 0, 콘솔 오류 없음 상태를 검증
- `npm run test:admin-audit-search` 통과. 390px 모바일 총괄 변경 기록의 `q` 검색 딥링크, 검색어 120자 상한, 검색어 지우기와 `전체 보기` URL 복구, 0건 빈 상태 초기화, 필터 입력/지우기/적용/초기화 44px 터치 목표, 하단 내비 clearance, overflow 0, 콘솔 오류 없음, 공통 처리/결과 라벨과 승급 심사·대회 공지 변경 기록 API 필터 허용, 검색어 121자·사유 501자·지점 ID 201자 요청의 잠금 전 400 무감사 차단, 동시 동일 조회의 단일 감사 기록 보존을 검증
- `npm run test:recurring-billing` 통과. 정기결제 약정 생성/해지 API가 세션과 역할/지점 권한을 요청 본문 검증보다 먼저 확인하고, 실제 달력 날짜·1-28 청구일·문자열 해지 사유, 공통 결제 잠금, 최신 권한·상태 재검증, 저장 충돌 409 매핑을 유지하는지 확인. 격리 `test:smoke`는 동시 생성 `200/409`, 동시 해지 `200/422`, 성공 작업별 상태 이력·감사 기록 1건을 검증
- `npm run test:payment-provider-handoff-draft` 통과
- `npm run test:payment-provider-handoff` 통과
- `npm run test:routes` 통과
- `npm run test:e2e` 통과
- `npm run test:smoke` 통과. 회원 등록/수정, 보호자 연결, 상담/주의 메모 작성, 출석 저장/사유 기록, 수업 생성/수정, 삭제된 요청 생성/승인/반려 API의 `404`, 공지 작성/일괄 읽음, 알림 구독/해지, 파일럿 준비 상태/운영 이슈/로그 API가 세션과 역할/지점/회원/담당 수업/대상 접근 권한을 요청 본문 검증보다 먼저 확인하는지 포함
- `npm run test:attendance-speed` 통과
- `npm run test:mobile-install` 통과. Android 내부 설치용 `android-capacitor-webview-report.json`이 있으면 `INSTALL_ONLY_final-judo-native-webview-debug.apk` 해시/크기, 동등본 `final-judo-native-webview-debug.apk`, APK 내부 런처 아이콘 proof, `INSTALL_ANDROID_WEBVIEW_APK.txt` 설치 가이드, TWA/Bubblewrap `app-debug.apk`와 과거 `mobile/android/twa/app-release-signed.apk` 설치 금지 경로, `DO_NOT_INSTALL_FOR_FIELD_WEBVIEW.txt` 오설치 방지 마커와 주소창 없는 WebView 설치 경고 문구까지 확인
- `npm run test:android-packaging` 통과. Android 런처/splash/TWA store icon은 `public/icons/final-judo-icon-512.png` 검정 FINAL 심볼 PNG에서 생성되어야 하고, PWA manifest의 stale SVG 우선 노출, SVG 대체 아이콘, Capacitor 기본 템플릿 아이콘이 재유입되면 실패해야 함
- `npm run test:android-play-release-artifacts` 통과. 최신 Play release report `.data/mobile-builds/android-play-release-20260721064936/google-play-release-report.json`의 `versionCode 40`/`versionName 1.0.39`, 운영 로그인 URL, timestamped AAB/APK, Desktop AAB/APK, jarsigner/apksigner 검증, APK badging이 서로 일치해야 함
- `npm run test:android-role-apks` 통과
- `npm run android:twa:doctor` 실행 결과 JSON blockers/ok 상태 확인
- `npm run test:ios-capacitor-connection` 통과
- `npm run test:ios-ipa-doctor` 통과
- `npm run test:ios-provisioning-runbook` 통과
- `npm run ios:ipa:doctor -- --out=.data/mobile-builds/ios/ios-ipa-doctor.json --markdown=.data/mobile-builds/ios/ios-ipa-doctor.md` 실행 결과 JSON blockers/ok 상태 확인. `mobile/ios/release-config.json`의 non-secret Team ID/bundle id 기본값으로 Apple Team ID false blocker가 생기지 않고, 운영 origin/provisioning profile blocker는 유지되어야 함
- `npm run test:android-release-handoff-draft` 통과
- `npm run test:android-release-handoff` 통과. Android 설치 smoke는 주소창/공유/더보기 브라우저 UI 비노출을 포함해야 함
- `npm run test:notification-readiness` 통과
- `npm run test:notification-push-handoff-draft` 통과
- `npm run test:notification-push-handoff` 통과
- `npm run test:db` 통과
- `npm run test:postgres-store` 통과. 서로 다른 PostgreSQL runtime store 인스턴스가 같은 결제 요청 키를 advisory lock으로 직렬화하고 잠금 내부 read/write가 성공 시 커밋·실패 시 롤백되며, stale snapshot의 서로 다른 추가가 병합되는지 확인한다. 임시 Next 서버에서는 동시 결제 route 요청이 결제·감사 기록 각각 1건만 저장되고, DB row barrier 뒤 같은 오래된 revision에서 시작한 결제·출석 요청이 두 변경과 두 감사 기록을 모두 보존하는지 Docker PostgreSQL에서 확인
- `npm run test:pilot` 통과
- `npm run test:pilot-readiness-contract` 통과. 파일럿 준비·이슈·운영 로그의 공통 문자열 상한, 잠금 전 차단 순서, 관리자 설정 입력의 동일 `maxLength` 계약을 확인
- `npm run test:pilot-import` 통과
- `npm run test:pilot-prelaunch-draft` 통과
- `npm run test:pilot-launch-package` 통과
- `npm run test:pilot-launch-command` 통과
- `npm run test:pilot-readiness-evidence` 통과
- `npm run test:pilot-readiness-evidence-apply` 통과
- `npm run test:pilot-password-rotation` 통과
- `npm run test:a11y-static` 통과
- `npm run test:release` 통과
- `npm run test:release`는 route/mobile E2E/API smoke/attendance-speed 구간에서 빈 로컬 포트와 임시 JSON 저장소를 할당해 직전 production build를 `next start`로 직접 실행하고 종료 후 데이터를 정리한다. 데모 로그인·reset 허용은 이 격리 자식 프로세스에만 한정하고 이미 응답하는 서버·원격 `SMOKE_BASE_URL`·실행 중인 개발 서버는 재사용하거나 종료하지 않는다. 단독 smoke 명령은 로컬 서버를 먼저 실행해야 한다.
- 5개 역할이 각자 허용된 메뉴만 본다.
- 직접 URL 접근이 허용되지 않은 화면은 403을 보여준다.
- 코치는 결제 금액과 다른 지점 데이터를 보지 않는다.
- 학부모는 연결된 자녀 데이터만 본다.
- 대표는 전체 지점/단일 지점 필터로 KPI와 지점 비교를 확인한다.
- 대표는 전용 내 지점/운영 리포트 화면에서 자기 지점 데이터만 확인한다.
- 총괄 어드민은 사용자/역할 목록, 권한 매트릭스, 변경 기록, 운영 설정을 확인한다.
- 총괄 어드민은 운영 설정에서 자동 릴리즈 게이트, 보안 audit, 운영 데이터 점검, 파일럿 현장 확인 항목의 상태/담당자/증빙을 확인한다.
- 총괄 어드민은 초대 링크를 생성하고 초대 수락/재설정 요청 변경 기록을 확인한다.
- 모바일 출석 버튼은 48px 이상 터치 영역, 코치 모바일 빠른 조치 패널, 수업 단위 전체 출석 일괄 저장, 수업별 처리율/미처리 인원, 명단 검색, 모바일 가로 칩 상태 필터, 빠른 메모, 미처리만 보기 필터, 최근 변경 되돌리기, sticky 저장 상태, 새로고침 후 복구되는 오프라인 대기 큐와 재동기화 액션을 제공한다. 390px 화면에서는 상태별 중복 요약 카드를 숨기고 기본 저장 상태 문구를 한 줄로 압축한다.
- `npm run test:a11y-static`이 버튼 이름, 폼 라벨, 이미지 alt, tabIndex 기본 규칙을 통과한다.
- 공지는 대상, 중요 표시, 읽음 수, 내 읽음 상태, 개별/보이는 공지 읽음 처리, 전체/미읽음/중요 필터, 전역 미읽음 배지를 제공한다.
- 상담/주의 메모는 역할별 공개 범위를 지키고 작성 변경 기록을 남긴다.
- 개발/테스트 전용 reset API는 production에서 기본 차단된다.
- 데모 역할 로그인은 production에서 기본 차단되고 session cookie는 production에서 secure/httpOnly 속성을 가진다.
- 운영 계정 휴대폰 번호/임시 비밀번호 로그인은 성공하고 잘못된 비밀번호는 실패하며, bootstrap/snapshot 응답에 `passwordHash`가 노출되지 않는다. 로그인 상태 유지 선택 시 세션 쿠키는 30일 유지되고, production에서는 localStorage 기반 `x-user-id` 헤더가 세션 대체 수단으로 동작하지 않는다.
- 총괄 어드민은 사용자별 임시 비밀번호를 발급하고, 발급 사유와 `auth.password_reset.complete` 변경 기록을 확인할 수 있다.
- 총괄 어드민은 파일럿 운영 중 P0/P1/P2 이슈와 우회책을 기록하고 `pilot_incident.create/update` 변경 기록을 확인할 수 있다.
- 총괄 어드민은 파일럿 일일 운영 로그에 출석/결제/공지 후속/공지 확인 수치와 증빙을 저장하고 `pilot_operation.update` 변경 기록을 확인할 수 있으며, 고유 운영일 기준 14일 누락일과 다음 입력 추천일을 확인한다.
- 운영 직전 `NODE_ENV=production npm run preflight:pilot -- --out=.data/pilot-preflight.pre-pilot.json`이 기본 임시 비밀번호, production 플래그, 온라인 결제 provider/checkout/webhook secret, 파일럿 준비 증빙, 미해결 P0 이슈, 주요 참조 무결성 차단 조건을 통과하고 결과 JSON을 남긴다.
- 파일럿 종료 후 `NODE_ENV=production npm run preflight:pilot -- --require-retro --out=.data/pilot-preflight.post-pilot.json`가 14개 운영일, 출석 기록 수치, 결제 상태 확인 수치, 회고 증빙을 통과하고 결과 JSON을 남긴다.
- 파일럿/앱 배포 산출물을 채우기 전 `npm run p1:handoff-draft -- --out-dir=.data`가 운영 배포, Android release, iOS IPA build/provisioning, 결제 provider, 운영 푸시, 파일럿 status 6개 handoff/IPA 초안/blocked 리포트, Postgres/Docker doctor JSON/Markdown, JSON/CSV/Markdown 담당자별 action checklist, 6인 팀 action brief, 담당자별 Markdown brief 폴더, 담당자별 handoff package index/package 폴더, bundle manifest, dispatch receipt 초안/blocked 리포트, issue draft, GitHub CLI 등록 명령, GitHub connector payload, GitHub JSON/CSV 등록 결과 템플릿, issue registration plan, issue registration receipt 초안/blocked 리포트, P1 readiness 감사 리포트, P1 final evidence intake JSON/CSV/Markdown을 생성하고 GitHub 이슈 생성 입력 repo를 `--github-repo`, `GITHUB_REPOSITORY`, 로컬 `git remote origin` 순서로 고정한다. GitHub connector 응답 변환은 REST식 `html_url`, normalized `htmlUrl`, API issue `url`, `displayTitle`, `labels.nodes`, `assignees.nodes` 형태를 모두 receipt results로 정규화한다.
- handoff blocked 리포트를 갱신한 뒤 `npm run p1:handoff-checklist -- --workspace=.data`가 JSON/CSV/Markdown 담당자별 action checklist, 6인 팀 action brief, 담당자별 Markdown brief 폴더, 담당자별 handoff package index/package 폴더를 재생성한다.
- handoff 산출물 전달 전 `npm run p1:handoff-bundle -- --workspace=.data --out=.data/p1-handoff-bundle-manifest.json`가 담당자별 brief/package index/package 폴더와 `team-agent-prompts.md`의 SHA-256/byte size manifest를 생성하고 원문 secret-like 값 유입을 차단한다.
- handoff package 전달 시 `npm run p1:handoff-dispatch:draft -- --bundle=.data/p1-handoff-bundle-manifest.json --out=.data/p1-handoff-dispatch-receipt.json --markdown=.data/p1-handoff-dispatch-receipt.md --csv=.data/p1-handoff-dispatch-receipt.csv`로 JSON/Markdown/CSV receipt 초안을 만들고, 운영자가 생성된 `.data/p1-handoff-dispatch-receipt.csv`의 담당자/채널/due date/acknowledgement 증빙을 채운 뒤 `npm run p1:handoff-dispatch:apply-csv -- --receipt=.data/p1-handoff-dispatch-receipt.json --csv=.data/p1-handoff-dispatch-receipt.csv --out=.data/p1-handoff-dispatch-receipt.completed.json --markdown=.data/p1-handoff-dispatch-receipt.completed.md`로 completed receipt를 생성한다. 이후 `npm run p1:handoff-dispatch -- --file=.data/p1-handoff-dispatch-receipt.completed.json --bundle=.data/p1-handoff-bundle-manifest.json --out=.data/p1-handoff-dispatch-report.json`가 6개 owner package 전달 receipt와 원문 secret-like 값 미기록을 검증한다.
- 담당자별 업무 등록 전 `npm run p1:handoff-issues:draft -- --receipt=.data/p1-handoff-dispatch-receipt.completed.json --out-dir=.data/p1-handoff-issue-drafts`가 GitHub/Slack 등록용 owner별 issue draft Markdown, index, JSON manifest, `github-issue-create-commands.sh`, `github-connector-issue-payloads.json`, `github-connector-issue-responses.template.json`, `github-connector-runbook.md`, `github-issue-create-results.template.json`, `github-issue-create-results.template.csv`, `issue-registration-plan.json`을 만들고 strict 명령/담당자/due date/완료 기준, GitHub repo 자동 추론, 원문 secret-like 값 미기록을 검증한다. dispatch receipt가 blocked이면 `publishReady=false`와 CLI/connector 호출 금지 가드가 runbook에도 남아야 하며, 실제 외부 이슈 생성은 `publishReady=true` 이후에만 진행한다. `npm run test:p1-github-connector-readiness`는 connector payload repo와 로컬 origin 일치, `_create_issue` tool name, issue draft publish 상태 일치, runbook의 repo/publishReady/publishGuard/body hash/response target 정합성, blocked payload의 `publishGuard`를 별도 검증한다. `.data/p1-github-connector-access.json`에 GitHub connector `_get_repo` 응답이 있으면 같은 검증에서 외부 connector repo/default branch/권한 증빙도 확인한다.
- `issue-drafts.json`의 `publishReady=true`를 확인한 뒤 실제 외부 이슈/메시지를 등록하고 `github-issue-create-results.template.csv`을 `github-issue-create-results.csv`로 채운 다음 `npm run p1:handoff-issue-receipt:apply-results-csv -- --issue-drafts=.data/p1-handoff-issue-drafts/issue-drafts.json --csv=.data/p1-handoff-issue-drafts/github-issue-create-results.csv --out=.data/p1-handoff-issue-registration-receipt.json --markdown=.data/p1-handoff-issue-registration-receipt.md`가 local issue draft에서 body hash와 labels를 자동 계산한 receipt를 생성한다. `publishReady=false` 상태에서는 receipt 초안과 strict report, operator status가 GitHub connector/Slack 외부 이슈 등록을 권하지 않고 dispatch acknowledgement CSV/receipt 완료와 issue draft 재생성을 먼저 안내한다. GitHub connector `_create_issue` 응답을 사용하면 응답 JSON과 row별 `acknowledgement.status`/`acknowledgement.acknowledgedAt`/`acknowledgement.evidence`를 `.data/p1-handoff-issue-drafts/github-connector-issue-responses.json`에 보관하고 `npm run p1:handoff-issue-receipt:connector-results -- --issue-drafts=.data/p1-handoff-issue-drafts/issue-drafts.json --responses=.data/p1-handoff-issue-drafts/github-connector-issue-responses.json --out=.data/p1-handoff-issue-drafts/github-issue-create-results.json`로 results JSON을 만든 뒤 `apply-results`를 실행한다. 자동 연동이 이미 results JSON을 내보내면 `github-issue-create-results.template.json`을 `github-issue-create-results.json`로 채우고 `npm run p1:handoff-issue-receipt:apply-results -- --issue-drafts=.data/p1-handoff-issue-drafts/issue-drafts.json --results=.data/p1-handoff-issue-drafts/github-issue-create-results.json --out=.data/p1-handoff-issue-registration-receipt.json --markdown=.data/p1-handoff-issue-registration-receipt.md`를 사용한다. 직접 입력이 필요하면 `npm run p1:handoff-issue-receipt:draft -- --issue-drafts=.data/p1-handoff-issue-drafts/issue-drafts.json --out=.data/p1-handoff-issue-registration-receipt.json --markdown=.data/p1-handoff-issue-registration-receipt.md`로 receipt 초안을 만들고, 최종적으로 `npm run p1:handoff-issue-receipt -- --file=.data/p1-handoff-issue-registration-receipt.json --issue-drafts=.data/p1-handoff-issue-drafts/issue-drafts.json --out=.data/p1-handoff-issue-registration-report.json`가 외부 URL, 담당자, labels, body hash, acknowledgement 증빙과 원문 secret-like 값 미기록을 검증한다.
- 최종 외부 증빙 입력을 담당 영역별로 넘기기 전 `npm run p1:evidence-intake:draft -- --workspace=.data --readiness=.data/p1-readiness.json`가 운영 배포, Android release, 결제 provider, 운영 푸시, P1 handoff issue registration receipt, 파일럿 최종 status 리포트의 blocker/필수 증빙/strict 명령/TODO 입력 필드를 JSON/CSV/Markdown으로 생성하고 원문 secret-like 값을 기록하지 않는다.
- 운영자가 `.data/p1-evidence-intake-draft.csv`의 `evidenceOwner`, `evidenceUrl`, `checkedAt`, `signoff`를 채운 뒤 `npm run p1:evidence-intake:apply-csv -- --csv=.data/p1-evidence-intake-draft.csv --json=.data/p1-evidence-intake-draft.json --out=.data/p1-evidence-intake.completed.json`가 operator 필드만 completed JSON으로 적용하고 고정 열 변조, TODO, 원문 secret-like 값 미기록을 검증한다.
- P1 readiness가 ready가 된 뒤 `npm run p1:evidence-intake -- --file=.data/p1-evidence-intake.completed.json --readiness=.data/p1-readiness.json --out=.data/p1-evidence-intake-report.json`가 7개 lane의 owner/evidence URL/checkedAt/signoff, row 계약, readiness 경로, 원문 secret-like 값 미기록을 strict 검증한다.
- 외부 증빙 입력 중간 점검에서는 `npm run p1:operator-status -- --workspace=.data --allow-pending --out=.data/p1-operator-status.json --markdown=.data/p1-operator-status.md --external-blockers-csv=.data/p1-operator-status-external-blockers.csv`가 P1 readiness, action checklist, bundle/dispatch, GitHub connector access receipt, GitHub connector payload publish 가능 여부, issue registration/evidence intake, release package/archive/storage receipt 상태, Release Custody prerequisite/package/archive/storage receipt 섹션, iOS Local Profile Inventory(profile 파일 수, Team ID/bundle id 매칭 수, 등록 기기 포함 profile 수, UDID 원문 미기록), 6인 팀+iOS lane별 `externalBlockers`를 운영자용 JSON/Markdown으로 요약하고 외부 blocker CSV에 `evidenceOwner`/`evidenceUrl`/`checkedAt`/`signoff` 입력 열을 생성한다. 운영자가 7개 row를 모두 채운 뒤 `npm run p1:operator-status:apply-external-blockers-csv -- --csv=.data/p1-operator-status-external-blockers.csv --json=.data/p1-operator-status.json --out=.data/p1-operator-status-external-blockers.completed.json --markdown=.data/p1-operator-status-external-blockers.completed.md`가 completed JSON/Markdown을 생성하고 고정 열 변조, TODO/placeholder, HTTP 증빙 URL, non-ISO `checkedAt`, 원문 secret-like 값을 차단해야 한다.
- P1 완료 기준 점검에서는 `npm run p1:completion-evidence -- --workspace=.data --allow-pending --out=.data/p1-completion-evidence.json --markdown=.data/p1-completion-evidence.md --csv=.data/p1-completion-evidence.csv`가 10개 완료 기준별 증거 명령, Android/iOS doctor support artifact, `externalBlockers`, Release Custody 상태, 내부 완료/외부 증빙 대기 판정과 `evidenceOwner`/`evidenceUrl`/`checkedAt`/`signoff` 입력 열을 JSON/Markdown/CSV 매트릭스로 남긴다. P0 회귀와 권한/감사 기준에는 `npm run test:role-csv-export-gates`를 포함해 회원/학부모 CSV 내보내기 비노출을 완료 증거로 보존한다. Android TWA doctor/iOS IPA doctor가 운영 origin, release signing, 실제 iPhone provisioning profile 같은 외부 조건으로 blocked이면 진단 상태는 support artifact에 남기되 완료 기준 집계는 외부 증빙 대기로 분류한다. 운영자가 10개 row를 모두 채운 뒤 `npm run p1:completion-evidence:apply-csv -- --csv=.data/p1-completion-evidence.csv --json=.data/p1-completion-evidence.json --out=.data/p1-completion-evidence.completed.json --markdown=.data/p1-completion-evidence.completed.md`가 completed JSON/Markdown을 생성하고 고정 열 변조, TODO/placeholder, HTTP 증빙 URL, non-ISO `checkedAt`, 원문 secret-like 값을 차단해야 한다.
- 파일럿/앱 배포 최종 전 `npm run p1:readiness -- --workspace=.data --out=.data/p1-readiness.json --markdown=.data/p1-readiness.md`가 운영 배포, Android release, iOS IPA build/provisioning, 결제 provider, 운영 푸시, P1 handoff issue registration receipt, 파일럿 최종 status 리포트를 모두 ready로 판정하고 공유용 Markdown에 release decision, 요구사항별 상태, blockers, next actions를 남긴다.
- 최종 ready 판정 후 `npm run p1:release-package -- --workspace=.data --out=.data/p1-release-package.json`가 P1 readiness, P1 evidence intake report, 7개 필수 readiness 리포트의 SHA-256/byte size manifest를 생성한다.
- 최종 package 생성 후 `npm run p1:release-archive -- --package=.data/p1-release-package.json --archive-dir=.data/p1-release-archives/final-judo-p1-20260715`가 package와 9개 필수 리포트 원본 파일 해시를 재검증하고 fresh archive 폴더에 복사한 뒤 복사본 해시까지 확인한다.
- P1 release archive 업로드 전 `npm run p1:release-storage-receipt:draft -- --archive=.data/p1-release-archives/final-judo-p1-20260715/p1-release-archive-manifest.json --out=.data/p1-release-storage-receipt.json`가 archive manifest 해시와 package+8개 리포트 업로드 항목을 자동 반영하고, 실제 업로드 후 `npm run p1:release-storage-receipt -- --file=.data/p1-release-storage-receipt.json --archive=.data/p1-release-archives/final-judo-p1-20260715/p1-release-archive-manifest.json`가 HTTPS/provider URI 보관 위치와 증빙, ISO uploadedAt, 보존 정책, upload coverage, 로컬 archive copy 해시, 원문 secret-like 값 미기록을 검증한다.
- 총괄 어드민 `/app/admin/settings`의 `P1 최종 배포 준비` 패널에서 운영 배포, Android release, 결제 provider, 운영 푸시, P1 handoff issue registration receipt, 파일럿 최종 status 리포트와 외부 증빙 수집 확인, 증빙 형식 가드, `--allow-pending` 감사 명령, 운영자 상태판 명령, 최종 strict readiness 명령, release package/archive/storage receipt 보관 명령을 확인할 수 있다.
- 같은 패널의 모바일 앱 배포 상태 섹션에서 Android 역할 APK 산출, iOS Simulator 실행 성공, iOS IPA 배포 차단을 분리해 확인하고, `npm run ios:ipa:doctor`가 Apple Team ID와 등록된 iPhone/provisioning profile을 점검해 Simulator 성공과 IPA 배포 가능 상태가 섞이지 않는지 확인한다.
- 같은 패널의 `보류 중인 외부 준비` 섹션에서 운영 웹앱 origin 확정과 iOS 실제 iPhone/provisioning profile이 `사용자 보류`로 표시되는지 확인한다. 이 항목은 내부 스냅샷/문서/테스트 갱신과 별개이며, `/login`과 `/app/dashboard`를 여는 운영 웹앱 origin 및 profile inventory 0 해소 전까지 출시 판단은 보류로 유지되어야 한다.
- 같은 화면의 `P2 개발 착수 상태` 섹션에서 현재 단계가 `P1 external-blocked / P2 development open`으로 표시되고 P1은 출시 완료가 아님을 확인한다. `P2 내부 작업 목록`에는 파일럿 운영 실무 강화, 코치 모바일 업무 속도, 회원/학부모 경험 개선, 결제/회원권 운영 고도화, 대표/운영 리포트 고도화, 공지 후속 조치 개선, 운영/QA 상태판 강화가 있어야 하며 회원/학부모 CSV 내보내기 노출 금지, 실 결제 연동 증빙 전 준비 상태, P1 release blocked 유지 확인 기준이 보여야 한다. `P2 일일 운영 체크리스트`는 추천 운영일, 운영일 로그 입력, 모바일 출석 30초 증빙, P0/P1 우선 이슈, P2 개선 후보, 파일럿 최종 승인 상태, P1/P2 상태 분리 확인을 보여주고 P2 개선 후보를 release blocker로 표시하지 않아야 한다. `P2 진행/QA 상태판`은 P2 완료 8/8, P2 내부 작업 완료, P2 대기 0/8, P2 QA 검증 묶음을 보여주며 P2 진행률을 P1 release readiness와 섞지 않아야 한다.
- 같은 화면의 `P3 운영 고도화 상태` 섹션에서 `P3 다음 작업과 남은 위험`을 확인한다. 대표 운영 보드 실사용 점검, 코치 1탭 현장 레일과 현장 체크리스트 모바일 확인, 회원 핵심 상태 패널과 회원/학부모 내부 운영 안내 비노출 확인, 서비스 품질 신호판 점검, 파일럿 피드백 grooming은 내부 P3 작업으로 표시되고, 운영 웹앱 origin 보류, iOS 실제 iPhone provisioning 보류, Android release handoff 미완, PG/운영 푸시/이슈 receipt/파일럿 최종 status 미완은 P1 release blocked 위험으로 남아야 한다.
- 같은 화면의 `P3 반복 운영 자동화` 섹션에서 운영일 마감 루틴, 지점 운영 리뷰, 서비스 품질 회고, 배포 readiness 재확인이 매일/매주/매월/릴리즈 전 루틴으로 표시되는지 확인한다. 이 루틴은 P3 운영 자동화 후보이며, P1 release blocked 상태를 ready로 바꾸지 않아야 한다.
- 같은 화면의 `P3 서비스 품질 신호판`에서 출석 마감 속도, 공지 미확인, 출석 확인, 결제 위험 후속, 상담/주의 후속이 반복 점검 신호로 표시되고 P1 release readiness와 분리되어야 한다.
- 같은 화면의 `P3 운영 자동화 실행`에서 운영일 마감 자동 점검, 지점 주간 운영 리뷰, 모바일 역할 가드, 배포 readiness 재확인이 담당, 실행 주기, 증거, 확인 기준, 검증 명령으로 표시되고 P3 진행률만 갱신하며 iOS Simulator 성공을 IPA ready로 보지 않는지 확인한다.
- 같은 화면의 `P3 운영 자동화 실패 복구 보드`에서 반복 운영 명령 실패가 실패 신호, 복구 담당, 재시도 창, 복구 증거, 에스컬레이션, 검증 명령으로 표시되고 P3 진행률만 갱신하며 P1 release readiness와 분리되는지 확인한다.
- 같은 화면의 `P3 운영 검증 매트릭스`에서 운영 데이터, 코치 현장, 회원/학부모, 파일럿 피드백, 관리자 상태판이 영역별 검증 상태, 증거 화면, 다음 재확인, 검증 명령으로 묶이고 P1 외부 blocker와 분리되는지 확인한다.
- 같은 화면의 `P3 주간 운영 리뷰 패킷`에서 대표 운영 리뷰, 코치 현장 회고, 회원/학부모 유지 리뷰, 파일럿 피드백 회고, 릴리즈 오판 방지 리뷰가 회의 증거 묶음, 결정 산출물, 후속 확인, 검증 명령으로 묶이고 P1 release blocked 상태와 분리되는지 확인한다.
- 같은 화면의 `P3 주간 리뷰 액션 추적 보드`에서 주간 리뷰 액션이 액션 담당, 마감 창, 업데이트 증거, 검증 명령으로 닫히고 P1 release blocked 분리 상태가 유지되는지 확인한다.
- 같은 화면의 `P3 주간 리뷰 효과 측정 보드`에서 닫은 주간 리뷰 액션이 효과 신호, 측정 창, 업데이트 증거, 다음 판단, 검증 명령으로 재확인되고 P1 release blocked 유지가 분리되는지 확인한다.
- 같은 화면의 `P3 피드백 트리아지 루프`에서 현장 피드백이 접수, 우선순위, QA 반영, 회고 순서로 반복되고 P3 백로그 후보를 만들되 P1 외부 blocker와 섞지 않는지 확인한다. `P3 피드백 접수 보드`는 개선 후보, 버그, 운영 차단, 외부 보류를 출처, 다음 확인, 담당자, 검증 명령으로 고정하고 릴리즈 분리 상태를 표시해야 한다. `P3 피드백 QA 반영 캘린더`는 재현 경로, QA 계획 반영, 백로그 반영, 다음 회고를 검증 명령과 연결하고 P1 release readiness와 분리해야 한다. `P3 피드백 증거 패킷 보드`는 피드백 증거 패킷을 개선 후보, 버그, 운영 차단, 외부 보류로 분류하고 증거 묶음, 재현/영향, QA/백로그 반영, 검증 명령을 함께 표시하며 P1 외부 blocker와 섞지 않아야 한다. `P3 피드백 운영 반영`는 분류된 현장 피드백을 반영 액션, 반영 위치, 다음 회고, 검증 명령으로 묶고 외부 보류는 P1 readiness blocked 유지로 분리해야 한다. `P3 피드백 실행 검증 보드`는 운영 반영 이후 실행 상태, 확인 증거, 재검증 시점, 검증 명령을 묶고 외부 보류는 P1 readiness blocked 증거로만 보관해야 한다. `P3 피드백 재발 방지 보드`는 실행 검증 이후 재발 신호, 방지 확인, 다음 점검, QA 명령을 묶고 외부 보류는 P1 readiness blocked 유지 사유로만 둬야 한다. `P3 피드백 SLA 레인`은 개선 후보, 버그, 운영 차단, 외부 보류를 접수 1영업일, 버그 2영업일, 운영 차단 당일, 외부 보류 별도 보관으로 나누고 담당자와 검증 명령을 표시해야 한다. `/app/dashboard`의 회원 화면은 모바일 핵심 상태를 단일 패널로 유지하고, 다음 행동 큐/오늘 확인 브리프/주간 확인 리듬/복귀 안내 레일/수업 전 준비 보드/확인 리마인드 큐 같은 내부 운영 안내와 회원/학부모 CSV 내보내기 액션을 노출하지 않아야 한다.
- `/app/owner/reports`의 `P3 지점 비교 액션 매트릭스`에서 기준 지점 대비 출석 격차, 위험 금액 비중, 추세 압력, 추천 액션, 다음 운영 리뷰가 표시되고 지점별 운영 비교와 액션 추천 개선이 P1 release readiness와 분리되는지 확인한다. 같은 화면의 `P3 지점 주간 리스크 스코어카드`는 출석 마감 누락, 출석 확인, 결제 위험, 회원 순감 신호를 주간 리스크 점수, 리뷰 트리거, 담당자, 다음 주간 리뷰로 보여주고 P1 release readiness와 분리해야 한다. `P3 지점 유지율 경보 큐`는 회원 순감, 출석률 하락, 휴면 회원, 결제 위험, 출석 확인을 유지율 경보, 감지 신호, 우선 대응, 확인 마감, 운영 증거, 검증 명령으로 묶고 P1 release readiness와 분리해야 한다. `P3 유지율 후속 기록 보드`는 유지율 경보 이후 후속 대상, 담당자, 변경 기록, 재확인 시점, 닫힘 기준을 표시하고 P1 release readiness와 분리해야 한다. `P3 월간 운영 리듬 보드`는 장기 추세 점검, 위험 회원/결제 회수, 재등록 후보 상담, 지점 비교/액션 리뷰를 1~4주차 의사결정, 운영 증거, 확인 기준, 검증 명령으로 묶어 P1 release readiness와 분리해야 한다. `P3 운영 결정 추적 보드`는 장기 추세, 위험 회원, 재등록 후보, 결제 회수 우선순위, 지점 비교 결과를 결정 기록, 운영 신호, 운영 증거, 다음 재확인, 검증 명령으로 묶고 P1 release readiness와 분리해야 한다. `P3 이번 주 운영 결정 큐`는 장기 추세, 결제 회수, 재등록 상담, 지점 액션을 이번 주 결정, 결정 트리거, 결정 마감, 운영 증거로 묶고 P1 release readiness와 분리해야 한다. `P3 다음 주 지점 운영 준비 보드`는 이번 주 결정 큐를 준비 항목, 다음 주 신호, 담당자, 준비 마감, 운영 증거, 검증 명령으로 바꾸고 P1 release readiness와 분리해야 한다. `P3 회원 유지/회수 액션 플래너`는 위험 회원, 재등록 후보, 결제 회수 우선순위를 오늘 연락 대상, 담당자, 연락 채널, 다음 확인으로 바꾸고 CSV 내보내기 권한은 대표/총괄만 유지해야 한다. `P3 재등록/회수 코호트 보드`는 재등록 후보 코호트, 결제 회수 코호트, 위험 회원 코호트, 장기 추세 코호트를 정렬 기준, 담당자, 다음 운영 리뷰, 검증 명령으로 묶고 P1 release readiness와 분리해야 한다. `/app/classes`의 `P3 코치 수업 확인 보드`는 수업 사이 확인, 담당, 확인 마감, 누락 방지, 운영 증거, 검증 명령을 표시하고 코치 화면 금액 미노출과 P1 release readiness 분리를 유지해야 한다. 같은 화면의 `P3 코치 현장 마감 우선순위 보드`는 출석 마감, 사유 메모, 주의/상담, 저장 마감를 바로 실행, 마감 기준, 운영 증거, 검증 명령으로 묶고 코치 화면 금액 미노출과 P1 release readiness 분리를 유지해야 한다. 같은 화면의 `P3 코치 3분 마감 루틴 보드`는 1분 출석, 1분 사유, 30초 보호자 안내, 30초 저장을 마감 순서, 현장 실행, 운영 증거, 검증 명령으로 묶고 결제 금액 비노출 유지와 P1 release readiness 분리를 유지해야 한다.
- `/app/dashboard`의 회원 화면은 운영자용 P3 안내 보드 대신 핵심 상태를 단일 패널로 보여야 하고, 학부모 화면은 자녀 학습 리포트에서 단계/피드백/심사/대회 정보를 우선 보여야 한다. 다음 행동 큐, 오늘 확인 브리프, 주간 확인 리듬, 오늘 복귀 안내 레일, 수업 전 준비 보드, 확인 리마인드 큐, 24시간 팔로업 큐, 우선순위 타임라인, 확인 마감 슬롯, 7일 유지 신호, 재방문 약속 큐, 확인 누락 방지 보드, 오늘 마감 액션 보드, 3분 복귀 체크 보드, 유지 루틴은 회원/학부모 화면에 노출하지 않아야 한다.

## 2. 공통 사전 조건

1. 개발 서버를 실행한다.
   ```bash
   npm run dev
   ```
2. 브라우저에서 `http://localhost:3000`에 접속한다.
3. 필요 시 로그아웃 후 역할 선택 화면에서 테스트 역할을 선택한다.
4. 테스트 전 `localStorage`의 장애 플래그를 초기화한다.
   - `final-judo-force-error`
   - `final-judo-force-offline`
   - `final-judo-force-conflict`
5. 자동 smoke와 reset 가능한 단독 UI 검사는 고유 포트·run-owned 임시 JSON 데이터 디렉터리·실제 `PILOT_DB_FILE`·소유권 마커·helper 발급 256비트 실행별 토큰을 공유한 서버에서만 중앙 reset helper를 호출한다. 단독 실행은 상속 토큰을 새 토큰으로 교체하며 공유/심볼릭 링크 경로, PostgreSQL, 다른 기존 서버에는 reset 요청을 적용하지 않는다.

## 3. 역할별 핵심 시나리오

| ID | 역할 | 경로 | 절차 | 기대 결과 |
| --- | --- | --- | --- | --- |
| QA-ROLE-01 | 회원 | `/app/dashboard` | 회원으로 로그인 | 본인 수업, 출석, 결제, 공지 중심 홈 표시 |
| QA-ROLE-02 | 학부모 | `/app/dashboard` | 학부모 로그인 후 자녀 선택 | 선택 자녀의 수업/출석/회원권/공지 카드 표시 |
| QA-ROLE-03 | 코치 | `/app/classes` | 코치 로그인 후 출석 상태 변경 | 상태 버튼, 저장 상태, 변경 기록 패널 표시 |
| QA-ROLE-04 | 대표 | `/app/dashboard` | 대표 로그인 후 기간/지점 필터 변경 | KPI, 지점 비교, 위험 알림이 스코프별 재계산 |
| QA-ROLE-05 | 총괄 어드민 | `/app/admin/roles` | 총괄 어드민 로그인 후 권한 메뉴 진입 | 사용자/역할 목록, 권한 매트릭스, 내 계정 보호 표시 |
| QA-ROLE-06 | 대표 | `/app/owner/branches` | 대표 로그인 후 내 지점 메뉴 진입 | 배정 지점 운영 상태, 시간대 라벨, 정책, 회원/수업/결제 요약 표시 |
| QA-ROLE-07 | 대표 | `/app/owner/reports` | 대표 로그인 후 리포트 메뉴 진입 | 지점별 운영 그래프, KPI, 오늘 우선순위, 우선 점검 지점, 대표 운영 판단, 운영/결제 내보내기 표시 |

## 4. 권한/스코프 테스트

| ID | 절차 | 기대 결과 |
| --- | --- | --- |
| QA-AUTH-01 | 로그인하지 않고 `/app/dashboard` 접속 | `/login?next=...`로 이동 |
| QA-AUTH-01A | `/login`에서 총괄 어드민 이메일과 교체된 비밀번호 입력 | 총괄 어드민 세션 생성 후 `/app/dashboard`로 이동, 응답에 `passwordHash` 미노출 |
| QA-AUTH-01B | `/login`에서 등록 이메일과 잘못된 임시 비밀번호 입력 | `401 UNAUTHENTICATED` 오류 메시지 표시, 세션 미생성 |
| QA-AUTH-01C | `/login`에서 회원가입 선택 후 `/signup`에서 가입 지점·이름·휴대폰 번호·비밀번호 입력 | 서버가 제공한 활성·운영 가능 지점 중 선택한 지점에 성인 회원 사용자와 회원 프로필이 함께 생성되고 같은 휴대폰 번호/비밀번호로 로그인 가능. `/login?registered=1`은 가입 완료 안내와 가입 휴대폰 번호를 유지하고, 로그인 전에는 잘못된 비밀번호 문구를 띄우지 않는다. `/signup`에는 초대 링크 입력이 보이지 않으며 초대 수락은 `/invite/:token`에서만 진행 |
| QA-AUTH-01D | 알려진 과거 관리자 seed 해시가 있는 기존 JSON 런타임을 재설정하지 않고 기본 운영 비밀번호로 로그인 | `user-admin`의 정확한 과거 seed 해시만 현재 공유 기본 해시로 승격되어 로그인되고, 다른 사용자 비밀번호와 대기 초대는 변경되지 않으며 응답에 `passwordHash`가 노출되지 않음 |
| QA-AUTH-01E | 로그인·회원가입·비밀번호 재설정 API에 객체형 휴대폰/이름/식별자, 배열 payload, 비문자 비밀번호 또는 허용되지 않은 역할을 전송 | 서버 오류나 PBKDF2 검증 없이 `400 VALIDATION_ERROR`로 차단되고 사용자·회원·세션·감사 기록이 생성되지 않음 |
| QA-AUTH-01F | 인증 없이 회원가입에 이름 31자·비밀번호 257자, 로그인에 식별자 255자·비밀번호 257자, 재설정 요청에 식별자 255자 전송 | 모든 요청이 PBKDF2·계정 조회·저장소 잠금 전에 `400 VALIDATION_ERROR`로 차단되고 같은 휴대폰의 정상 가입이 이어진다. 390px 폼은 서버 상한과 일치하는 `maxLength`를 사용 |
| QA-AUTH-02 | 코치로 `/app/payments` 직접 접속 | 403 권한 없음 화면 |
| QA-AUTH-03 | 코치로 `/app/admin/roles` 직접 접속 | 403 권한 없음 화면 |
| QA-AUTH-03A | `npm run test:role-csv-export-gates`, `npm run test:smoke` 실행 | 회원/학부모는 `/app/payments`에서 결제 상태만 볼 수 있고 CSV 내보내기 버튼, `/app/owner/reports`, `/app/admin/audit-logs`, `/app/admin/settings`, `/api/v1/exports/*` 권한은 대표/총괄 또는 관리자 전용으로 유지된다. 현재 계정·지점 범위를 다시 확인하고 동시 결제·운영 CSV 4건의 감사 기록을 모두 보존한다. `test:smoke`의 회원/학부모 CSV export API 403 런타임 요청에서 `/api/v1/exports/payments`, `/api/v1/exports/operations`가 403이고 CSV content-type을 반환하지 않으면 통과 |
| QA-AUTH-04 | `/reset-password`에서 등록 이메일 입력 후 요청 | 사용자 존재 여부 노출 없이 완료 메시지 표시, `auth.password_reset.request` 기록 |
| QA-AUTH-05 | 총괄이 `/app/admin/roles`에서 사용자 초대 | 대기 초대 수 증가, 초대 링크 생성, `user.invite.create` 기록 |
| QA-AUTH-05A | 총괄 | 객체형 이름·휴대폰 또는 비문자 지점 배열로 초대한 뒤 서로 다른 계정 2건과 같은 휴대폰 2건을 각각 동시에 초대 | 잘못된 타입은 `400 VALIDATION_ERROR`이고 사용자·감사 기록 무변경. 서로 다른 초대는 UUID 기반 사용자·감사 ID로 모두 보존되고, 같은 휴대폰 초대는 `200/409`로 한 계정만 저장됨 |
| QA-AUTH-05B | 총괄 | 비밀번호 재발급과 역할 변경에 객체형 사유 또는 비문자 지점 배열 전송 | 두 요청 모두 `400 VALIDATION_ERROR`이고 기존 비밀번호 로그인이 계속 성공하며 역할·담당 지점·감사 기록이 유지됨. 정상 변경은 기존 세션 폐기와 감사 기록을 유지 |
| QA-AUTH-05C | 총괄/대표 | 사용자 초대에 이름 81자·이메일 255자·휴대폰 41자·원본 지점 101개를 전송하거나 역할 변경에 사유 501자·원본 지점 101개를 전송 | 모든 요청이 `400 VALIDATION_ERROR`로 차단되고 사용자·역할·담당 지점·감사 기록이 변하지 않는다. 초대 UI는 이름 80자·이메일 254자·휴대폰 40자, 역할 변경 사유는 500자 상한을 사용 |
| QA-AUTH-05D | 총괄 | 사용자 수정에 객체형 이메일·혼합 연결 배열 또는 이름 81자·설명 121자·비밀번호 257자·연결 ID 101개를 전송하고 삭제·비밀번호 재발급에 501자 사유를 전송 | 모든 요청이 `400 VALIDATION_ERROR`로 차단되고 기존 이메일·연결·비밀번호·감사 기록이 변하지 않는다. 사용자 수정 UI는 서버와 같은 계정 필드 상한, 삭제·재발급 사유는 500자 상한을 사용 |
| QA-AUTH-06 | 생성된 `/invite/:token` 접속 후 12자 이상 초기 비밀번호로 초대 수락 | 초대 사용자가 로그인되고 `auth.invite.accept` 기록, `passwordHash` 미노출, 초기 비밀번호로 재로그인 가능 |
| QA-AUTH-06A | 총괄이 `/app/admin/users`에서 초대 수락 사용자에 발급 사유 입력 후 임시 비밀번호 발급 | 임시 비밀번호가 1회 표시되고 새 비밀번호로 로그인 가능, 기본 임시 비밀번호는 재사용되지 않으며 `auth.password_reset.complete` 기록 |
| QA-AUTH-06B | 초대 수락에서 비밀번호 누락, 기본 임시 비밀번호 사용, 이미 사용한 링크 재사용 시도 | 각각 400/422/409로 차단되고 가입 세션이 새로 생성되지 않음 |
| QA-AUTH-06C | 같은 초대 링크로 두 요청을 동시에 수락 | 전용 저장소 잠금 아래 정확히 한 요청만 200과 로그인 세션을 받고 다른 요청은 409, 성공 감사 기록도 한 건만 생성. 저장소·감사에는 토큰 원문과 비밀번호 원문이 남지 않음 |
| QA-AUTH-06D | 유효한 대기 초대에서 15분 안에 비밀번호 정책 실패 5회 후 다시 수락 | 사용자 target의 최근 실패 감사 기록을 기준으로 다음 요청은 `429 RATE_LIMITED`, `Retry-After` 포함. 미일치 256-bit 토큰은 404이며 대상 감사·세션·PBKDF2 작업 없음. 앞뒤 공백 비밀번호는 자동 trim 없이 400 |
| QA-AUTH-06G | 유효한 대기 초대에 257자 비밀번호로 수락 요청 | `400 VALIDATION_ERROR`, 계정은 `pending` 유지, PBKDF2 미실행. 실패 감사에는 `too_long`만 기록되고 비밀번호 원문은 저장되지 않음 |
| QA-AUTH-06E | 총괄 또는 담당 지점 대표가 대기 초대의 `링크 다시 만들기` 실행 | 새 원문 링크는 해당 응답에서만 표시되고 저장소에는 SHA-256 해시만 남는다. 이전 링크는 즉시 404, 비관리자·담당 밖 지점은 403이며 bootstrap 사용자 목록에는 해시도 노출되지 않음 |
| QA-AUTH-06F | 총괄이 같은 대기 초대를 두 요청으로 동시에 승인 | 초대 보안 잠금 아래 정확히 한 요청만 `200`과 1회 표시 임시 비밀번호를 받고 다른 요청은 `409`. 성공 임시 비밀번호로 로그인 가능하고 UUID 기반 `user.invite.approve` 감사 기록은 한 건만 생성되며 원문 비밀번호는 응답 외 저장소·감사에 남지 않음 |
| QA-AUTH-07 | `/app/account` 접속 | 계정 유형, 이용 지점, 웹/PWA의 홈 화면 추가 액션 표시. 네이티브 앱에서는 설치 안내 카드가 숨겨짐 |
| QA-AUTH-08 | 대표가 `/app/members`에서 코치/학부모/회원 초대 | 자기 지점 범위 초대 링크 생성, `user.invite.create` 기록 |
| QA-AUTH-09 | 대표가 총괄 어드민 초대 API 호출 | `403 FORBIDDEN`으로 차단 |
| QA-AUTH-10 | `/select-role` 접속 후 역할 선택 | 선택한 역할 세션이 생성되고 해당 역할 기본 운영 화면으로 이동 |
| QA-SCOPE-01 | 코치 대시보드 확인 | 강남 본관 데이터만 표시, 송파 도장 데이터 없음 |
| QA-SCOPE-02 | 대표 대시보드에서 전체 지점 선택 | 강남 본관, 송파 도장 비교표 표시 |
| QA-SCOPE-03 | 대표 대시보드에서 송파 도장 선택 | KPI와 비교표가 송파 데이터로 축소 |
| QA-SCOPE-04 | 학부모 회원 화면에서 자녀 전환 | 연결된 이준/이서만 표시 |
| QA-SCOPE-05 | 총괄로 `/app/owner/branches` 직접 접근 | 403 권한 없음 화면 |
| QA-SCOPE-06 | 대표로 `/app/admin/users` 직접 접근 | 403 권한 없음 화면 |
| QA-SCOPE-07 | 학부모 회원 화면의 상담/주의 메모 확인 | `guardian_visible` 메모만 표시, 코치/스태프 전용 메모 미노출 |
| QA-SCOPE-08 | 대표가 `/app/members`에서 학부모를 회원에 연결 | 학부모 재로그인 시 새 자녀가 표시되고 `member.update` 변경 기록 생성 |

## 5. 모바일 출석 테스트

권장 뷰포트: 390 x 844

| ID | 절차 | 기대 결과 |
| --- | --- | --- |
| QA-MOB-01 | 코치로 `/app/classes` 접속 | 출석 요약과 수업 카드가 한 컬럼으로 표시 |
| QA-MOB-02 | 출석/지각/결석/사유 버튼 확인 | 버튼이 터치 가능한 크기로 표시되고 요청 버튼은 표시되지 않음 |
| QA-MOB-03 | 수업 카드의 `전체 출석` 클릭 | 등록 명단 전체가 출석으로 저장되고 회원별 `attendance.update` 변경 기록 생성, 해당 수업 처리율이 100%와 미처리 0명으로 갱신 |
| QA-MOB-03B | 출석 요약의 `미처리만 보기` 토글 활성화 | 처리 완료 회원은 숨겨지고 미처리 회원만 남으며 완료 수업은 `미처리 인원 없음` 빈 상태 표시 |
| QA-MOB-03C | `/app/notices`에서 `미읽음`/`중요` 필터 선택 | 390px에서 필터 버튼이 터치 가능한 크기로 표시되고 `aria-pressed` 상태, 표시 건수, 권한 스코프의 공지 목록이 유지 |
| QA-MOB-03D | `/app/notices`에서 보이는 공지 읽음 처리 선택 | 390px에서 버튼이 터치 가능한 크기로 표시되고 화면에 보이는 미읽음 공지만 읽음 처리되며 미읽음 필터 빈 상태와 전역 배지 해제가 확인 |
| QA-MOB-03A | 현장 메모 입력 후 출석 상태 변경 | 하단 sticky 저장 바가 `저장됨` 상태 표시, 변경 기록에 수정 사유 기록 |
| QA-MOB-03E | 기존 출석 상태가 있는 회원의 출석 버튼을 잘못 선택한 뒤 `되돌리기` 선택 | 하단 sticky 되돌리기 버튼이 터치 가능한 크기로 표시되고 이전 출석 상태로 서버 원복, `attendance.update` 변경 기록 생성, 되돌리기 패널 해제 |
| QA-MOB-03F | 출석 요약의 `명단 검색`에 회원 이름 입력 후 검색어 지우기 | 검색어와 일치하는 회원만 남고 표시 건수와 검색 빈 상태가 갱신되며, 검색어 지우기 후 원래 명단 복구 |
| QA-MOB-03G | 출석 요약의 상태 빠른 필터에서 `지각` 선택 후 `전체` 복귀 | 390px에서 상태 칩이 터치 가능한 크기로 표시되고 `aria-pressed`/상태별 건수/상태별 빈 상태가 갱신되며, 지각 회원만 표시된 뒤 전체 명단이 복구 |
| QA-MOB-03H | 출석 명단에서 현장 메모 입력 후 `늦게 도착` 빠른 메모 선택 | 390px에서 빠른 메모 칩이 터치 가능한 크기로 표시되고 기존 메모에 중복 없이 추가되며, 출석 저장 후 메모와 `attendance.update` 변경 기록이 유지 |
| QA-MOB-03I | 코치 모바일 빠른 조치 패널 확인 | 390px에서 사유 빠른 입력, 수업 상태, 상담/주의 회원, 결제 금액 비노출 유지가 보이고 미처리/사유/주의 회원 빠른 필터가 터치 가능한 크기로 작동 |
| QA-MOB-03J | 회원/학부모로 `/app/dashboard` 접속 후 390px 화면 확인 | 회원은 수업/출석, 결제 상태, 상담/공지 핵심 카드가 보조 제목 없이 표시되고 학부모는 자녀 학습 리포트가 먼저 표시되며 요청 작성 카드와 회원/학부모 CSV 내보내기 액션은 없다 |
| QA-MOB-03K | 코치로 `/app/classes` 접속 후 `P3 코치 수업 전/후 체크리스트` 확인 | 수업 전 확인, 수업 후 마감, 상담/주의 후속, 결제 금액 비노출 유지가 표시되고 현장 체크가 코치 역할에만 보임 |
| QA-MOB-03K1 | 코치로 `/app/classes` 접속 후 `P3 코치 수업 전 준비 큐` 확인 | 출석 명단 선확인, 주의/상담 선확인, 사유 메모 준비, 저장 상태 확인이 수업 전 준비, 준비 신호, 현장 실행, 준비 마감, 운영 증거, 검증 명령으로 표시되고 결제 금액 비노출 및 P1 release readiness 분리가 유지됨 |
| QA-MOB-03L | 회원/학부모로 `/app/dashboard` 접속 후 내부 운영 안내 비노출 확인 | 회원 핵심 상태 패널과 학부모 학습 리포트만 보이고 다음 행동 큐, 오늘 확인 브리프, 주간 확인 리듬 같은 운영자용 안내와 CSV 내보내기 액션은 없음 |
| QA-MOB-03L1 | 회원으로 `/app/dashboard` 접속 후 모바일 상태 카드 확인 | 본인 기준 다음 수업, 출석, 결제 상태, 상담/공지 카드가 표시되고 내부 운영 안내는 없음 |
| QA-MOB-03L2 | 학부모로 `/app/dashboard` 접속 후 모바일 상태 카드 확인 | 선택 자녀 기준 다음 수업, 출석, 결제 상태, 상담/공지 카드가 표시되고 내부 운영 안내는 없음 |
| QA-MOB-03M | 코치로 `/app/classes` 접속 후 `P3 코치 현장 후속 조치 큐` 확인 | 사유 메모, 주의 확인, 상담 후속이 수업 전/후 1분 점검으로 표시되고 코치 화면 결제 금액 비노출 유지가 함께 보임 |
| QA-MOB-03N | 회원/학부모로 `/app/dashboard` 접속 후 유지 루틴 비노출 확인 | 운영자용 유지 루틴 보드가 표시되지 않고 회원 핵심 상태 패널과 학부모 학습 리포트만 남아 있음 |
| QA-MOB-03O | 코치로 `/app/classes` 접속 후 `P3 코치 1탭 현장 레일` 확인 | 모바일 출석, 상담, 주의 회원 처리가 미처리 명단 좁히기, 사유 대상 바로 보기, 주의 회원 찾기, 저장 마감 순서로 표시되고 코치 화면 결제 금액 비노출 유지가 함께 보임 |
| QA-MOB-03P | 회원/학부모로 `/app/dashboard` 접속 후 우선순위 타임라인 비노출 확인 | 우선순위 타임라인 대신 다음 수업, 출석, 결제 상태, 상담/공지 상태 카드만 표시됨 |
| QA-MOB-03Q | 코치로 `/app/classes` 접속 후 `P3 코치 보호자 안내 마감 큐` 확인 | 보호자 안내 마감, 저장 마감, 운영 증거 확인이 수업 후 운영 증거와 검증 명령으로 표시되고 결제 금액 비노출 유지와 P1 release readiness 분리가 함께 보임 |
| QA-MOB-03Q1 | 코치로 `/app/classes` 접속 후 `P3 코치 수업 후 24시간 후속 큐` 확인 | 사유 메모, 보호자 안내, 주의/상담, 저장 기록가 후속 트리거, 후속 행동, 완료 증거, 재확인 시점으로 표시되고 코치 화면 금액 미노출과 P1 release readiness 분리가 함께 보임 |
| QA-MOB-03Q2 | 코치로 `/app/classes` 접속 후 `P3 코치 현장 기록 압축 보드` 확인 | 사유/상담, 보호자 안내, 저장 기록가 기록 압축, 현장 기록, 기록 마감, 다음 재확인으로 표시되고 코치 화면 금액 미노출과 P1 release readiness 분리가 함께 보임 |
| QA-MOB-03R | 회원/학부모로 `/app/dashboard` 접속 후 재방문 약속 큐 비노출 확인 | 재방문 약속 큐와 검증 명령 문구가 표시되지 않고 회원/학부모가 확인할 실제 상태 카드만 표시됨 |
| QA-MOB-03S | 코치로 `/app/classes` 접속 후 `P3 코치 현장 마감 우선순위 보드` 확인 | 출석 마감 우선순위, 사유 메모 우선순위, 주의/상담 우선순위, 저장 마감 우선순위가 바로 실행, 마감 기준, 운영 증거, 검증 명령으로 표시되고 코치 화면 금액 미노출과 P1 release readiness 분리가 함께 보임 |
| QA-MOB-03T | 회원/학부모로 `/app/dashboard` 접속 후 확인 누락 방지 보드 비노출 확인 | 확인 누락 방지 보드와 검증 명령 문구가 표시되지 않고 회원/학부모 CSV 내보내기 액션도 없음 |
| QA-MOB-03T1 | 회원/학부모로 `/app/dashboard` 접속 후 오늘 마감 액션 보드 비노출 확인 | 오늘 마감 액션 보드가 표시되지 않고 실제 상태 카드만 표시됨 |
| QA-MOB-03U | 코치로 `/app/classes` 접속 후 `P3 코치 3분 마감 루틴 보드` 확인 | 1분 출석, 1분 사유, 30초 보호자 안내, 30초 저장가 마감 순서, 현장 실행, 운영 증거, 검증 명령으로 표시되고 결제 금액 비노출 유지와 P1 release readiness 분리가 함께 보임 |
| QA-MOB-03V | 회원/학부모로 `/app/dashboard` 접속 후 복귀 안내 레일 비노출 확인 | 오늘 복귀 안내 레일과 다음 행동/확인 증거/검증 명령 문구가 표시되지 않음 |
| QA-MOB-03W | 코치로 `/app/classes` 접속 후 `P3 코치 수업 확인 보드` 확인 | 출석 마감, 사유 메모, 보호자 안내, 저장 상태이 수업 사이 확인, 담당, 확인 마감, 운영 증거, 검증 명령으로 표시되고 누락 방지, 결제 금액 비노출 유지, P1 release readiness 분리가 함께 보임 |
| QA-MOB-03X | 회원/학부모로 `/app/dashboard` 접속 후 수업 전 준비 보드 비노출 확인 | 수업 전 준비 보드가 표시되지 않고 다음 수업 상태는 회원 핵심 카드로만 표시됨 |
| QA-MOB-03X1 | 회원/학부모로 `/app/dashboard` 접속 후 확인 리마인드 큐 비노출 확인 | 확인 리마인드 큐와 리마인드 트리거/안내 채널/확인 마감 문구가 표시되지 않음 |
| QA-MOB-03X2 | 회원/학부모로 `/app/dashboard` 접속 후 24시간 팔로업 큐 비노출 확인 | 24시간 팔로업 큐가 표시되지 않고 회원/학부모 CSV 내보내기 액션도 없음 |
| QA-MOB-04 | 출석 사유 기록 API 호출 | 기존 출석 상태는 유지되고 출석 메모와 변경 기록이 갱신 |
| QA-MOB-05 | `final-judo-force-offline=1` 설정 후 출석 변경 후 새로고침 | 저장 대기 메시지와 대기 건수 복구, 서버 변경 기록는 아직 생성되지 않음 |
| QA-MOB-05A | `final-judo-force-offline` 제거 후 대기 출석 저장 재시도 클릭 | 대기 출석이 서버에 저장되고 `attendance.update` 변경 기록 생성, 대기 건수 0건 |
| QA-MOB-06 | `final-judo-force-conflict=1` 설정 후 출석 변경 | 충돌 메시지 표시, 기존 출석 데이터 유지 |
| QA-MOB-07 | `npm run test:attendance-speed` 실행 | 담당 수업 전체 명단 출석 저장이 30초 이내 완료되고 `attendance.update` 변경 기록 생성 |
| QA-MOB-08 | `npm run test:e2e` 실행 | 390px Chrome E2E에서 코치 운영 계정 로그인, 모바일 계정 요약/계정 전환/로그아웃 액션, 코치 모바일 빠른 조치 패널/빠른 필터/결제 금액 비노출, 상단 공지 버튼 미읽음 배지, 공지 미읽음/중요 필터 터치 영역과 pressed 상태, 확인할 공지, 보이는 공지 읽음 처리 후 배지 해제, 하단 네비게이션 safe area/fixed 위치/48px 이상 활성 메뉴 터치 영역/`aria-current`, 수업별 처리율/미처리 인원, 명단 검색/검색 빈 상태/터치 영역, 출석 상태 빠른 필터/상태별 빈 상태/pressed 상태/터치 영역, 빠른 메모 칩/메모 조합/터치 영역, 미처리만 보기 필터/빈 상태/터치 영역, 전체 출석 일괄 저장 후 100% 처리율 갱신, 출석 변경, 최근 변경 되돌리기, 새로고침 후 오프라인 대기 큐 복구/재동기화, sticky 저장 상태, 변경 기록, 결제 금액 마스킹 통과 |
| QA-MOB-09 | `npm run test:mobile-install` 실행 | iOS/Android 홈 화면 설치용 manifest, `/app` start URL, standalone display, 192/512 아이콘, iOS apple web app, 모바일 viewport/theme color, production 서비스 워커 등록, `/app/account` 홈 화면 추가 액션, `beforeinstallprompt` 설치 프롬프트, standalone/iOS 설치 상태 감지, 오프라인 앱 셸 캐시, API 응답 캐시 금지 정책이 검증되면 통과. Android 내부 설치용 `android-capacitor-webview-report.json`이 있으면 주소창 없는 `INSTALL_ONLY_final-judo-native-webview-debug.apk`의 SHA-256/byte size, 동등본 `final-judo-native-webview-debug.apk`, APK 내부 `res/mipmap-xxxhdpi-v4/ic_launcher.png`와 `icon-proof/apk-ic_launcher-xxxhdpi.png`/Android 런처 리소스 SHA-256 일치, `INSTALL_ANDROID_WEBVIEW_APK.txt` 설치 가이드, TWA/Bubblewrap `app-debug.apk` 설치 금지 경로, `DO_NOT_INSTALL_FOR_FIELD_WEBVIEW.txt` 마커와 브라우저/TWA 오설치 경고도 함께 검증 |
| QA-MOB-10 | `npm run test:android-packaging` 실행 | Android TWA/Bubblewrap 전략, `mobile/android` 템플릿, Digital Asset Links, Next PWA manifest 호환성, Android 런처/splash의 `public/icons/final-judo-icon-512.png` 원본 생성, `android:twa:prepare`의 `buildReady/buildBlockers`, `android:twa:doctor -- --markdown` 공유 리포트와 설치 힌트, Java/keytool/sdkmanager/adb/npx/Android SDK home 가드, GitHub Actions 수동 패키징 workflow, release/admin gate 포함 여부가 검증되면 통과 |
| QA-MOB-10K | `npm run test:android-play-release-artifacts` 실행 | 최신 `.data/mobile-builds/android-play-release-*/google-play-release-report.json`의 versionCode/versionName이 Gradle과 같고, Android Capacitor `server.url`이 `https://final-judo.vercel.app/login`이며, timestamped AAB/APK와 Desktop `final-judo-play-release.aab`/`final-judo-release.apk`의 byte size/SHA-256, jarsigner/apksigner 검증, APK badging, AAB TWA 런타임 미포함이 모두 맞으면 통과 |
| QA-MOB-10A | `npm run android:twa:doctor` 실행 | 운영 웹앱 origin/릴리즈 지문/JDK/Android SDK/keytool/adb/npx/ANDROID_HOME 또는 repo-local `.data/toolchains` 준비 상태가 JSON으로 출력되고, `--markdown=.data/android-twa-doctor.md`를 지정하면 공유용 Markdown 점검표와 macOS/환경변수/CI 설치 힌트가 생성되며, 실제 APK/AAB 직전에는 `/login`과 `/app/dashboard`를 서빙하는 `--strict --origin=<https-origin> --sha256=<fingerprint>`가 blockers 없이 통과하면 완료. API-only `api.*` origin은 기본 차단 |
| QA-MOB-10F | `npm run test:ios-capacitor-connection` 실행 | iOS Main.storyboard가 `CAPBridgeViewController`를 사용하고 generated Capacitor config가 실제 서비스 `/app/dashboard` 화면을 열며, localhost Simulator 연결이 IPA 배포 ready로 처리되지 않으면 통과 |
| QA-MOB-10G | `npm run test:ios-ipa-doctor` 실행 | iOS IPA doctor가 운영 웹앱 origin 누락 또는 API-only `api.*` origin 입력 시 blocked를 유지하고 Apple Developer 실제 iPhone UDID 등록, Xcode 수동 provisioning profile 다운로드, `mobile/ios/release-config.json` 기본 Team ID/bundle id fallback, `APPLE_TEAM_ID`/`FINAL_JUDO_IOS_SERVER_URL`, strict 재실행, IPA build 힌트를 JSON/Markdown에 남기면 통과 |
| QA-MOB-10H | `npm run test:ios-provisioning-runbook` 실행 | [docs/IOS_IPA_PROVISIONING_RUNBOOK.md](docs/IOS_IPA_PROVISIONING_RUNBOOK.md)가 Apple 계정 `roehf45@naver.com`, Team ID `5GWZ792DWH`, bundle id `kr.co.finaljudo.multigym`, `FINAL_JUDO_IOS_SERVER_URL`, 실제 iPhone UDID 등록, matching provisioning profile, Xcode Download Manual Profiles, strict doctor/build 명령, UDID 원문 미기록 규칙을 유지하면 통과 |
| QA-MOB-10I | `npm run test:p4-simulator-rehearsal` 실행 | P4 실제 사용 환경 검증으로 앱 UI에는 P4 내부 상태판을 노출하지 않고, `.data/mobile-builds/ios/p4-simulator-screenshots`의 관리자 대시보드/관리자 설정 운영 확인 화면/코치/회원/학부모 캡처 5개가 존재하고, `.data/p1-readiness.json`은 blocked 7/7, `.data/mobile-builds/ios/ios-ipa-doctor.json`은 origin/provisioningProfile blocked를 유지하며 iOS Simulator 성공을 IPA ready로 보지 않음 |
| QA-MOB-10J | `npm run p5-p10:internal-audit`와 `npm run test:p5-p10-internal-readiness` 실행 | P5~P10 내부 readiness audit로 공통 header의 iOS safe area padding이 유지되고, 하단 모바일 내비게이션 active 메뉴와 코치 저장 상태 패널이 iOS safe area를 침범하지 않으며, 코치 저장 상태 패널 아래 콘텐츠가 가려지지 않도록 모바일 스크롤 여백을 확보하고, Next 개발 표시 배지가 하단 내비게이션 캡처를 가리지 않는다. 역할별 `autoLogin` deep link는 이미 같은 역할로 로그인된 세션에서도 로그인 화면이 아닌 실제 서비스 화면으로 이동한다. 로그인 `quickLogin=1` 개발 역할 버튼은 mount 이후에만 켜져 SSR hydration mismatch로 iOS WebView 빈 화면을 만들지 않는다. P5~P10 내부 release/audit 카드와 외부 blocker 문구는 실제 앱 UI에 노출하지 않고, 비밀번호 재설정/회원 초대/총괄 사용자 초대 화면은 `name@example.com` 또는 `email@example.com` 같은 예시 이메일 placeholder를 노출하지 않으며, 회원 연락처 입력은 `010-0000-0000` 같은 더미 번호 placeholder를 노출하지 않는다. 로그인 클라이언트 화면 코드는 `.test` seed 이메일, 공통 비밀번호, `계정 입력 도구/계정 채우기` UI를 싣지 않는다. 초대/공지 작성 화면의 지점 안내는 `지점 권한`, `역할과 지점 범위` 같은 내부 기준 대신 담당/선택 지점 표현을 사용한다. 대표/총괄 지점 화면은 `Asia/Seoul` 같은 기술 시간대 값과 `branch-gangnam` 같은 내부 지점 ID를 직접 보여주지 않고 앱용 라벨과 운영 정보만 표시한다. 총괄 대시보드는 내부 변경 기록 요약 대신 대기 초대와 사용자 관리 액션을 우선 표시하고, 권한 화면은 `권한/감사`, `감사 이벤트`, `최근 감사` 같은 요약 라벨 대신 기본 역할과 최근 계정 변경처럼 운영자가 읽는 라벨을 사용한다. 운영 KPI는 보조기기가 라벨/값/설명을 문장 단위로 읽는 요약 라벨을 제공한다. 관리자 변경 기록 화면은 stale runtime 기록이 있더라도 `변경 기록을 조회했습니다.` 대신 `변경 기록을 조회했습니다.`를 렌더링하고 `.data/mobile-builds/ios/visible-text-audit-20260621-recheck`에 recheck 캡처를 남긴다. 학부모 학습 리포트는 단계/띠별 수련 수준, 코치 피드백, 심사결과, 대회 정보를 유지하되 긴 공지 본문과 피드백을 모바일 카드 안에서 축약하고 액션 링크 접근성 라벨을 제공한다. `.data/mobile-builds/ios/admin-dashboard-copy-cleanup` 증빙을 남긴다. 대표 운영 그래프의 우선 신호는 숫자와 설명이 붙어 읽히지 않도록 분리된 텍스트 구조를 사용하고 iPhone 16e Simulator 증빙을 `.data/mobile-builds/ios/owner-graph-spacing/owner-dashboard-graph-spacing-ios-sim.jpg`에 남긴다. 대표 대시보드 지점 비교/위험 알림은 기본 화면에서 우선 지점 1개와 위험 요약만 보이고 상세 지점/결제 위험 회원은 토글 뒤에 둔다. 코치 빠른 조치와 대표 운영 판단/지점 비교/기간별 운영 추세는 반복 소개문 없이 바로 핵심 조치와 지표를 보여준다. 로그인 기본 화면은 `파이널 로그인`으로 표시하고 `운영 계정 로그인`, `세션을 시작합니다` 같은 내부/기술 문구를 노출하지 않는다. 41개 역할/경로 확장 visible copy 캡처는 `.data/mobile-builds/ios/visible-copy-expanded`에 보관하고 모두 비어 있지 않아야 한다. `.data/p5-p10-internal-audit.json`과 `.data/p5-p10-internal-audit.md`에만 보관한다. `.data/mobile-builds/ios/p5-p10-simulator-screenshots`의 관리자 설정/코치/회원/학부모 캡처 4개가 존재하고, `.data/p1-readiness.json`은 P1 외부 blocker blocked 7/7을 유지하고 출시 완료, 운영 ready, iOS Simulator 성공을 IPA ready로 보지 않음 |
| QA-MOB-10B | GitHub Actions `Android TWA Package` 수동 실행 | `production_origin`, `release_sha256` 입력으로 strict doctor와 TWA 입력 파일 생성 artifact가 업로드되고, `build_artifacts=true`에서는 APK/AAB 빌드 산출물 업로드까지 시도되면 통과 |
| QA-MOB-10C | `npm run test:android-release-handoff-draft` 실행 | Android handoff 초안 생성기가 doctor/assetlinks/build-plan/APK/AAB에서 origin, release fingerprint, SHA-256, byte size를 자동 입력하고, 증빙 값이 채워진 초안이 strict handoff를 통과하면 완료 |
| QA-MOB-10D | `npm run test:android-release-handoff` 실행 | Android handoff manifest 검증기가 APK/AAB 해시, assetlinks 배포, signing custody, 설치 smoke, 주소창/공유/더보기 브라우저 UI 비노출, HTTPS/provider URI 증빙, 템플릿의 `*_EVIDENCE_URI` placeholder, ISO 생성/승인 시각, 생성 이후 승인 순서, signoff ready fixture를 통과시키고 누락 AAB/해시 불일치/템플릿 대기/브라우저 주소창 smoke 실패/non-reference evidence/non-ISO timestamp/시간 역전 상태를 차단하면 통과 |
| QA-MOB-10E | 실제 `.data/android-release-handoff.json` 작성 후 `npm run android:release-handoff -- --file=.data/android-release-handoff.json --out=.data/android-release-handoff.report.json` 실행 | 실제 APK/AAB, doctor report, assetlinks/build-plan, Play/App signing, keystore custody, Android 설치 smoke, 주소창/공유/더보기 브라우저 UI 비노출 승인 증빙, HTTPS/provider URI 증빙, ISO 생성/승인 시각과 생성 이후 승인 순서가 모두 ready이고 blockers가 없으면 파일럿 배포 전 통과 |
| QA-MOB-11 | `npm run test:notification-readiness`, `npm run test:notification-outbox`, `npm run test:smoke` 실행 | 공지 화면 알림 권한 UI, VAPID 구성 필요/구독 가능/구독됨 상태, PushSubscription 저장/해지 API, 역할별 활성 구독 수 스코프, 동일 브라우저 endpoint의 단일 계정 소유권 이전 감사, 이전 계정 provider 호출 중 이전 보류, 공지별 push dispatch API, 중요 공지 발행/배지/푸시 제목, 공지함 미읽음/중요 필터와 빈 상태, 보이는 공지 읽음 처리, 확인할 공지, 공지 요약, AppShell 미읽음 공지 배지, 테스트 알림 표시, push/click handler, 공지함 이동 경로, `브라우저`, `브라우저 권한`, `서비스 워커` 같은 구현 기준 문구 비노출이 검증되면 통과 |
| QA-MOB-11C | `npm run test:mobile-notification-inbox` 실행 | 회원·학부모 390px 알림함에서 알림 권한 요청, PushManager 구독, 구독 API 1회 저장, 연결 후 안내 행 제거, 44px 터치 영역, 가로 오버플로 0, 공지 읽음 톤다운이 함께 검증되면 통과 |
| QA-MOB-11A | `npm run test:notification-push-handoff-draft`와 `npm run test:notification-push-handoff` 실행 | 운영 푸시 handoff 초안이 원문 VAPID private key를 저장하지 않고, strict 검증기가 HTTPS origin, VAPID secret store, Android 실기기 push 수신/클릭, 공지 발송 변경 기록, 권한 차단/미지원 상태 HTTPS/provider URI 증빙 누락, 템플릿의 `*_EVIDENCE_URI` placeholder, `localhost`/`.example`/TODO production origin과 VAPID subject, non-reference evidence, non-ISO timestamp, 시간 역전을 차단하면 통과 |
| QA-MOB-11B | `npm run test:notification-outbox`와 `npm run test:notification-outbox-integration` 실행 | 수동 재발송 `Idempotency-Key` 원문 미저장·동일 요청 재사용, revision fencing, 공지 변경/삭제 시 대기·임대 작업 취소, provider 호출 전 발송 중단, 호출 후 취소·15초 timeout의 전송 가능성 감사, 늦은 이전 결과의 상태 덮어쓰기 차단, CRON_SECRET timing-safe 검증이 통과 |

## 6. 결제/공지/삭제된 요청 테스트

| ID | 역할 | 절차 | 기대 결과 |
| --- | --- | --- | --- |
| QA-REQ-00 | 전체 | `npm run test:deleted-request-surface` 실행 | `/app/requests`, `/requests`, 요청 생성/승인/반려 API, 하단 보강 메뉴, 요청 카드/작성 폼/알림 링크가 앱 소스에 재유입되지 않고 README/QA/릴리즈 문서와 release runner가 삭제 기준을 함께 검증 |
| QA-PAY-01 | 코치 | 대시보드와 수업 화면 확인 | 회원권 상태는 보이되 원화 금액 미노출 |
| QA-PAY-02 | 대표 | `/app/payments` 접속 | 금액, 미납, 만료 예정 표시 |
| QA-PAY-03 | 대표 | `/app/payments`에서 회원 검색 후 일반 상태와 취소·환불 완료 상태로 수기 결제 등록 | 일반 상태는 결제 목록에 UUID 기반 새 회원권이 추가되고 동일 `Idempotency-Key` 재시도는 한 건만 유지. 취소·환불 완료 상태는 사유 입력 전 제출할 수 없고, 정규화한 사유·처리 시각이 `refundReason`/`refundedAt`, 최초 상태 이력과 `payment.create` 변경 기록에 저장되며 Payment 루트에는 감사용 `reason`이 중복되지 않음 |
| QA-PAY-03A | 대표 | 수기 결제 등록 API의 회원·회원권·상태·날짜·사유·회비 상품·혜택 필드에 객체나 배열을, 금액 필드에 문자열을 전송 | 문자열 정규화나 금액 계산 전에 `400 VALIDATION_ERROR`로 차단되고 결제·상태 이력·감사 기록이 생성되지 않으며 서버 오류가 발생하지 않음 |
| QA-PAY-03B | 대표/총괄 | 수기 결제 등록·수정의 회원권명에 101자, 등록·수정·삭제·환불/취소 사유에 501자를 전송 | 모든 요청이 `400 VALIDATION_ERROR`로 차단되고 결제 원본·상태 이력·감사 기록이 생성·수정·삭제되지 않으며 UI도 같은 100자·500자 상한을 사용 |
| QA-PAY-04 | 대표 | `/app/payments`에서 결제 내보내기 | CSV가 내려오고 `export.create` 변경 기록 생성 |
| QA-PAY-05 | 대표 | `/app/payments`에서 환불 금액과 사유 입력 후 처리 | 상태가 부분/전액 환불로 변경되고 `payment.refund` 변경 기록 생성 |
| QA-PAY-05A | 대표/총괄 | 동일 결제에 1,000원 환불 12건을 동시에 요청하고 `0.4원`·안전 정수 범위 밖 금액을 제출 | 12건은 공통 결제 잠금 안에서 순차 반영되어 `500` 없이 환불액·상태 이력 12건이 일치하고, 소수·비안전 정수 금액은 `400 VALIDATION_ERROR`로 차단 |
| QA-PAY-05B | 대표/총괄 | 환불·취소 API의 사유에 객체, 금액에 문자열 또는 취소 여부에 문자열을 전송 | 문자열 정규화와 상태 계산 전에 `400 VALIDATION_ERROR`로 차단되고 환불액·결제 상태·상태 이력·감사 기록이 변경되지 않으며 서버 오류가 발생하지 않음 |
| QA-PAY-06 | 대표 | `/app/payments`에서 예정/미납 결제를 사유와 함께 취소 | 상태가 취소로 변경되고 `payment.refund` 변경 기록 생성 |
| QA-PAY-07 | 대표 | `/app/payments`에서 상태 필터를 `미납/만료 예정`으로 변경 | 위험 결제만 표시되고 조회 건수/미납/확인 필요 금액이 필터 기준으로 재계산 |
| QA-PAY-08 | 대표 | `/app/payments` 결제 행의 `재등록` 클릭 | 같은 회원/회원권/금액/할인이 수기 결제 등록 폼에 채워지고 상태는 납부 예정으로 설정 |
| QA-PAY-09 | 대표 | `/app/payments` 결제 행 확인 | 회원별 결제 이력 건수와 최근 만료일이 표시 |
| QA-PAY-10 | 대표 | `/app/payments`에서 예정/미납 결제의 `온라인 요청` 클릭 | 온라인 결제 대기 상태, provider 결제 ID, 결제 링크가 저장되고 `payment.online_checkout.create` 변경 기록 생성 |
| QA-PAY-10A | 대표/총괄 | production 결제창 기본 주소를 HTTP, 자격증명 포함 URL 또는 경로·쿼리가 붙은 값으로 설정한 뒤 온라인 결제 요청 | `PAYMENT_CHECKOUT_BASE_URL_INVALID` 준비 차단 사유가 확인되고 API는 `503 PAYMENT_RUNTIME_NOT_READY`를 반환하며 결제 요청·상태 이력·감사 로그를 저장하지 않음 |
| QA-PAY-11 | 대표 | `/api/v1/payments/webhook`에 성공/실패/환불 이벤트 전송 후 같은 결제와 다른 결제에 동일 `providerEventId`를 순차·동시에 재전송한다. 금액 없는 환불, 비문자 식별자, 실행 가능 스킴의 영수증 URL도 전송 | 같은 결제의 중복 event ID는 상태 이력과 변경 기록을 추가하지 않고 멱등 응답하며, 다른 두 결제의 동시 재사용도 정확히 한 건만 성공하고 나머지는 상태·이력·감사 기록을 변경하지 않은 채 `409 PROVIDER_EVENT_CONFLICT`로 차단한다. 금액 없는 환불과 잘못된 provider/영수증 메타데이터는 결제 상태·환불액·이벤트 ID·감사 기록을 변경하지 않고 `400`으로 차단 |
| QA-PAY-11A | 결제 provider | `paid → refunded → 다른 ID의 paid`와 최신 처리 시각보다 오래된 이벤트를 전송 | 환불 완료 상태는 `409 INVALID_TRANSITION`, 오래된 이벤트는 `409 OUT_OF_ORDER`로 차단되고 결제·온라인 결제 상태와 환불액이 환불 완료로 유지 |
| QA-PAY-11B | 대표/결제 provider | 같은 결제에 `paid` 웹훅과 정기결제 약정 생성을 동시에 전송 | 전역 provider event 잠금 뒤 결제별 잠금을 같은 순서로 얻어 두 요청이 모두 완료되고, 최종 결제는 납부 완료 상태와 정기결제 약정을 함께 보존하며 웹훅·약정 상태 이력과 감사 기록이 각각 한 건만 생성 |
| QA-PAY-12 | 코치 | 온라인 요청이 있는 결제 포함 수업/회원 화면 확인 | 회원권 상태는 보이되 `onlinePayment.amount`와 결제 링크는 노출되지 않음 |
| QA-PAY-13 | 대표 | `/app/payments`에서 결제 완료/납부 예정/미납/만료 예정 결제의 `정기결제 약정` 클릭 | provider 약정 ID, 월 청구일, 다음 청구일, `payment.recurring_agreement.create` 변경 기록이 저장됨 |
| QA-PAY-14 | 대표 | 정기결제 약정이 있는 결제에서 사유 입력 후 `정기결제 해지` 클릭 | 약정 상태가 해지로 바뀌고 해지 사유, 해지 시각, `payment.recurring_agreement.cancel` 변경 기록이 저장됨 |
| QA-PAY-14A | 대표 | 같은 결제의 정기결제 약정 생성 또는 해지를 동시에 2건 제출하고 잘못된 달력 날짜·청구일·객체형 해지 사유도 제출 | 생성은 `200/409`, 해지는 `200/422`로 한 건만 반영되고 생성·해지 상태 이력과 감사 기록은 각각 1건. 잘못된 입력은 `400`이며 약정·이력·감사 기록 무변경 |
| QA-PAY-15 | 대표 | 수기 결제의 `수정`에서 회원권명·금액·할인·상태·납부일·만료일과 사유를 변경하고, API로 존재하지 않는 날짜를 전송 | 정상 정정은 목록에 반영되고 실제 상태 변경 시에만 상태 이력이 추가되며 전후 값과 사유가 `payment.update` 변경 기록에 저장됨. `2026-02-29`, `2026-04-31`처럼 존재하지 않는 날짜는 `400 VALIDATION_ERROR`로 차단되고 실제 윤년 날짜는 허용 |
| QA-PAY-15A | 학부모/대표 | 같은 자녀 결제에 학부모 납부 요청과 대표의 수기 결제 정정을 동시에 전송하고, 납부 요청 본문에 `null`·상한 초과 문자열을 전송한다. 느린 미완성 납부 본문을 열어 둔 동안 대표 정정도 전송 | 잘못된 본문은 `400`이며 결제·감사 기록 무변경. 느린 본문이 끝나기 전에 대표 정정이 먼저 완료되고, 동시 정상 요청은 둘 다 `200`이며 최종 결제에 납부 요청과 정정값이 함께 남고 서로 다른 감사 기록 2건이 보존됨 |
| QA-PAY-15B | 학부모 | 같은 지점의 연결되지 않은 회원 결제 ID와 존재하지 않는 결제 ID로 납부 요청 | 두 요청 모두 동일한 `404 NOT_FOUND` 오류를 반환해 다른 가족의 결제 존재 여부를 노출하지 않고 결제·감사 기록을 변경하지 않음 |
| QA-PAY-16 | 대표 | 오등록 또는 환불 금액이 없는 취소 수기 결제의 `삭제`에서 삭제 사유 입력 후 확정 | 결제 목록에서 제거되고 삭제 전 값과 사유가 `payment.delete` 변경 기록에 남음 |
| QA-PAY-16A | 대표 | iPhone 16e Simulator 네이티브 앱에서 수기 결제 195,000원 등록, 사유와 함께 205,000원으로 수정, 삭제 사유 입력 후 삭제 | 생성·수정·삭제가 앱 재시작 없이 반영되고 수정 전후 스냅샷과 삭제 사유가 감사 기록에 남으며 삭제 뒤 `수기 결제 기록을 삭제했습니다.` 안내가 표시됨. `.data/mobile-builds/ios/manual-payment-management-20260713/simulator-summary.json` 증빙은 내부 QA 전용이고 실 PG·운영 데이터·IPA 준비 완료를 의미하지 않음 |
| QA-PAY-17 | 대표/코치 | 온라인·정기·환불 이력 결제의 직접 수정/삭제 및 코치의 수기 결제 삭제 API 시도 | 외부/환불 이력은 `422`, 코치는 `403`으로 차단되고 원본 결제 기록이 유지됨 |
| QA-PAY-18 | 코치 | 정기결제 약정이 있는 결제 포함 수업/회원 화면 확인 | 회원권 상태는 보이되 `recurringAgreement.providerAgreementId`는 노출되지 않음 |
| QA-PAY-19 | 총괄 | `.data/payment-provider-handoff.json` 작성 후 `npm run payment-provider:handoff -- --file=.data/payment-provider-handoff.json --out=.data/payment-provider-handoff.report.json` 실행 | 실 PG/VAN 계약, HTTPS checkout, webhook 서명/idempotency, 영수증 URL, billing key 보관, 정기결제 해지, 코치 금액 마스킹, 변경 기록, HTTPS/provider URI 증빙, 템플릿의 `*_EVIDENCE_URI` placeholder, `localhost`/`.example`/TODO checkout origin, ISO 생성/승인 시각, 생성 이후 승인 순서, 원문 secret 미보관 증빙이 ready이면 통과 |
| QA-PAY-20 | 대표/총괄 | `/app/payments`에서 확인할 결제 확인 | 현재 필터 기준 미납 연락, 재등록 안내, 온라인 결제 재요청/확인, 정기결제 실패 확인이 우선순위와 회원/지점/금액 근거로 표시 |
| QA-PAY-21 | 대표/총괄 | `/app/payments`에서 `P2 결제/회원권 운영 보드` 확인 | 현재 필터 기준 미납 회수, 만료/재등록, 환불/할인 점검, 상태 이력, 실 결제 연동 증빙 전 준비 상태가 표시되고 외부 증빙 전 결제 기능은 출시 가능 상태로 분리하지 않음 |
| QA-PAY-22 | 대표/총괄 | 수기 결제 생성·수정에서 만료일을 납부일보다 앞선 날짜로 제출하고, 상태는 유지한 채 금액·날짜만 정상 정정 | 역전된 날짜는 `400 VALIDATION_ERROR`로 차단되고 정상 정정은 감사 기록만 남기며 `statusHistory` 건수는 증가하지 않음 |
| QA-PAY-23 | 대표/총괄 | 수기 결제 등록 API에 `partially_refunded`를 제출하고, 같은 결제의 수기 수정과 온라인 요청을 동시에 시도 | 부분 환불 직접 등록은 `400`으로 차단되고 두 변경은 순차 처리되어 온라인 요청 금액과 저장 금액이 어긋나지 않음 |
| QA-PAY-24 | 대표 | 0원 환불 완료 또는 환불액 누락 부분 환불 레코드의 수정·삭제 API 호출 | 환불 상태 자체를 이력으로 인식해 두 요청 모두 `422 BUSINESS_RULE_FAILED`로 차단되고 원본 결제와 상태 이력이 유지됨 |
| QA-AUTH-11 | 비로그인 사용자 | 같은 휴대폰 번호와 비밀번호로 회원가입 요청 2건을 동시에 제출 | 정확히 한 건만 `200`, 다른 한 건은 `409 CONFLICT`이며 사용자와 연결 회원 프로필이 각각 1건만 저장됨 |
| QA-AUTH-11A | 비로그인 사용자 | 복수 활성 지점에서 지점 없이 가입하거나 존재하지 않는 지점 ID로 가입 | 두 요청 모두 `400 VALIDATION_ERROR`, 사용자·회원·감사 기록 미생성. 공개 지점 조회는 `id`, `name`, `district`만 노출하고 정상 선택 시 사용자와 회원이 같은 선택 지점에 저장됨 |
| QA-REPORT-01 | 대표 | `/app/owner/reports`에서 운영 리포트 내보내기 | 지점별 회원/수업/출석/매출/결제위험/출석 미처리/점검 점수 CSV가 내려오고 `export.create` 변경 기록 생성 |
| QA-REPORT-02 | 대표 | `/app/owner/reports` 우선 점검 지점 확인 | 출석 미처리, 결제 위험, 휴면 회원 배지가 지점별 점검 점수와 함께 표시 |
| QA-REPORT-03 | 대표 | `/app/owner/reports` 오늘 우선순위 확인 | 출석 미처리 정리, 결제 위험 확인, 휴면 회원 케어가 우선순위와 담당 역할로 표시되고 요청 승인 카드는 표시되지 않음 |
| QA-REPORT-04 | 대표/총괄 | `/app/owner/reports`에서 `대표 운영 판단`과 기간 필터 확인 | 최근 3/6/12개월 필터 기준 회원 증감, 출석률 변화, 결제 위험 변화, 매출 추이, 지점 비교가 표시되고 CSV는 대표/총괄 전용 내보내기로 유지 |
| QA-REPORT-05 | 대표/총괄 | `/app/owner/reports`에서 `P3 운영 판단 보드` 확인 | 장기 추세, 위험 회원, 재등록 후보, 결제 회수 우선순위, 지점별 액션 추천, `P3 지점 액션 실행표`가 표시되고 담당자, 확인 지표, 다음 확인이 반복 운영 루틴으로 연결되며 P1 release readiness와 분리됨 |
| QA-REPORT-06 | 대표/총괄 | `/app/owner/reports`에서 `P3 회원 유지/회수 액션 플래너` 확인 | 위험 회원, 재등록 후보, 결제 회수 우선순위가 오늘 연락 대상, 담당자, 연락 채널, 다음 확인으로 표시되고 CSV 내보내기 권한은 대표/총괄 전용으로 유지 |
| QA-REPORT-07 | 대표/총괄 | `/app/owner/reports`에서 `P3 지점 운영 실험 큐` 확인 | 지점별 액션 추천이 운영 실험 가설, 성공 지표, 회고 시점, 중단 신호, 검증 명령으로 바뀌고 다음 운영 리뷰와 P1 release readiness 분리가 함께 보임 |
| QA-REPORT-08 | 대표/총괄 | `/app/owner/reports`에서 `P3 재등록/회수 코호트 보드` 확인 | 재등록 후보 코호트, 결제 회수 코호트, 위험 회원 코호트, 장기 추세 코호트가 코호트 신호, 정렬 기준, 담당자, 다음 운영 리뷰, 검증 명령으로 표시되고 P1 release readiness와 분리됨 |
| QA-REPORT-09 | 대표/총괄 | `/app/owner/reports`에서 `P3 지점 조기 경보 보드` 확인 | 출석률 하락, 결제 위험 증가, 회원 순감, 매출 하락이 조기 경보 점수, 감지 신호, 1차 대응, 확인 창, 검증 명령으로 표시되고 P1 release readiness와 분리됨 |
| QA-REPORT-10 | 대표/총괄 | `/app/owner/reports`에서 `P3 월간 운영 리듬 보드` 확인 | 1~4주차 장기 추세 점검, 위험 회원/결제 회수, 재등록 후보 상담, 지점 비교/액션 리뷰가 의사결정, 운영 증거, 확인 기준, 검증 명령으로 표시되고 P1 release readiness와 분리됨 |
| QA-REPORT-11 | 대표/총괄 | `/app/owner/reports`에서 `P3 지점 유지율 경보 큐` 확인 | 회원 순감, 출석률 하락, 휴면 회원, 결제 위험이 유지율 경보, 감지 신호, 우선 대응, 확인 마감, 운영 증거, 검증 명령으로 표시되고 P1 release readiness와 분리됨 |
| QA-REPORT-12 | 대표/총괄 | `/app/owner/reports`에서 `P3 유지율 후속 기록 보드` 확인 | 유지율 경보 이후 후속 대상, 담당자, 변경 기록, 재확인 시점, 닫힘 기준, 검증 명령이 표시되고 P1 release readiness와 분리됨 |
| QA-CLASS-01 | 대표 | `/app/classes`에서 수업 생성 | 수업 목록에 새 수업이 추가되고 변경 기록 생성 |
| QA-CLASS-02 | 대표 | `/app/classes`에서 장소/정원 수정 | 수정값이 반영되고 변경 기록 생성 |
| QA-MEMBER-01 | 코치 | `/app/members`에서 담당 회원 상담/주의 메모 작성 | 메모가 회원 카드에 추가되고 `counseling_note.create` 변경 기록 생성 |
| QA-DASH-01A | `npm run test:dashboard-priority-kpi` 실행 | 총괄 대시보드 첫 화면 KPI가 내부 변경 기록 요약으로 회귀하지 않고 대기 초대와 사용자 관리 액션을 우선 표시한다 |
| QA-MEMBER-01A | `npm run test:smoke` 실행 | 회원 수정, 보호자 연결, 상담/주의 메모 작성 API에서 익명 요청은 401, 권한 없는 역할은 403으로 본문 검증보다 먼저 차단. 객체형 상담 메모 본문은 `400`이고 메모·감사 기록 무변경, 정상 동시 작성 2건은 UUID 기반 메모·감사 ID로 모두 보존 |
| QA-MEMBER-01A1 | `npm run test:member-profile-guardian-edit` 실행 | 운영자 회원 상세에서 연령 수정 저장 요약, 보호자 검색 기반 변경/해제 UI, 보호자-자녀 양방향 링크 갱신 API와 smoke 회귀 범위가 유지된다 |
| QA-MEMBER-01A2 | `npm run test:member-management-touch-targets` 실행 | 390px 모바일 대표 회원 관리 컨트롤이 44px 이상이고, 회원 상세 결제·회원권 요약은 대표만 금액을 확인하며 회원·학부모는 상태·납부일·만료일만 보고 코치는 요약을 받지 않는다. 학부모가 정확한 자녀·결제 딥링크 진입 후 다른 자녀를 선택해도 선택이 유지되고 overflow 0, 콘솔 오류 없음, iPhone 16e 증빙이 있으면 통과 |
| QA-MEMBER-01A3 | 대표/총괄 | 보호자 연결 POST·변경 PUT·해제 DELETE에 객체·배열 또는 201자 `guardianUserId`를 전송하고, 미완성 POST 본문을 열어 둔 동안 다른 회원의 DELETE를 실행한 뒤 서로 다른 유소년 회원 2명을 동시에 정상 연결 | 잘못된 타입·길이는 `400 VALIDATION_ERROR`이고 회원 `guardianIds`, 학부모 `childMemberIds`, 감사 기록이 변하지 않는다. 느린 본문은 공통 관계 잠금을 점유하지 않아 다른 변경이 먼저 완료되며, 정상 동시 연결은 양방향 관계와 UUID 기반 서로 다른 `member.update` 감사 기록 2건을 모두 보존 |
| QA-MEMBER-01A4 | 학부모/코치 | 연결되지 않은 회원 ID와 존재하지 않는 회원 ID로 회원 수정 API 호출 후, 담당 회원에 코치가 수정 요청 | 학부모의 두 요청은 동일한 `404 NOT_FOUND` 오류를 반환해 타 가족 회원 존재를 노출하지 않는다. 담당 회원을 읽을 수 있으나 수정 권한이 없는 코치 요청은 `403 FORBIDDEN`을 유지하며 회원·감사 기록은 변하지 않는다 |
| QA-MEMBER-01A5 | 대표 | 회원 지점에 연결할 수 없는 실재 학부모 ID와 존재하지 않는 학부모 ID로 보호자 변경·해제 요청 | 두 변경 요청은 같은 `422 BUSINESS_RULE_FAILED` 오류를 반환하고, 연결되지 않은 두 ID의 해제는 모두 멱등 성공한다. 회원·학부모 관계와 감사 기록은 바뀌지 않아 계정 존재 여부를 노출하지 않는다 |
| QA-MEMBER-01A6 | 코치 | 담당 회원에게 2,001자 상담/주의 메모 작성 요청 후 2,000자 이하 정상 메모 작성 | 초과 요청은 `400 VALIDATION_ERROR`이고 메모·감사 기록이 변하지 않는다. 작성 폼은 서버와 같은 `maxLength=2000`과 44px 이상 입력 영역을 사용하고 정상 메모는 공개 범위·감사 기록과 함께 저장됨 |
| QA-MEMBER-01B | `npm run test:smoke` 실행 | 회원 등록 API에서 익명 요청은 401, 코치 요청은 403으로 본문 검증보다 먼저 차단 |
| QA-MEMBER-01B1 | 대표/총괄 | 회원 등록 API에 객체형 회원명, 존재하지 않는 생년월일을 전송한 뒤 정상 회원 2명을 동시에 등록 | 잘못된 타입·날짜는 `400 VALIDATION_ERROR`이고 회원·감사 기록 무변경. 동시 정상 등록은 UUID 기반 서로 다른 회원 ID로 둘 다 저장되고 `member.create` 감사 기록도 각각 한 건 생성 |
| QA-MEMBER-01B2 | 대표/총괄 | 회원 수정 API에 객체형 생년월일 또는 존재하지 않는 날짜를 전송한 뒤 같은 회원 상태를 두 값으로 동시에 변경 | 잘못된 타입·날짜는 `400 VALIDATION_ERROR`이고 기존 생년월일·상태·감사 기록이 변하지 않는다. 동시 정상 수정은 두 요청 모두 성공하고 마지막 직렬화 상태와 UUID 기반 `member.update` 감사 기록 2건을 보존 |
| QA-MEMBER-01B3 | 대표/총괄 | 이름·레벨·띠 31자 또는 비상 연락처 41자로 회원 등록 | 모든 요청이 `400 VALIDATION_ERROR`로 차단되고 회원·감사 기록이 생기지 않는다. 등록·수정 화면은 공통 이름/레벨/띠 30자, 연락처 40자, 주소 100자와 주의사항 8개·항목당 80자 상한을 사용 |
| QA-CLASS-01A | `npm run test:smoke` 실행 | 출석 저장/사유 기록과 수업 생성/수정 API에서 익명 요청은 401, 권한 없는 역할은 403으로 본문 검증보다 먼저 차단 |
| QA-CLASS-01B | `npm run test:class-management-touch-targets` 실행 | 390px 모바일 대표 수업 화면에서 수업 생성 폼은 기본 접힘으로 시작하고, `열기` 후 수업명/연령/레벨/코치/정원/시작/종료/장소 입력과 생성 버튼, 각 수업 카드의 장소/정원 수정 입력과 저장 버튼이 44px 이상이다. 코치 수업 화면에서는 명단을 연 뒤 출석 메모 토글, 현장 메모 입력, 빠른 메모, 사유 저장 버튼이 44px 이상이고 overflow 0, 콘솔 오류 없음, 증빙 스크린샷이 유지되면 통과 |
| QA-CLASS-01C | 대표/총괄 | 수업 생성 API에 객체형 수업명, 문자열 정원 또는 비문자 등록 회원 목록을 전송한 뒤 정상 수업 2개를 동시에 등록 | 잘못된 타입은 `400 VALIDATION_ERROR`이고 수업·감사 기록 무변경. 동시 정상 등록은 UUID 기반 서로 다른 수업 ID로 둘 다 저장되고 `class.create` 감사 기록도 각각 한 건 생성 |
| QA-CLASS-01D | 대표/총괄 | 수업 수정 API에 객체형 장소 또는 존재하지 않는 시간 값을 전송한 뒤 같은 수업의 장소를 두 값으로 동시에 변경 | 잘못된 타입·시간은 `400 VALIDATION_ERROR`이고 기존 수업·감사 기록이 변하지 않는다. 동시 정상 수정은 두 요청 모두 성공하고 마지막 직렬화 장소와 UUID 기반 `class.update` 감사 기록 2건을 보존 |
| QA-CLASS-01E | 대표/총괄 | 수업명·장소 81자, 레벨 41자, 201자 코치/회원 ID 또는 등록 회원 원본 배열 81개로 수업 생성·수정 | 모든 요청이 `400 VALIDATION_ERROR`로 차단되고 수업·감사 기록이 변하지 않는다. 생성·수정 화면은 수업명 80자, 레벨 40자, 장소 80자 상한을 사용 |
| QA-PROMOTION-01 | 대표/총괄/담당 코치 | 승급 심사 생성에 객체형·201자 회원 ID, 생성·결과 기록에 객체형·501자 공개 메모를 전송한 뒤 서로 다른 회원의 정상 심사 2건을 동시에 등록 | 잘못된 타입·상한 초과 입력은 `400 VALIDATION_ERROR`이고 심사·회원 띠·감사 기록 무변경. 정상 동시 등록은 UUID 기반 서로 다른 심사 ID로 둘 다 저장되고 `promotion.create` 감사 기록도 각각 한 건 생성. 같은 회원 동시 등록과 같은 심사 동시 결과는 정확히 한 요청만 반영됨 |
| QA-NOTICE-11A | `npm run test:smoke` 실행 | 삭제된 요청 API는 404, 공지 작성/일괄 읽음, 알림 구독/해지 API에서 익명 요청은 401, 권한 없는 역할은 403으로 본문 검증보다 먼저 차단 |
| QA-PILOT-01A | `npm run test:smoke` 실행 | 파일럿 준비 상태 변경, 운영 이슈 생성/수정, 운영 로그 저장 API에서 익명 요청은 401, 코치 요청은 403으로 본문 검증보다 먼저 차단 |
| QA-PILOT-01B | 총괄 | 파일럿 준비·이슈·운영 로그 API에 객체형 문자열 필드와 확인 메모 1,001자·이슈 상세 2,001자·우회책 1,001자·모바일 출석 확인 501자를 전송한 뒤 같은 준비 항목, 같은 이슈, 같은 지점·일자 운영 로그를 각각 동시에 변경 | 잘못된 타입과 초과 입력은 잠금 전에 `400 VALIDATION_ERROR`이고 상태·이슈·운영 로그·감사 기록이 변하지 않는다. 동시 요청은 모두 정상 응답하고 마지막 직렬화 결과와 UUID 기반 고유 감사 기록 2건을 보존하며, 동시 이슈 생성은 고유 이슈 ID 2건을 보존 |
| QA-MEMBER-02 | 코치 | 스태프 전용 상담 메모 API 호출 | `403 FORBIDDEN`으로 차단 |
| QA-MEMBER-03 | 대표/총괄 | `/app/members`에서 스태프 전용 메모 작성 | 대표/총괄 화면에 표시되고 학부모 화면에는 미노출 |
| QA-MEMBER-04 | 회원/학부모 | `/app/members`에서 본인/자녀 긴급 연락처 수정 | 연락처가 저장되고 상태 변경 같은 제한 필드는 `403 FORBIDDEN`, `member.update` 변경 기록 생성 |
| QA-REQ-01 | 전체 | `/app/requests` 또는 `/requests` 직접 접근 | `404`이고 요청 작성 폼, 요청 카드, 승인/반려 액션이 표시되지 않음 |
| QA-REQ-02 | 회원/학부모 | `POST /api/v1/requests` 호출 | `404`, 요청 레코드와 변경 기록은 추가되지 않음 |
| QA-REQ-03 | 코치/대표/총괄 | `POST /api/v1/requests/:requestId/approve` 호출 | `404`, 출석 기록과 요청 레코드는 변경되지 않음 |
| QA-REQ-04 | 코치/대표/총괄 | `POST /api/v1/requests/:requestId/reject` 호출 | `404`, 요청 레코드와 변경 기록은 변경되지 않음 |
| QA-NOTICE-01 | 코치 | `/app/notices` 공지 확인 | 대상 공지, 미읽음 상태, 읽음 처리 표시. 삭제된 요청 화면 없이 공지함이 전용 화면에서만 보임 |
| QA-NOTICE-02 | 코치 | 공지 읽음 처리 클릭 | 상태가 읽음으로 변경되고 읽음 수 증가 |
| QA-NOTICE-02A | 회원/학부모/코치 | `/app/notices`에서 단일 공지 `확인`/`읽음` 클릭 | 버튼은 저장 중 중복 클릭을 막고 `공지 확인을 저장했습니다.` 피드백을 표시하며 읽은 공지는 톤다운 상태로 전환 |
| QA-NOTICE-02B | 대표/총괄/코치 | `/app/notices`에서 공지 읽음 후 같은 공지의 알림 발송 클릭 | 새 알림 발송 피드백만 남고 이전 읽음/작성/삭제 피드백은 함께 보이지 않음 |
| QA-NOTICE-02C | 대표/총괄/코치 | 공지 생성·수동 재발송 뒤 `notification.dispatch` 감사 기록 확인 | 공지 생성과 `dispatchState: requested` 감사 기록이 외부 발송보다 먼저 저장되고 결과가 `completed`/`blocked`/`failed`와 완료 시각으로 갱신됨. 생성 후 푸시 실패는 공지 생성 실패로 오인되지 않아 같은 공지를 다시 만들지 않음 |
| QA-NOTICE-03 | 대표 | `/app/notices`에서 공지 작성 | `작성 열기`로 폼을 연 뒤 새 공지를 발행하면 공지함에 표시되고 `notice.create` 변경 기록 생성 |
| QA-NOTICE-03A | 대표/총괄 | 공지 생성 API에 객체형 제목 또는 비문자 대상 배열 전송 후 정상 공지 2건 동시 생성 | 잘못된 본문은 `400 VALIDATION_ERROR`이고 공지·감사·outbox가 생기지 않는다. 정상 동시 생성은 UUID 기반 공지 ID가 서로 다르고 `notice.create` 감사 기록 2건이 모두 보존됨 |
| QA-NOTICE-03B | 대표/총괄 | 제목 121자, 본문 5,001자, 같은 회원 ID 501개로 공지 생성 후 기존 공지 제목을 121자로 수정 | 모든 요청이 `400 VALIDATION_ERROR`로 차단되고 공지·감사·outbox 건수와 기존 공지 내용이 유지된다. 작성·수정 화면 입력도 서버와 같은 제목 120자·본문 5,000자 상한을 사용 |
| QA-NOTICE-04 | 전체 | `/app/notices` 접속 | 공지 목록, 읽음 상태, 읽음 처리 표시 |
| QA-NOTICE-05 | 대표/총괄 | `/app/notices`에서 공지 작성 | 새 공지가 공지함에 표시되고 `notice.create` 변경 기록 생성 |
| QA-NOTICE-06 | 대표/총괄 | `/app/notices`에서 반 대상/개인 대상 공지 작성 | 대상 반/회원 관계 사용자에게만 노출되고 비대상 회원/학부모는 미노출, 읽음 처리와 `notice.create` 변경 기록 생성 |
| QA-NOTICE-07 | 전체 | `/app/notices` 알림 설정 확인 | 알림 꺼짐/허용됨/차단됨/미지원 상태가 표시되고 허용 상태에서는 알림 확인 버튼이 활성화 |
| QA-NOTICE-07A | 회원/학부모 | 활성 푸시 구독 상태에서 객체형 endpoint로 구독 해제 요청 후 정상 해제 요청 | 잘못된 해지는 `400 VALIDATION_ERROR`이고 현재 사용자 활성 구독·표시 건수가 유지됨. 정상 해제 요청은 `enforcedAlwaysOn: true`, `disabledAt: null`을 반환하고 UUID 기반 차단 감사 기록을 남김 |
| QA-NOTICE-07B | 회원/학부모 | 2,048자를 초과한 endpoint나 512자를 초과한 key로 푸시 구독 등록·해지 요청 | URL 파싱·잠금·저장 전 `400 VALIDATION_ERROR`로 거부되고 현재 사용자의 활성 구독·표시 건수가 유지됨 |
| QA-NOTICE-08 | 전체 | 미읽음 공지가 있는 계정으로 모바일 진입 | 상단 공지 버튼에 현재 사용자/지점 스코프의 미읽음 공지 수가 표시되고 접근성 이름에 미읽음 건수가 포함된다. 요청 메뉴는 하단 내비에 표시되지 않는다 |
| QA-NOTICE-09 | 대표/총괄 | `/app/notices`에서 `중요 공지` 체크 후 발행 | 공지 목록 상단에 중요 배지가 표시되고, API 응답과 `notice.create` 변경 기록에 `important: true`, 푸시 제목에 `[중요]` prefix가 반영 |
| QA-NOTICE-14 | 대표/총괄 | 읽은 공지의 제목·본문·중요 여부·역할 audience를 실제 변경 | 기존 `readByUserIds`가 초기화되어 대상 회원/학부모 알림함에 다시 미확인으로 보이고 `notice.update` 감사 로그에는 본문 원문 없이 길이·변경 여부·읽음 초기화 여부가 기록됨. 수정만으로 휴대폰 푸시는 재발송하지 않음 |
| QA-NOTICE-15 | 대표/총괄/코치 | 동일 내용 재저장, 코치 audience 변경, 반·개인 대상 PATCH, 읽음·수정 동시 요청 | 동일 내용과 순서·중복만 다른 같은 audience 재저장은 읽음 상태를 유지하고, 코치의 audience 변경은 `403 FORBIDDEN`, 생성 후 반·개인 대상 변경과 잘못된 타입은 `400 VALIDATION_ERROR`. 읽음·수정은 직렬화되어 마지막 작업과 최종 읽음 상태가 일치 |
| QA-NOTICE-10 | 전체 | `/app/notices`에서 전체/미읽음/중요 보기 전환 | 현재 사용자/지점 스코프 안에서 표시 건수와 목록이 바뀌고, 결과가 없으면 선택한 보기의 빈 상태가 표시 |
| QA-NOTICE-11 | 전체 | `/app/notices`에서 필터 적용 후 보이는 공지 읽음 처리. 단일·일괄 API에 타인 개인 공지 ID와 존재하지 않는 ID를 각각 전송 | 화면에 보이는 미읽음 공지만 읽음 처리되고, 타인·다른 지점 공지와 존재하지 않는 공지는 식별자 목록 없이 동일한 `404 NOT_FOUND`로 응답해 존재 여부를 노출하지 않으며 부분 읽음 없이 정상 요청만 `notice.read` 변경 기록을 남김 |
| QA-NOTICE-11B | 회원/학부모 | 읽을 수 있는 미확인 공지 ID에 객체형 항목을 섞거나 같은 ID를 51개 넣어 일괄 읽음 요청 | 혼합 타입은 `400 VALIDATION_ERROR`, 중복 포함 원본 51건은 `422 BUSINESS_RULE_FAILED`로 중복 제거 전에 차단되고 대상 공지는 미확인 상태를 유지하며 감사 기록도 생기지 않음 |
| QA-NOTICE-12 | 전체/운영자 | `/app/notices` 확인할 공지 확인 | 현재 필터 기준 중요 미읽음 확인, 미읽음 공지 읽음 처리, 중요 공지 푸시 점검, 읽음 반응 점검이 우선순위로 표시 |
| QA-NOTICE-13 | 전체/운영자 | `/app/notices`에서 `공지 요약` 확인 | 중요 공지, 미확인 공지, 읽음 확인, 알림 수신이 현재 필터 기준으로 분리되고 운영 푸시 외부 증빙은 P1 blocker로 유지 |

## 7. 총괄 어드민 테스트

| ID | 절차 | 기대 결과 |
| --- | --- | --- |
| QA-ADMIN-01 | 총괄 어드민 로그인 | 하단/사이드 메뉴에 권한 메뉴 표시 |
| QA-ADMIN-02 | `/app/admin/users` 접속 | 전체 사용자, 활성 계정, 대기 초대, 대표/코치/학부모/회원 역할별 보기, 사용자 초대 폼, 사용자별 수정/삭제/비밀번호 재발급 액션 표시 |
| QA-ADMIN-02A | `/app/admin/users`에서 사용자 수정 열기 | 이름, 이메일, 역할, 설명, 담당 지점, 선택 새 비밀번호/확인, 수정 사유 입력과 `user.update` 변경 기록. 새 비밀번호 원문과 password hash는 화면 응답/변경 기록에 노출되지 않음 |
| QA-ADMIN-02B | `/app/admin/users`에서 사용자 삭제 열기 | 삭제 사유 입력이 필요하고 본인 계정, 마지막 총괄 어드민, 단독 대표 계정은 차단된다. 담당 수업·회원은 다른 코치/대표/총괄에게 인계되고 성공 시 인계 건수와 `user.delete` 변경 기록 생성 |
| QA-ADMIN-02C | 사용자 삭제 제출 중 API 연결 실패 | 삭제 폼을 유지하고 `사용자 계정을 삭제하지 못했습니다. 현재 계정, 총괄 유지, 지점 대표 배정 상태를 확인해 주세요.`를 표시한다. iPhone 16e Simulator 증빙에서 안내 문구가 하단 내비게이션과 겹치지 않는다 |
| QA-ADMIN-02D | 사용자 수정 API에 숫자·배열·빈 객체를, 삭제 API에 숫자 본문을 전송 | 모두 `400 VALIDATION_ERROR`이며 대상 계정, 수업·회원 인계, 세션, 감사 기록이 변경되지 않고 서버 오류가 발생하지 않음 |
| QA-ADMIN-03 | `/app/admin/roles` 접속 | 시스템 역할, 관리 권한 보유자, 대기 초대, 감사 이벤트 카드 표시 |
| QA-ADMIN-04 | 사용자 검색 | 이름/역할/RBAC 키워드로 목록 필터 |
| QA-ADMIN-05 | 권한 매트릭스 확인 | 리소스 x 보기/생성/수정/삭제/승인 상태 표시 |
| QA-ADMIN-06 | 내 계정 보호 확인 | 내 계정 총괄 상태 유지, 기본 역할 삭제 제한, 위험 권한 승인 필요 표시 |
| QA-ADMIN-07 | 담당 수업이 있는 코치 역할 변경 후 사유 저장, 단독 대표 역할 제거 시도 | 코치 역할 변경은 수업·회원이 다른 운영자에게 인계되고 인계 건수와 `user.role.update` 변경 기록이 생성된다. 단독 대표 역할 제거는 `422`로 차단됨 |
| QA-ADMIN-08 | 내 총괄 어드민 권한 제거 시도 | 서버가 `422 BUSINESS_RULE_FAILED`로 차단 |
| QA-ADMIN-09 | `/app/admin/branches`에서 지점 생성 | 새 지점이 추가되고 `branch.create` 변경 기록 생성 |
| QA-ADMIN-09A | 총괄 | 객체형 또는 지점명 81자·지역 101자·시간대 65자·사유 501자·대표 ID 201자 요청을 전송하고, 생성·수정·대표 배정의 미완성 본문을 열어 둔 동안 다른 지점 변경을 실행한 뒤 같은 이름 동시 생성과 생성·이름 변경 교차 요청 | 잘못된 타입·길이는 `400 VALIDATION_ERROR`이고 지점·대표·감사 기록 무변경. 느린 본문은 공통 지점 잠금을 점유하지 않아 다른 변경이 먼저 완료된다. 같은 이름 생성과 생성·이름 변경은 각각 한 요청만 `200`, 다른 요청은 `409`; 서로 다른 지점은 고유 ID와 UUID 감사 기록으로 모두 보존 |
| QA-ADMIN-10 | 지점 대표 배정 | 대표 사용자 지점 범위에 해당 지점이 추가되고 `branch.owner.assign` 변경 기록 생성 |
| QA-ADMIN-11 | 지점 운영 설정 변경 후 사유 저장 | 지점명/지역/상태/시간대 라벨/출석 수정 사유 정책이 반영되고 `branch.update` 변경 기록 생성 |
| QA-ADMIN-12 | 코치로 `/app/admin/branches` 직접 접근 | 403 권한 없음 화면 표시 |
| QA-ADMIN-13 | 총괄로 `/app/admin/audit-logs` 접속 후 액션/지점/검색어 필터 적용 | 변경 기록 목록, 요약 카운트, before/after diff 표시 |
| QA-ADMIN-14 | 코치로 `/api/v1/admin/audit-logs?reason=test` 직접 호출 | `403 FORBIDDEN` 반환 |
| QA-ADMIN-15 | 총괄로 `/api/v1/admin/audit-logs`를 `reason` 없이 호출 | `400 VALIDATION_ERROR` 반환 |
| QA-ADMIN-16 | `/app/admin/settings` 접속 | 역할/RBAC 기준, 지점 정책, 변경 기록 필수 이벤트 표시 |
| QA-ADMIN-17 | `/app/admin/settings` 파일럿 준비 게이트 확인 | 자동 검증 명령, 보안 audit, 운영 데이터 점검, 현장 확인 필요 항목 표시 |
| QA-ADMIN-18 | 파일럿 준비 항목 상태/담당자/증빙 저장 | 저장 후 상태 배지와 증빙이 유지되고 `pilot_readiness.update` 변경 기록 생성 |
| QA-ADMIN-19 | 파일럿 준비 항목에서 `계정별 임시 비밀번호 교체` 확인 | 보안 카테고리 항목이 표시되고 증빙 저장 가능 |
| QA-ADMIN-20 | `/app/admin/users`에서 임시 비밀번호 발급 후 `/app/admin/audit-logs` 조회 | 발급 원문은 변경 기록에 없고 사유/대상/`auth.password_reset.complete`만 표시 |
| QA-ADMIN-21 | `/app/admin/settings`에서 파일럿 이슈 기록 | P0/P1/P2 심각도, 역할, 화면, 우회책이 저장되고 `pilot_incident.create` 변경 기록 생성 |
| QA-ADMIN-22 | 파일럿 이슈를 모니터링/해결 상태로 변경 | 우회책/조치 메모가 유지되고 `pilot_incident.update` 변경 기록 생성 |
| QA-ADMIN-23 | 코치가 `/api/v1/admin/pilot-incidents` 직접 호출 | `403 FORBIDDEN` 반환 |
| QA-ADMIN-24 | `/app/admin/settings`에서 일일 운영 로그 저장 | 운영 일자, 지점, 출석/결제/공지 후속/공지 수치, 모바일 출석 30초 계측 증빙이 저장되고 `pilot_operation.update` 변경 기록 생성. 화면은 지점별 중복 로그가 아닌 고유 운영일 기준 14일 누락일, 다음 입력 추천일, 모바일 30초 증빙 누락을 표시 |
| QA-ADMIN-25 | 코치가 `/api/v1/admin/pilot-operations` 직접 호출 | `403 FORBIDDEN` 반환 |
| QA-ADMIN-26 | 파일럿 운영 로그에 불가능한 날짜 또는 차단 사유 없는 `blocked` 상태 저장 시도 | API가 400으로 거절하고 운영 로그가 생성되지 않음 |
| QA-ADMIN-27 | `npm run test:pilot-readiness-contract`, `npm run test:visible-app-copy-stability` 실행 | 기본 준비 항목, import 생성 항목, preflight 필수 항목의 ID/문구/담당자 계약이 정합하고 390px 관리자 설정의 준비·운영·이슈 생성·수정 폼이 서버와 같은 입력 상한, overflow 0, 콘솔 오류 0을 유지하면 통과 |
| QA-ADMIN-28 | `npm run test:preflight-runtime` 실행 | 현재 런타임 DB에서 남아 있는 데모 기본 비밀번호와 준비 미완료 외 blocker, 예상 warning 외 warning, collection count drift, 참조 오류, 민감정보, 깨진 운영 로그가 없으면 통과. 이미 비밀번호를 교체한 데모 계정은 정상으로 인정 |
| QA-ADMIN-29 | `npm run test:pilot-evidence` 실행 | 파일럿 증빙 리포트가 runtime/preflight/준비 항목/이슈/출석·결제·공지 후속·공지 운영 로그 요약과 post-pilot 차단 조건을 포함하면 통과 |
| QA-ADMIN-29A | `npm run test:pilot-field-evidence-draft` 실행 | source CSV와 pre/post evidence 리포트에서 현장 증빙 manifest 초안의 지점/계정/명령/운영 수치가 자동 반영되고, placeholder 초안은 strict 검증에서 차단되며 운영자가 현장 값을 채운 manifest가 validator를 통과하면 완료 |
| QA-ADMIN-30 | `npm run test:pilot-field-evidence` 실행 | 현장 증빙 manifest 검증기가 템플릿 shape, 통과 fixture, source CSV 검증과 dry-run/import 명령 CSV 참조, manifest 지점/계정 email·role·branchScope 대조, pre-pilot evidence readiness와 source CSV count 대조, 스크린리더 출석 상태/공지 읽음 상태의 기대 낭독·실제 낭독·이슈 없음 기록, 공지 후속/공지 누락과 post-pilot 리포트 수치/경로/freshness/signoff chronology 불일치를 포함한 실패 fixture를 검증하면 통과 |
| QA-ADMIN-31 | `npm run test:pilot-closeout-package` 실행 | post-pilot strict preflight JSON, `pilot:evidence` JSON, Markdown 회고 리포트, 현장 증빙 manifest가 하나의 ready 판단 패키지로 묶이고 blocked preflight/Markdown 누락 fixture가 차단되면 통과 |
| QA-ADMIN-32 | `npm run test:pilot-artifact-manifest` 실행 | pre-pilot/post-pilot preflight, `pilot:evidence` JSON/Markdown, launch package, 계정별 비밀번호 교체 CSV, 현장 manifest, closeout package의 ready 상태와 SHA-256 해시 manifest가 생성되고 변조된 launch package/closeout 및 누락 파일 fixture가 차단되면 통과 |
| QA-ADMIN-33 | `npm run test:pilot-archive-artifacts` 실행 | ready 아티팩트 manifest의 원본 파일 SHA-256/byte size가 archive 직전에 재검증되고 fresh archive 폴더에 산출물/원본 manifest/archive manifest가 복사되며 복사본 해시 검증, 변조 파일, blocked manifest, 기존 archive 폴더, placeholder archive 경로 fixture가 차단되면 통과 |
| QA-ADMIN-33A | `npm run test:pilot-storage-receipt-draft` 실행 | archive manifest에서 장기 보관 receipt 초안의 archive manifest SHA-256/byte size와 산출물별 archivedPath/sha256/sizeBytes가 자동 반영되고, placeholder 초안은 strict receipt 검증에서 차단되며 운영자가 보관 URI와 증빙을 채운 receipt가 validator를 통과하면 완료 |
| QA-ADMIN-34 | `npm run test:pilot-storage-receipt` 실행 | archive manifest와 장기 보관 업로드 receipt의 보관 URI, 업로드 담당자/provider/evidence, 최소 365일 보존 정책, 산출물별 경로/해시/byte size/storage location, archive copy 해시 검증과 placeholder/누락/시간순서/변조 fixture 차단이 통과 |
| QA-ADMIN-35 | `npm run test:pilot-final-handoff` 실행 | artifact manifest, archive manifest, storage receipt가 같은 산출물 경로/해시/byte size를 가리키고 storage receipt 검증이 ready이며, 누락 archive 항목/변조 archive 파일/blocked manifest fixture가 차단되면 통과 |
| QA-ADMIN-35A | `npm run test:pilot-status` 실행 | 파일럿 런타임, pre/post preflight/evidence, pre-pilot Markdown, 준비 증빙 CSV, 비밀번호 교체 CSV, launch package, 현장 manifest, closeout, artifact/archive manifest, storage receipt, final handoff 상태 요약이 누락/대기/ready/14일 운영 부족/strict 실패/변조 archive fixture를 올바르게 판정하고, 준비/비밀번호 증빙 CSV 누락 시 전용 다음 액션과 ready/blocked status JSON 출력 파일을 보존하면 통과. `blockers`에는 blocked뿐 아니라 missing/waiting non-ready 항목도 포함되어야 한다. PostgreSQL runtime source로 상태를 읽는 흐름은 `npm run test:postgres-store`에서 함께 검증 |
| QA-ADMIN-35B | `npm run test:pilot-operator-support` 실행 | `/app/admin/settings` 현장 운영 관제 패널이 준비 확인, 고유 운영일 기준 14일 누락 점검, 다음 입력 추천일, 미해결 P0/P1, 모바일 30초 증빙 누락, 다음 액션을 표시하고 `P1 최종 배포 준비`의 담당 영역별 외부 증빙 입력 지도, `보류 중인 외부 준비`, 모바일 앱 배포 상태, `npm run test:ios-capacitor-connection`, `npm run ios:ipa:doctor`, [docs/IOS_IPA_PROVISIONING_RUNBOOK.md](docs/IOS_IPA_PROVISIONING_RUNBOOK.md) iOS IPA provisioning 런북과 `npm run test:ios-provisioning-runbook`, 외부 증빙 수집 확인, 증빙 형식 가드, `.data/android-twa-doctor.md` Android doctor Markdown support artifact, `.data/mobile-builds/ios/ios-ipa-doctor.md` iOS IPA doctor Markdown support artifact, `.data/postgres-docker-readiness.md` Postgres/Docker doctor support artifact, [docs/P1_OWNER_PROGRESS_REPORT.md](docs/P1_OWNER_PROGRESS_REPORT.md) 대표 진행 보고서와 `npm run test:owner-progress-report`, `externalBlockers`, Markdown GitHub Connector 섹션, Markdown Release Custody 섹션, release custody prerequisite 운영자 상태판 안내, `사용자 보류` 항목이 출시 판단은 보류로 유지된다는 안내, `npm run postgres:doctor -- --strict --out=.data/postgres-docker-readiness.json --markdown=.data/postgres-docker-readiness.md`, `npm run pilot:prelaunch-draft -- --out-dir=.data`, `npm run pilot:status -- --strict --out=.data/pilot-status.json` 안내와 release gate 문서가 유지되고 Simulator 성공과 IPA 배포 가능 상태를 구분하면 통과 |
| QA-ADMIN-35BDB | `npm run test:postgres-doctor` 실행 | Docker ready/daemon-down/CLI-missing fixture가 각각 ready/blocked 상태를 내고, non-strict blocked 상태는 JSON 산출물을 남기며 strict daemon-down은 nonzero로 실패한다. `--require-postgres-env` production fixture는 `FINAL_JUDO_DB_DRIVER=postgres`와 `FINAL_JUDO_POSTGRES_URL` 누락을 차단하고, Postgres URL password는 JSON/Markdown/stdout 어디에도 원문으로 남지 않으면 통과 |
| QA-ADMIN-35B0 | `npm run test:p1-handoff-draft` 실행 | `p1:handoff-draft`가 운영 배포, Android release, 결제 provider, 운영 푸시, 파일럿 최종 status 초안/blocked 리포트, Postgres/Docker doctor JSON/Markdown, Android doctor Markdown, JSON/CSV/Markdown action checklist, 6인 팀 action brief, 담당자별 Markdown brief, 담당자별 handoff package index/package, bundle manifest, dispatch receipt 초안/blocked 리포트, issue draft, GitHub CLI 등록 명령, GitHub connector payload, GitHub JSON/CSV 등록 결과 템플릿, issue registration plan, issue registration receipt 초안/blocked 리포트, P1 readiness 감사 리포트, P1 final evidence intake JSON/CSV/Markdown을 생성하고 GitHub repo를 인자/env/origin에서 고정하며 원문 webhook/VAPID/Postgres secret을 기록하지 않고 issue registration을 missing이 아닌 blocked로 보존하면 통과 |
| QA-ADMIN-35B0A | `npm run test:p1-handoff-checklist` 실행 | `p1:handoff-checklist`가 6개 handoff/IPA blocked 리포트를 담당자, 담당 영역, 증빙 초안, strict 명령, 다음 액션이 포함된 JSON/CSV/Markdown action checklist, 6인 팀 action brief, 담당자별 Markdown brief와 package index/package로 변환하고 각 owner package에 `team-agent-prompts.md`를 포함하며 원문 webhook/VAPID secret을 기록하지 않으면 통과 |
| QA-ADMIN-35B0B | `npm run test:p1-handoff-bundle` 실행 | `p1:handoff-bundle`이 담당자별 brief, package index, package 폴더와 `team-agent-prompts.md`를 SHA-256/byte size manifest로 검증하고, P1 release가 외부 handoff 때문에 blocked여도 bundle 구조는 ready가 될 수 있으며, owner package 안의 원문 secret-like 값 유입을 차단하면 통과 |
| QA-ADMIN-35B0C | `npm run test:p1-handoff-dispatch` 실행 | ready bundle에서 dispatch receipt JSON/Markdown/CSV 초안이 생성되고, placeholder 초안은 strict 검증에서 차단되며, 실제 담당자/채널/due date/acknowledgement 증빙이 채워진 6개 owner package receipt는 통과하고 원문 secret-like 값 유입은 차단하면 통과 |
| QA-ADMIN-35B0D | `npm run test:p1-handoff-dispatch-apply-csv` 실행 | draft에서 생성된 dispatch CSV는 TODO 상태로 차단되고, 운영자가 채운 dispatch CSV의 담당자/채널/due date/assignment evidence/acknowledgement evidence만 completed receipt JSON에 적용되어 strict verifier까지 통과하며, duplicate/unknown/missing row, read-only column 변조, pending acknowledgement, dueAt/acknowledgedAt 시간 역전, non-ISO timestamp, non-reference evidence, 원문 secret-like 값은 차단 |
| QA-ADMIN-35B0E | `npm run test:p1-handoff-issues` 실행 | dispatch receipt에서 담당자별 GitHub/Slack issue draft Markdown 6개, index, JSON manifest, GitHub CLI command file, connector payload, connector response template, JSON/CSV results template, registration plan이 생성되고 pending receipt는 blocked/publishReady=false와 CLI/connector 호출 금지, completed receipt는 ready/publishReady=true로 판정되며 GitHub repo 자동 추론, strict 명령/완료 기준 보존, 원문 secret-like 값 차단이 통과 |
| QA-ADMIN-35B0E1 | `npm run test:p1-github-connector-readiness` 실행 | `.data/p1-handoff-issue-drafts/github-connector-issue-payloads.json`의 repo가 로컬 `git remote origin`과 일치하고 connector tool name, issue draft publish 상태, issue payload별 repo/title/labels/body 계약이 맞으며 `github-connector-runbook.md`의 repo/publishReady/publishGuard/response target/body SHA-256 표가 payload와 일치하고 `publishReady=false`일 때 외부 이슈 생성 금지 `publishGuard`와 다음 액션을 유지하면 통과. `.data/p1-github-connector-access.json`이 있으면 GitHub connector `_get_repo` 접근 repo, default branch, pull/push/triage 권한, archived 여부까지 검증 |
| QA-ADMIN-35B0F | `npm run test:p1-handoff-issue-receipt` 실행 | ready issue draft manifest에서 등록 receipt 초안이 생성되고 GitHub connector `_create_issue` 응답의 row별 acknowledgement 변환과 HTTPS/ISO/시간순서 검증, GitHub/Slack 등록 results CSV/JSON 적용으로 외부 URL, 담당자, labels, body hash, acknowledgement 증빙을 채운 receipt는 ready, JSON/CSV missing·duplicate·unknown·read-only mismatch·pending·http·non-ISO timestamp·timing fixture, connector 응답 누락, template TODO, secret, tampered draft fixture는 blocked로 판정 |
| QA-ADMIN-35B0G | `npm run test:p1-evidence-intake-draft` 실행 | P1 readiness의 7개 최종 요구사항이 DevOps/총괄 PM, Android/Release, iOS/Release, Backend/Data, Frontend/QA, Product Lead, QA/Release lane별 JSON/CSV/Markdown 입력표로 생성되고 blocker code, 필수 증빙, strict 명령, TODO 입력 필드, 원문 secret-like 값 redaction fixture가 통과 |
| QA-ADMIN-35B0H | `npm run test:p1-evidence-intake-apply-csv` 실행 | 운영자가 채운 CSV의 owner/evidence URL/checkedAt/signoff만 completed JSON에 적용되고 strict verifier까지 통과하며, draft TODO, duplicate/unknown/missing row, read-only column 변조, 원문 secret-like 값, invalid evidence URL/checkedAt/signoff는 차단 |
| QA-ADMIN-35B0I | `npm run test:p1-evidence-intake` 실행 | ready P1 readiness 기반 intake에서 7개 lane의 owner/evidence URL/checkedAt/signoff가 채워진 fixture는 통과하고, draft TODO, blocked readiness, row 계약 변조, 원문 secret-like 값은 차단 |
| QA-ADMIN-35B0J | `npm run test:p1-handoff-draft`, `npm run test:p1-handoff-issues`, `npm run test:p1-github-connector-readiness` 실행 | 운영 배포, Android doctor/release, 결제 provider, 운영 푸시, GitHub issue draft/connector/results/registration receipt 초안의 operator-facing 다음 액션이 한국어로 출력되고, blocked `publishReady=false` 상태에서는 GitHub CLI/connector/외부 이슈 생성을 아직 하지 말라는 한국어 가드가 유지되면 통과 |
| QA-ADMIN-35B1 | `npm run test:p1-readiness` 실행 | 운영 배포, Android release, 결제 provider, 운영 푸시, P1 handoff issue registration receipt, 파일럿 최종 status 리포트를 하나로 묶어 ready/missing/blocked/unreadable/allow-pending/workspace 상태와 다음 액션을 검증하면 통과 |
| QA-ADMIN-35B1A | `npm run test:p1-operator-status` 실행 | P1 readiness, Android doctor Markdown, iOS IPA doctor JSON/Markdown, action checklist, bundle/dispatch, GitHub connector access receipt/payload, 대표 의사결정 등록표, issue registration/evidence intake report, release package/archive/storage receipt 상태와 담당 영역별 `externalBlockers`, external blockers CSV handoff template, `GitHub connector access ready`, Markdown GitHub Connector 섹션의 repo/checkedAt/default branch/권한/publish 상태, Deferred External Prep 섹션의 운영 웹앱 origin 확정, iOS 실제 iPhone/provisioning profile, `사용자 보류`, profile inventory 0, 출시 판단은 보류 유지 사유, Owner Decision Register 섹션의 `register_missing`/`needs_owner_input`/`decisions_recorded` 상태, Evidence Format Guardrails 섹션의 HTTPS/provider URI, ISO timestamp, 승인 시간순서, TODO placeholder 제거, 원문 secret 금지 규칙, Release Custody 섹션의 prerequisite/package/archive/storage receipt 상태와 다음 보관 액션을 운영자 상태판 JSON/Markdown/CSV로 요약한다. CSV에는 key, owner lane, required evidence, strict command, guardrail 요약과 `evidenceOwner`/`evidenceUrl`/`checkedAt`/`signoff` 입력 열이 있어야 한다. dispatch blocked 또는 issue draft `publishReady=false` 상태에서는 담당자 전달/acknowledgement CSV/receipt 완료와 issue draft 재생성을 먼저 안내하고 외부 이슈 등록을 권하지 않는다. publishReady 이후 이슈 등록 결과가 비어 있으면 GitHub connector `_create_issue`, CSV, JSON, 수동 workflow 다음 액션을 한국어 운영 안내로 표시한다. P1 readiness/evidence intake가 blocked이면 release package/archive/storage receipt 명령을 바로 노출하지 않고 prerequisite 안내를 표시하며, storage receipt blocked 다음 액션도 한국어로 표시하고 영어 fallback 문구, Android doctor Markdown 누락/형식 불일치, iOS IPA doctor blocked/누락, GitHub payload 누락/placeholder, publishable payload의 access receipt 누락, storage receipt placeholder, 원문 secret-like 값 fixture를 차단하면 통과 |
| QA-ADMIN-35B1A1 | `npm run test:p1-operator-status-apply-external-blockers-csv` 실행 | 운영자가 채운 7개 externalBlockers CSV row가 원본 operator status JSON의 고정 열과 정확히 일치할 때만 completed JSON/Markdown으로 적용되고 `externalBlockerEvidenceReady=true`, completed external blocker row 7개가 기록된다. 빈 입력 필드, 고정 열 변조, HTTP 증빙 URL, non-ISO timestamp, 원문 secret-like 값 fixture가 차단되면 통과 |
| QA-ADMIN-35B1B | `npm run test:p1-completion-evidence` 실행 | P1 완료 기준 10개가 P0 회귀, 회원/학부모 CSV 비노출과 회원/학부모 CSV export API 403 smoke 증거, 모바일 계정/PWA+iOS, Android, 대표 리포트, 결제/회원권, 공지/알림, 파일럿 운영, 권한/감사, 문서/빌드 증거 명령과 Android/iOS doctor support artifact, `externalBlockers`, Release Custody 상태에 매핑되고 pending fixture는 내부 완료 5개와 외부 증빙 대기 5개를 구분한다. Android TWA doctor/iOS IPA doctor가 blocked여도 역할별 APK와 iOS Capacitor/doctor Markdown 산출물이 있으면 운영 origin/release signing/실제 iPhone provisioning profile 대기인 외부 blocker로 집계하고, CSV에 `evidenceOwner`/`evidenceUrl`/`checkedAt`/`signoff` 입력 열이 있고 ready fixture는 10개 기준 모두 ready가 되고 secret-like operator status 입력은 차단되면 통과 |
| QA-ADMIN-35B1C | `npm run test:p1-completion-evidence-apply-csv` 실행 | 운영자가 채운 10개 완료 기준 CSV가 원본 JSON 매트릭스의 고정 열과 정확히 일치할 때만 completed JSON/Markdown으로 적용되고 `operatorEvidenceReady=true`, completed evidence row 10개가 기록된다. 빈 입력 필드, 고정 열 변조, HTTP 증빙 URL, non-ISO timestamp, 원문 secret-like 값 fixture가 차단되면 통과 |
| QA-ADMIN-35B2 | `npm run test:p1-release-package` 실행 | ready P1 readiness, P1 evidence intake report, 7개 필수 readiness 리포트의 SHA-256/byte size manifest가 생성되고 누락 handoff/intake, blocked readiness, path mismatch, stale readiness/intake, workspace fixture가 차단되면 통과 |
| QA-ADMIN-35B3 | `npm run test:p1-release-archive` 실행 | ready P1 release package와 9개 필수 리포트 원본 파일 SHA-256/byte size가 archive 직전에 재검증되고 fresh archive 폴더에 package/리포트/archive manifest가 복사되며 복사본 해시 검증, 변조 파일, blocked package, 기존 archive 폴더, placeholder archive 경로, 원문 secret-like 값 fixture가 차단되면 통과 |
| QA-ADMIN-35B4 | `npm run test:p1-release-storage-receipt` 실행 | P1 release archive manifest에서 장기 보관 receipt 초안이 생성되고 package+8개 리포트 업로드 항목, archive manifest hash/byte size, HTTPS/provider URI 보관 위치와 증빙, ISO uploadedAt, 최소 365일 보존 정책, 로컬 archive copy 해시가 검증되며 placeholder/누락/시간순서/변조/HTTP storage/non-reference evidence/원문 secret-like 값 fixture가 차단되면 통과 |
| QA-ADMIN-35B4A | `npm run test:team-agent-prompts` 실행 | [docs/TEAM_AGENT_PROMPTS.md](TEAM_AGENT_PROMPTS.md)가 11팀 60명 책임 체계, 11팀 현장 운영 인수 경계, 기존 P1 6개 handoff lane 호환성을 유지하고 Android APK artifact, iOS Simulator/IPA release 분리, Team ID/bundle id, 7개 외부 blocker, P1 검증 명령을 누락하지 않으면 통과 |
| QA-ADMIN-35B5 | `npm run test:implementation-backlog` 실행 | `docs/IMPLEMENTATION_BACKLOG.md`의 P0/P1 백로그 ID가 중복되지 않고 섹션 번호와 일치하며 그룹별 순번 누락, 빈 작업/완료 기준, 지원하지 않는 상태 값, `바로 다음 작업` 운영 순서 번호 중복/누락이 없으면 통과 |
| QA-ADMIN-35B6 | `npm run test:qa-plan` 실행 | `docs/QA_TEST_PLAN.md`의 QA 시나리오 ID가 중복되지 않고 각 실행/기대 결과가 비어 있지 않으며 문서에 적힌 `npm run` 스크립트가 `package.json`에 존재하면 통과 |
| QA-ADMIN-35B7 | `/app/admin/settings`에서 `P3 피드백 SLA 레인` 확인 | 개선 후보, 버그, 운영 차단, 외부 보류가 접수 1영업일, 버그 2영업일, 운영 차단 당일, 외부 보류 별도 보관으로 표시되고 담당자, 검증 명령, P1 외부 blocker와 분리 가드가 함께 보임 |
| QA-ADMIN-35B8 | `/app/admin/settings`에서 `P3 피드백 접수 보드` 확인 | 개선 후보, 버그, 운영 차단, 외부 보류가 출처, 다음 확인, 담당자, 검증 명령으로 표시되고 릴리즈 분리 상태와 P1 외부 blocker 분리 가드가 함께 보임 |
| QA-ADMIN-35B9 | `/app/owner/reports`에서 `P3 지점 주간 리스크 스코어카드` 확인 | 출석 마감 누락, 결제 위험, 회원 순감 신호가 주간 리스크 점수, 리뷰 트리거, 담당자, 다음 주간 리뷰로 표시되고 P1 release readiness와 분리됨 |
| QA-ADMIN-35B10 | `/app/dashboard`에서 `P3 회원/학부모 7일 유지 신호` 비노출 확인 | 회원/학부모에게 내부 운영 보드가 보이지 않고 다음 수업, 출석, 결제 상태, 상담/공지 중심 카드만 표시되며 내보내기 액션은 보이지 않음 |
| QA-ADMIN-35B11 | `/app/classes`에서 `P3 코치 수업 전환 리듬` 확인 | 코치에게 다음 수업 전환 전 출석 마감, 현장 메모, 보호자 안내, 저장 상태가 수업 전/후 1분 점검으로 표시되고 결제 금액 비노출 유지가 함께 보임 |
| QA-ADMIN-35B12 | `/app/admin/settings`에서 `P3 운영 기록 갱신` 확인 | 운영일 마감, 대표 주간 리뷰, 모바일 역할 가드, 릴리즈 오판 방지 명령이 내부 품질 증거로 표시되고 readiness blocked 7/7과 P1 blocked guard가 함께 보임 |
| QA-ADMIN-35B12A | `/app/admin/settings`에서 `P3 운영 자동화 실행` 확인 | 운영일 마감 자동 점검, 지점 주간 운영 리뷰, 모바일 역할 가드, 배포 readiness 재확인이 담당, 실행 주기, 증거, 확인 기준, 검증 명령으로 표시되고 P3 진행률만 갱신하며 iOS Simulator 성공을 IPA ready로 보지 않음 |
| QA-ADMIN-35B13 | `/app/owner/reports`에서 `P3 주간 운영 약속 보드` 확인 | 주간 리스크 점수가 이번 주 약속, 마감, 증빙 기준, 검증 명령으로 바뀌고 P1 release readiness와 분리됨 |
| QA-ADMIN-35B14 | `/app/classes`에서 `P3 코치 보호자 안내 마감 큐` 확인 | 보호자 안내 마감, 저장 마감, 운영 증거 확인이 수업 후 운영 증거와 검증 명령으로 묶이고 결제 금액 비노출 유지와 P1 release readiness 분리가 함께 보임 |
| QA-ADMIN-35B14A | `/app/classes`에서 `P3 코치 수업 후 24시간 후속 큐` 확인 | 후속 트리거, 후속 행동, 완료 증거, 재확인 시점이 24시간 후속 업무로 묶이고 코치 화면 금액 미노출과 P1 release readiness 분리가 함께 보임 |
| QA-ADMIN-35B15 | `/app/owner/reports`에서 `P3 지점 운영 실험 큐` 확인 | 운영 실험 가설, 성공 지표, 회고 시점, 중단 신호가 지점별 액션 추천과 연결되고 검증 명령 및 P1 release readiness 분리가 함께 보임 |
| QA-ADMIN-35B16 | `/app/dashboard`에서 `P3 회원/학부모 재방문 약속 큐` 확인 | 재방문 약속, 보호자/회원 안내, 다음 확인, 검증 명령이 공지/결제/상담/수업 준비와 연결되고 회원/학부모 내보내기 액션이 없으며 P1 release readiness와 분리됨 |
| QA-ADMIN-35B17 | `/app/admin/settings`에서 `P3 피드백 QA 반영 캘린더` 확인 | 현장 피드백이 재현 경로, QA 계획 반영, 백로그 반영, 다음 회고 순서로 표시되고 검증 명령 및 P1 release readiness 분리가 함께 보임 |
| QA-ADMIN-35B18 | `/app/dashboard`에서 `P3 회원/학부모 확인 마감 슬롯` 비노출 확인 | 내부 운영 마감 슬롯이 보이지 않고 공지, 결제 상태, 출석, 상담/공지, 다음 수업 준비를 사용자용 카드로만 확인하며 회원/학부모 내보내기 액션이 없음 |
| QA-ADMIN-35B19 | `/app/classes`에서 `P3 코치 현장 마감 우선순위 보드` 확인 | 출석 마감, 사유 메모, 주의/상담, 저장 마감이 현장 마감 우선순위, 바로 실행, 마감 기준, 운영 증거로 표시되고 코치 화면 금액 미노출 및 P1 release readiness 분리가 유지됨 |
| QA-ADMIN-35B20 | `/app/dashboard`에서 `P3 회원/학부모 확인 누락 방지 보드` 비노출 확인 | 내부 운영 누락 방지 보드가 보이지 않고 회원/학부모 내보내기 액션이 없으며 실제 상태 카드만 표시됨 |
| QA-ADMIN-35B21 | `/app/admin/settings`에서 `P3 피드백 회고 액션 보드` 확인 | 개선 후보 회고, 버그 회고, 운영 차단 회고, 외부 보류 회고가 담당자, 완료 기준, 재확인 시점, 검증 명령으로 표시되고 P1 release readiness와 분리됨 |
| QA-ADMIN-35B22 | `/app/owner/reports`에서 `P3 지점 조치 효과 리뷰 보드` 확인 | 지점별 운영 실험이 기준 신호, 기대 변화, 리뷰 지표, 다음 결정, 증빙 기준으로 표시되고 검증 명령 및 P1 release readiness 분리가 함께 보임 |
| QA-ADMIN-35B22A | `/app/owner/reports`에서 `P3 지점 액션 닫힘 점검 보드` 확인 | 지점 액션 실행 이후 닫힘 신호, 점검 증거, 닫힘 창, 다음 점검이 표시되고 P3 진행률만 갱신하며 P1 release readiness와 분리됨 |
| QA-ADMIN-35B22B | `/app/owner/reports`에서 `P3 지점 액션 표준화 보드` 확인 | 닫힌 지점 액션이 표준화 액션, 적용 조건, 확산 대상, 표준화 증거, 다음 표준 리뷰로 표시되고 P3 진행률만 갱신하며 P1 release readiness와 분리됨 |
| QA-ADMIN-35B22C | `/app/owner/reports`에서 `P3 지점 표준 확산 추적 보드` 확인 | 표준화된 지점 액션이 확산 상태, 적용 지점, 교육/공유 패킷, 적용 신호, 다음 확산 리뷰로 표시되고 P3 진행률만 갱신하며 P1 release readiness와 분리됨 |
| QA-ADMIN-35B22D | `/app/owner/reports`에서 `P3 지점 표준 품질 점검 보드` 확인 | 확산된 표준 운영이 품질 점검, 보정 액션, 점검 창, 품질 증거, 다음 품질 리뷰로 표시되고 P3 진행률만 갱신하며 P1 release readiness와 분리됨 |
| QA-ADMIN-35B22E | `/app/owner/reports`에서 `P3 지점 표준 재교육 큐` 확인 | 품질 점검에서 흔들린 표준 운영이 재교육 트리거, 재교육 담당, 재교육 패킷, 재교육 마감, 수용 신호, 다음 재교육 리뷰로 표시되고 P3 진행률만 갱신하며 P1 release readiness와 분리됨 |
| QA-ADMIN-35B22F | `/app/owner/reports`에서 `P3 지점 표준 재교육 효과 검증 보드` 확인 | 재교육 이후 표준 운영이 효과 신호, 측정 창, 효과 증거, 정착 액션, 재발 방지, 다음 효과 리뷰로 표시되고 P3 진행률만 갱신하며 P1 release readiness와 분리됨 |
| QA-ADMIN-35B22G | `/app/owner/reports`에서 `P3 지점 표준 운영 정착 보드` 확인 | 재교육 효과가 확인된 표준 운영이 정착 기준, 운영 주기, 정착 증거, 담당 고정, 재발 감시, 다음 정착 리뷰로 표시되고 P3 진행률만 갱신하며 P1 release readiness와 분리됨 |
| QA-ADMIN-35B22H | `/app/owner/reports`에서 `P3 지점 표준 성과 리포트 보드` 확인 | 정착된 표준 운영이 성과 지표, 보고 주기, 성과 증거, 대표 판단, 공유 대상, 다음 성과 리뷰로 표시되고 P3 진행률만 갱신하며 P1 release readiness와 분리됨 |
| QA-ADMIN-35B22I | `/app/owner/reports`에서 `P3 지점 표준 성과 액션 추적 보드` 확인 | 성과 리포트의 대표 판단이 후속 액션, 액션 담당, 액션 마감, 액션 증거, 에스컬레이션, 다음 액션 리뷰로 표시되고 P3 진행률만 갱신하며 P1 release readiness와 분리됨 |
| QA-ADMIN-35B23 | `/app/classes`에서 `P3 코치 3분 마감 루틴 보드` 확인 | 3분 마감이 1분 출석, 1분 사유, 30초 보호자 안내, 30초 저장로 나뉘고 마감 순서, 현장 실행, 운영 증거, 검증 명령, 결제 금액 비노출 유지, P1 release readiness 분리가 함께 보임 |
| QA-ADMIN-35B24 | `/app/dashboard`에서 회원/학부모 복귀 안내 레일 비노출 확인 | 오늘 복귀 안내 레일과 운영 검증 문구가 회원/학부모 화면에 표시되지 않음 |
| QA-ADMIN-35B25 | `/app/dashboard`에서 회원/학부모 수업 전 준비 보드 비노출 확인 | 수업 전 준비 보드는 표시되지 않고 회원 핵심 상태 패널과 학부모 학습 리포트만 표시됨 |
| QA-ADMIN-35B26 | `/app/admin/settings`에서 `P3 피드백 증거 패킷 보드` 확인 | 피드백 증거 패킷이 개선 후보, 버그, 운영 차단, 외부 보류로 분류되고 증거 묶음, 재현/영향, QA/백로그 반영, 검증 명령이 함께 표시되며 P1 외부 blocker와 섞이지 않고 P1 release readiness와 분리됨 |
| QA-ADMIN-35B27 | `/app/owner/reports`에서 `P3 운영 결정 추적 보드` 확인 | 장기 추세, 위험 회원, 재등록 후보, 결제 회수 우선순위, 지점 비교 결과가 결정 기록, 운영 신호, 운영 증거, 다음 재확인, 검증 명령으로 묶이고 P1 release readiness와 분리됨 |
| QA-ADMIN-35B27A | `/app/owner/reports`에서 `P3 이번 주 운영 결정 큐` 확인 | 장기 추세, 결제 회수, 재등록 상담, 지점 액션이 이번 주 결정, 결정 트리거, 결정 마감, 운영 증거, 검증 명령으로 묶이고 P1 release readiness와 분리됨 |
| QA-ADMIN-35B27AA | `/app/owner/reports`에서 `P3 다음 주 지점 운영 준비 보드` 확인 | 이번 주 결정 큐가 준비 항목, 다음 주 신호, 담당자, 준비 마감, 운영 증거, 검증 명령으로 바뀌고 회원/학부모 내보내기 액션 없음, 대표/총괄 CSV 권한, P1 release readiness 분리가 유지됨 |
| QA-ADMIN-35B27B | `/app/admin/settings`에서 `P3 피드백 운영 반영` 확인 | 개선 후보, 버그, 운영 차단, 외부 보류가 반영 액션, 반영 위치, 다음 회고, 검증 명령으로 묶이고 외부 보류는 P1 readiness blocked 유지로 분리됨 |
| QA-ADMIN-35B27C | `/app/admin/settings`에서 `P3 피드백 실행 검증 보드` 확인 | 개선 후보, 버그, 운영 차단, 외부 보류가 실행 상태, 확인 증거, 재검증 시점, 검증 명령으로 묶이고 외부 보류는 P1 readiness blocked 증거로만 보관됨 |
| QA-ADMIN-35B27D | `/app/admin/settings`에서 `P3 피드백 재발 방지 보드` 확인 | 개선 후보, 버그, 운영 차단, 외부 보류가 재발 신호, 방지 확인, 다음 점검, QA 명령으로 묶이고 외부 보류는 P1 readiness blocked 유지 사유로만 보관됨 |
| QA-ADMIN-35B28 | `/app/classes`에서 `P3 코치 예외 처리 라우팅 보드` 확인 | 모바일 출석, 사유 메모, 상담/주의, 저장 예외가 현장 신호별로 즉시 처리, 보호자 안내, 대표 확인, 다음 수업 전 재확인으로 분리되고 검증 명령, 결제 금액 비노출 유지, P1 release readiness 분리가 함께 보임 |
| QA-ADMIN-35B28A | `/app/classes`에서 `P3 코치 현장 기록 압축 보드` 확인 | 사유/상담, 보호자 안내, 저장 기록가 기록 압축, 현장 기록, 기록 마감, 다음 재확인, 검증 명령으로 묶이고 코치 화면 금액 미노출 및 P1 release readiness 분리가 유지됨 |
| QA-ADMIN-35B28B | `/app/classes`에서 `P3 코치 수업 전 5분 준비 보드` 확인 | 명단 위험, 사유 누락, 상담/보호자 안내, 저장 상태가 5분 준비, 현장 실행, 다음 재확인, 운영 증거, 검증 명령으로 묶이고 코치 화면 금액 미노출 및 P1 release readiness 분리가 유지됨 |
| QA-ADMIN-35B29 | `/app/dashboard`에서 회원/학부모 오늘 확인 브리프 비노출 확인 | 오늘 확인 브리프, 10분 점검, 확인 마감, 확인 증거 같은 운영 안내가 회원/학부모 화면에 표시되지 않음 |
| QA-ADMIN-35B30 | `/app/dashboard`에서 회원/학부모 주간 확인 리듬 비노출 확인 | 주간 확인 리듬과 운영 증거 문구가 회원/학부모 화면에 표시되지 않음 |
| QA-ADMIN-35B30A | `/app/dashboard`에서 회원/학부모 3분 복귀 체크 보드 비노출 확인 | 3분 복귀 체크 보드와 다음 행동/확인 증거 문구가 회원/학부모 화면에 표시되지 않음 |
| QA-ADMIN-35B31 | `/app/admin/settings`에서 `P3 운영 검증 매트릭스` 확인 | 운영 데이터, 코치 현장, 회원/학부모, 파일럿 피드백, 관리자 상태판이 영역별 검증 상태, 증거 화면, 다음 재확인, 검증 명령으로 묶이고 P1 외부 blocker와 분리됨 |
| QA-ADMIN-35B32 | `/app/admin/settings`에서 `P3 주간 운영 리뷰 패킷` 확인 | 대표 운영 리뷰, 코치 현장 회고, 회원/학부모 유지 리뷰, 파일럿 피드백 회고, 릴리즈 오판 방지 리뷰가 회의 증거 묶음, 결정 산출물, 후속 확인, 검증 명령으로 묶이고 P1 release blocked 상태와 분리됨 |
| QA-ADMIN-35B33 | `/app/admin/settings`에서 `P3 주간 리뷰 액션 추적 보드` 확인 | 주간 리뷰 액션이 액션 담당, 마감 창, 업데이트 증거, 검증 명령으로 닫히고 P1 release blocked 분리 상태가 유지됨 |
| QA-ADMIN-35B34 | `/app/admin/settings`에서 `P3 주간 리뷰 효과 측정 보드` 확인 | 닫은 주간 리뷰 액션이 효과 신호, 측정 창, 업데이트 증거, 다음 판단, 검증 명령으로 재확인되고 P1 release blocked 유지가 분리됨 |
| QA-ADMIN-35B35 | `/app/admin/settings`에서 `P3 피드백 운영 지표 보드` 확인 | 개선 후보, 버그, 운영 차단, 외부 보류가 분류별 운영 지표, 노화 창, 백로그 신호, 운영 판단, 검증 명령으로 묶이고 외부 보류는 P1 release blocked 유지 사유로만 보관됨 |
| QA-ADMIN-35C | `npm run test:owner-report-trends` 실행 | `/app/owner/reports` 기간별 운영 추세가 수업일, 출석 확인, 결제 기한, 회원 생성일/퇴관일/상태 변경일을 지점 스코프별로 집계하고 대표 오늘 우선순위가 출석 미처리/결제 위험/휴면 회원 우선 항목을 표시하며 운영 CSV `trend_period`, `new_members`, `withdrawn_members`, `net_member_change` 행/컬럼과 문서/release gate가 유지되면 통과 |
| QA-ADMIN-35C1 | `npm run test:owner-progress-report` 실행 | [docs/P1_OWNER_PROGRESS_REPORT.md](docs/P1_OWNER_PROGRESS_REPORT.md)가 비개발자 대표에게 공유할 현재 단계, MVP 기능 개발 완료, 파일럿 운영 및 앱 배포 전 최종 준비 단계, 완료 항목, 남은 외부 준비, 대표 결정 필요 항목, 과장 금지 문구를 유지하고 원문 secret-like 값을 기록하지 않으면 통과 |
| QA-ADMIN-35C2 | `npm run test:owner-progress-report-draft` 실행 | `owner:progress-report:draft -- --workspace=.data --allow-pending --out=.data/p1-owner-progress-report.md --json=.data/p1-owner-progress-report.json`가 P1 operator status와 completion evidence의 외부 blocker/완료 기준, 대표 의사결정 등록표 상태를 비개발자 대표용 Markdown/JSON 초안으로 번역하고, pending에서는 개발 미완성이 아니라 외부 준비 대기와 대표 결정 입력 대기를 표시하며, ready fixture는 최종 배포 판단 문안과 결정 기록 완료 상태, secret-like fixture는 차단, README/릴리즈 체크리스트/Admin settings 참조가 유지되면 통과 |
| QA-ADMIN-35C3 | `npm run test:owner-decision-register` 실행 | `owner:decision-register -- --workspace=.data --out=.data/p1-owner-decision-register.json --markdown=.data/p1-owner-decision-register.md --csv=.data/p1-owner-decision-register.csv --guide=.data/p1-owner-decision-register.guide.md`가 P1 operator status와 대표 초안에서 7개 외부 blocker를 대표 결정 문항, 담당 lane, 필수 증빙, 검증 명령, `decisionOwner`/`dueDate`/`evidenceOwner`/`evidenceUrl`/`checkedAt`/`signoff` 입력 열과 CSV 작성 안내 guide로 변환하고, ready fixture는 남은 decision row 없음, missing/secret-like fixture는 차단, README/릴리즈 체크리스트/Admin settings 참조가 유지되면 통과 |
| QA-ADMIN-35C4 | `npm run test:owner-decision-register-apply-csv` 실행 | `owner:decision-register:apply-csv -- --workspace=.data --csv=.data/p1-owner-decision-register.csv --json=.data/p1-owner-decision-register.json --out=.data/p1-owner-decision-register.completed.json --markdown=.data/p1-owner-decision-register.completed.md`가 대표가 채운 7개 결정표 CSV를 원본 JSON의 고정 열과 대조한 뒤 별도 completed JSON/Markdown으로 적용하고, 빈 담당자/기한/증빙 책임자, 고정 열 변조, 잘못된 dueDate, HTTP 증빙 URL, non-ISO checkedAt, 원문 secret-like 값 fixture를 차단하면 통과 |
| QA-ADMIN-35C5 | `npm run test:owner-briefing-package` 실행 | `owner:briefing-package -- --workspace=.data --out=.data/p1-owner-briefing-package.json --markdown=.data/p1-owner-briefing-package.md --package-dir=.data/p1-owner-briefing-package`가 정적 대표 보고서, [docs/TEAM_AGENT_PROMPTS.md](TEAM_AGENT_PROMPTS.md) P1 6인 팀 목표 프롬프트, 현재 대표 보고 초안, 대표 의사결정 등록표와 `p1-owner-decision-register.guide.md`, P1 운영자 상태판, 완료 기준 매트릭스, 완료 기준 CSV, 외부 blocker CSV, 패키지 README, 한국어 대표 요약 Markdown을 SHA-256/byte size manifest와 공유 폴더로 묶고, `.data/p1-owner-decision-register.completed.*`가 있으면 optional 산출물로 포함하며, P1 source가 blocked여도 안전 공유 가능 상태로 유지하되 누락 산출물/JSON 파싱 실패/원문 secret-like fixture는 차단하면 통과 |
| QA-ADMIN-35D | `npm run test:payment-lifecycle` 실행 | 결제 생성의 처리자·지점 범위 `Idempotency-Key` 동일 재시도는 기존 한 건을 반환하고 다른 payload·삭제 원본 재시도는 `409`로 차단되며, 결제 생성/환불/취소 상태 이력이 `statusHistory`, PostgreSQL `payment_status_events`, `/app/payments` 상태 변경 이력과 확인할 결제, 결제 CSV lifecycle 컬럼, 결제 등록/환불 API 인증·지점 권한 선확인, 문서/release gate에 유지되면 통과 |
| QA-ADMIN-35E | `npm run test:online-payments` 실행 | provider-neutral 온라인 결제 요청, webhook secret, 영수증 메타, 결제 CSV provider 컬럼, 코치 금액/결제 링크 마스킹, production provider/checkout/webhook secret 누락 차단, API/DB 문서/release gate가 유지되면 통과 |
| QA-ADMIN-35E0 | `npm run test:family-payment-checkout` 실행 | 회원/학부모 결제 카드가 `/app/payments/checkout` 내부 결제 준비 화면으로 이동하고, 성인 회원 본인 결제와 학부모 자녀 결제만 허용되며 유소년/청소년 회원 직접 결제는 차단된다. 실 PG/API 연결 전 상태와 모바일 하단 내비 안전 영역 증빙이 유지되면 통과 |
| QA-ADMIN-35E0A | `npm run test:payment-checkout-method-flow` 실행 | 성인 회원과 학부모 자녀 결제 상세에서 compact 납부 요약이 245px 이하, 요약 grid가 150px 이하, 결제자 정보 시작 위치가 480px 이하이고, 결제자 정보 기본 높이가 280px 이하이며 결제수단 시작 위치가 730px 이하이다. 주소·일반전화·이메일 추가 영역은 기본 접힘이고 44px 토글로 펼쳤을 때 이메일 placeholder, 주소검색, 일반전화 입력이 동작한다. 결제수단 라디오, 무통장입금 입력, 카드사 그리드, 카드 안내 버튼 44px 터치 목표, 우리WON페이 모달, 저장/1회성 확인 피드백이 `role=status`/`aria-live=polite`로 390px 모바일에서 동작하고 `납부 정보 확인` 버튼/`납부 정보 접수` 안내가 하단 고정 내비게이션과 24px 이상 떨어지며 실제 PG/API 호출 없이 overflow/콘솔 오류가 없으면 통과 |
| QA-ADMIN-35E0A1 | `npm run test:payment-create-touch-targets` 실행 | 390px 모바일 대표 결제 화면에서 결제 내보내기, 상태 필터, 표시 건수, 결제/회원권 요약 metric, 결제 행의 재등록/온라인 요청/정기결제 약정/전액 입력/환불/취소 액션이 모두 44px 이상이다. 수기 결제 등록 폼은 기본 접힘으로 시작하고, `등록 열기` 후 회원 검색 입력/검색 결과/회원권명/상태/금액/할인/납부일/만료일/등록 버튼도 모두 44px 이상이다. `최민재` 검색 결과를 선택하면 결과 목록은 접히고 선택 회원 카드가 표시되며 등록 버튼이 활성화된다. 저장 요청 중 버튼이 잠기고, 서버 저장 뒤 첫 응답을 `504`로 유실시켜도 입력 폼을 유지한 채 같은 `Idempotency-Key`로 재시도해 성공 안내와 동일 회원권 1건만 목록에 반영되며 overflow 0, 콘솔 오류 없음, 증빙 스크린샷이 유지되면 통과 |
| QA-ADMIN-35E0B | `npm run test:operator-list-search` 실행 | 390px 모바일 운영자 결제 목록에서 `q` 검색 딥링크가 회원/회원권/연락처 기준으로 목록을 좁히고 검색어 지우기로 URL을 복구하며, 0건 검색에서는 수기 등록/운영 요약 카드 대신 검색 빈 상태를 바로 보여준다. 공지함도 `q` 딥링크로 제목/내용 검색어를 복원하고 표시 건수와 목록을 좁히며 0건 빈 상태와 검색어 지우기 URL 복구를 제공한다. 공지 0건 상태에서는 대상 없는 읽음 처리 액션을 숨기고 `검색어 지우기`가 44px 이상이며 하단 내비와 24px 이상 간격을 유지하고 overflow/콘솔 오류 없이 증빙 스크린샷을 남기면 통과 |
| QA-ADMIN-35E0B1 | `npm run test:global-search` 실행 | 대표/총괄 상단 통합 검색이 메뉴·회원·결제·공지·사용자 결과를 권한 범위 안에서 반환하고 회원/결제/공지/사용자 결과가 실제 목록 `q` 딥링크로 연결된다. 코치는 결제 금액과 사용자 관리 결과를 받지 않고, 빈 검색어와 권한 외 결과를 노출하지 않으면 통과 |
| QA-ADMIN-35E0C | `npm run test:admin-role-search` 실행 | 390px 모바일 총괄 권한 관리에서 `/app/admin/roles?q=...` 딥링크가 사용자/역할/관리 항목 기준으로 목록을 좁히고 검색어 지우기로 URL을 복구한다. 0건 검색에서는 stale 사용자 행 없이 빈 상태와 `전체 보기` 44px 액션을 보여주고 검색 중 권한 매트릭스/최근 변경 부가 패널은 모바일에서 숨긴다. 하단 safe-area spacer, 하단 내비와 24px 이상 간격, overflow 0, 콘솔 오류 없음, 증빙 스크린샷을 유지하면 통과 |
| QA-ADMIN-35E0C1 | `npm run test:admin-user-search` 실행 | 390px 모바일 총괄 사용자 관리에서 `/app/admin/users?role=guardian&q=...` 딥링크가 역할 필터와 검색어를 함께 복원하고 사용자/휴대폰/역할/연결 회원 기준으로 목록을 좁힌다. 검색어 지우기는 역할 필터를 유지하고, 0건 검색에서는 stale 사용자 행 없이 빈 상태와 `전체 보기` 44px 액션을 보여주며 q/role을 모두 제거한다. 검색 입력/인라인 지우기/역할 필터 초기화/빈 상태 초기화가 44px 이상이고 하단 내비와 24px 이상 간격, overflow 0, 콘솔 오류 없음, 증빙 스크린샷을 유지하면 통과 |
| QA-ADMIN-35E0C1A | `npm run test:admin-user-management-touch-targets` 실행 | 390px 모바일 총괄 사용자 관리에서 초대 폼은 기본 접힘으로 시작하고, 목록 수정/삭제/재발급 액션과 초대/수정/삭제/비밀번호 재발급 입력 및 저장 버튼이 모두 44px 이상이다. 수정 action bar는 하단 내비와 24px 이상 간격을 유지하고, overflow 0, 콘솔 오류 없음, iOS Simulator 앱 chrome 없는 사용자 관리 화면 증빙이 유지되면 통과 |
| QA-ADMIN-35E0C2 | `npm run test:admin-audit-search` 실행 | 390px 모바일 총괄 변경 기록에서 `/app/admin/audit-logs?q=...&detail=...` 딥링크가 적용 조건 chip, 목록, 선택 기록 상세를 복원하고 상세 열기/닫기·검색어 지우기·`전체 보기`로 URL을 갱신한다. 검색 입력은 120자로 제한하며 0건 검색에서는 stale 변경 기록 행 없이 빈 상태와 44px 초기화 액션을 보여주고 하단 내비와 24px 이상 간격, overflow 0, 콘솔 오류 없음, 증빙 스크린샷을 유지한다. 변경 기록·권한 화면이 공통 처리/결과 라벨을 사용하고 승급 심사와 대회 공지 등록·수정·삭제 필터가 API에서 400으로 깨지지 않으며, 검색어 121자·사유 501자·지점 ID 201자 요청은 `audit_logs.read`를 남기지 않고 400으로 차단된다. 동일 사용자·필터·사유 조회 2회는 5초 안에 한 기록으로 저장된다. 날짜만 지정한 한국 시간 종료일은 그날 23:59:59.999까지 포함하고 시작일이 종료일보다 늦은 조합은 화면에서 정리되며 API에서 400으로 거부되면 통과 |
| QA-ADMIN-35E0C3 | `npm run test:audit-action-contract` 실행 | 앱 `AuditAction`의 모든 값이 공통 표시 라벨, 정규화 PostgreSQL `audit_action` 초기/추가 마이그레이션, DB 스키마 문서에 존재하고 API route의 변경 기록 액션이 모두 앱 타입에 선언되어 있으면 통과 |
| QA-ADMIN-35E0C4 | `npm run test:audit-log-privacy` 실행 | 변경 기록의 휴대폰·이메일·계정 식별값이 마스킹되고 비밀번호·토큰·알림 endpoint와 민감 메모 원문이 저장·응답·화면에 남지 않는다. 총괄 변경 기록 상세가 원문 JSON 대신 한국어 필드별 변경 전후 비교를 제공하고 서버 저장소가 모든 기록에 같은 정책을 적용하며 iOS 앱에서 `detail` 딥링크 상세 증빙이 유지되면 통과 |
| QA-ADMIN-35E0D | `npm run test:visible-app-copy-stability` 실행 | 390px 모바일 코치/회원/학부모 회원 화면에서 비상 연락처 전화 링크가 아이콘이 있는 44px 이상 터치 목표로 렌더링되고, 보호자 현재 연결 연락처도 `test:member-profile-guardian-edit` 정적 가드로 텍스트 링크 회귀 없이 44px 전화 액션을 유지하면 통과 |
| QA-OWNER-08C | `npm run test:visible-app-copy-stability` 실행 | 390px 모바일 대표 대시보드에서 지점 비교 카드와 지점 행이 하단 내비게이션에 겹치지 않고 24px 이상 여백을 유지하면 통과 |
| QA-ADMIN-35E1 | `npm run test:payment-provider-handoff-draft` 실행 | PG/VAN handoff 초안 생성기가 env/인자에서 provider, checkout, webhook mapping을 채우고 원문 webhook secret을 기록하지 않으며 pending 초안 strict 차단, 운영자 완료 초안 strict 통과, 누락 field mapping 차단 fixture를 검증하면 통과 |
| QA-ADMIN-35F | `npm run test:env-readiness` 실행 | `.env.example`, `.env.production.example`, `docs/ENVIRONMENT_MATRIX.md`가 개발/운영 저장소, 위험 플래그, PostgreSQL, 결제, 푸시 필수 변수와 안전한 placeholder를 유지하고 release/admin/docs gate와 정합하면 통과 |
| QA-ADMIN-35G | `npm run test:deployment-handoff-draft` 실행 | 운영 배포 handoff 초안 생성기가 운영 env/preflight 값에서 secret 이름과 보관 증빙만 기록하고 PostgreSQL URL, webhook secret, VAPID private key 원문을 JSON에 쓰지 않으며 증빙이 채워진 초안이 strict handoff를 통과하면 완료 |
| QA-ADMIN-35H | `npm run test:deployment-handoff` 실행 | 운영 배포 handoff 검증기가 secret store 설정, production origin, PostgreSQL runtime store, 결제 provider, VAPID push, production preflight, release gate, HTTPS/provider URI 증빙, 템플릿의 `*_EVIDENCE_URI` placeholder, ISO 생성/승인 시각, 생성 이후 승인 순서, signoff를 통과시키고 원문 secret 값/누락 env/blocked preflight/`localhost`·`.example`·TODO origin 또는 mailto subject/non-reference evidence/non-ISO timestamp/시간 역전을 차단하면 통과 |
| QA-ADMIN-36 | `npm run test:admin-settings-gates` 실행 | `/app/admin/settings` 자동 검증 명령과 `test:release` 실행 목록이 정합하면 통과 |
| QA-ADMIN-36A | `npm run test:pilot-prelaunch-draft` 실행 | `pilot:prelaunch-draft -- --out-dir=<tmp>`가 준비 증빙 CSV, 계정별 비밀번호 교체 CSV, audit launch package, pre-pilot evidence/preflight/status, blocked next action summary를 생성하고, ready 판정을 위조하지 않으면 통과 |
| QA-ADMIN-37 | `npm run test:pilot-launch-package` 실행 | `npm run test:pilot`, `npm run test:pilot-import`, `npm run test:pilot-prelaunch-draft`, `npm run pilot:prelaunch-draft -- --out-dir=.data`, `npm run pilot:launch-package -- --allow-incomplete --out=.data/pilot-launch-package.json`, `npm run pilot:evidence -- --out=.data/pilot-evidence.json`, pre-pilot Markdown 리포트, 준비 증빙 CSV와 계정별 비밀번호 교체 CSV 검증, 현장 증빙 manifest 템플릿, README/런북/릴리즈 체크리스트/QA 문서의 필수 명령 안내가 모두 검증되면 통과 |
| QA-ADMIN-37A | `npm run test:pilot-launch-command` 실행 | `pilot:launch-package`가 audit 모드에서 blocked launch package/evidence/preflight/readiness evidence/password rotation 산출물 요약을 쓰고, strict 모드에서 차단 조건이 있으면 nonzero로 종료하면서 패키지 JSON을 보존하며, archived commands에 PostgreSQL DB URL 비밀번호가 남지 않으면 통과 |
| QA-ADMIN-37B | `npm run test:pilot-readiness-evidence` 실행 | 공유 readiness 계약에서 준비 증빙 CSV 초안이 생성되고, pre-pilot 초안은 retro 항목을 제외하며, verified/blocked 항목의 placeholder 증빙, unknown ID, 필수 ID 누락이 차단되면 통과. PostgreSQL runtime state의 기존 readiness 값을 초안에 반영하는 흐름은 `npm run test:postgres-store`에서 함께 검증 |
| QA-ADMIN-37C | `npm run test:pilot-readiness-evidence-apply` 실행 | 검증된 준비 증빙 CSV가 dry-run에서는 런타임을 바꾸지 않고, write 모드에서는 pre-pilot 준비 상태와 `pilot_readiness.update` 변경 기록을 반영하며, retro 항목 유지, idempotent 재실행, pending 템플릿/non-admin actor/잘못된 checkedAt 차단이 통과 |
| QA-ADMIN-37D | `npm run test:pilot-password-rotation` 실행 | 계정별 비밀번호 교체 CSV 초안이 런타임 이메일 계정을 모두 포함하고, 기본 임시 비밀번호 잔존, 계정 누락, 변경 기록 누락, 원문 임시 비밀번호 노출이 차단되며 변경 기록이 있는 교체 완료 fixture가 통과 |
| QA-ADMIN-38 | 실제 파일럿 데이터 반영 후 `npm run pilot:launch-package -- --csv=docs/pilot-templates/pilot-data-intake.csv --readiness-evidence=.data/pilot-readiness-evidence.csv --password-rotation=.data/pilot-password-rotation.csv --out=.data/pilot-launch-package.json` 실행 | CSV 검증, import dry-run, 준비 증빙 CSV, 계정별 비밀번호 교체 증빙, pre-pilot evidence JSON/Markdown, production preflight JSON, launch package JSON이 생성되고 기본 임시 비밀번호/준비 증빙/production 플래그/미해결 P0 이슈/참조 무결성 차단 조건 없이 통과. PostgreSQL 운영 DB에서는 `--driver=postgres --state-key=<key> --postgres-url=<url>`을 함께 사용하며 패키지에 DB URL 비밀번호가 `REDACTED`로만 기록되어야 함 |
| QA-ADMIN-38B | 파일럿 준비 전 `npm run pilot:readiness-evidence:draft -- --phase=pre-pilot --out=.data/pilot-readiness-evidence.csv` 실행 후 owner/status/evidence를 채우고 `npm run pilot:readiness-evidence -- --file=.data/pilot-readiness-evidence.csv --phase=pre-pilot`, `npm run pilot:readiness-evidence:apply -- --file=.data/pilot-readiness-evidence.csv --phase=pre-pilot --actor-user-id=user-admin` 실행 | 준비 항목 ID/category/label이 공유 계약과 일치하고 verified/blocked 항목은 최종 증빙을 포함하며, 검증된 내용이 `/app/admin/settings`와 같은 런타임 준비 상태 및 `pilot_readiness.update` 변경 기록에 반영되면 통과 |
| QA-ADMIN-38C | 파일럿 준비 전 `/app/admin/users`에서 계정별 임시 비밀번호를 발급한 뒤 `npm run pilot:password-rotation:draft -- --runtime=.data/final-judo-db.json --out=.data/pilot-password-rotation.csv`, `npm run pilot:password-rotation -- --runtime=.data/final-judo-db.json --file=.data/pilot-password-rotation.csv` 실행 | 모든 이메일 계정의 기본 임시 비밀번호가 교체되어 있고, `auth.password_reset.complete` 또는 `auth.invite.accept` 변경 기록와 전달 채널 증빙이 있으며, CSV에 원문 임시 비밀번호가 없으면 통과 |
| QA-ADMIN-38A | 실제 파일럿 데이터 반영 후 `NODE_ENV=production npm run preflight:pilot -- --out=.data/pilot-preflight.pre-pilot.json` 실행 | 기본 임시 비밀번호/준비 증빙/production 플래그/미해결 P0 이슈/참조 무결성 차단 조건 없이 통과하고 JSON 아티팩트가 보관됨 |
| QA-ADMIN-39 | 파일럿 종료 후 `NODE_ENV=production npm run preflight:pilot -- --require-retro --out=.data/pilot-preflight.post-pilot.json` 실행 | 14개 운영일, 출석 기록 수치, 모바일 출석 30초 계측 증빙, 결제 상태 확인 수치, 회고 증빙이 모두 통과하고 JSON 아티팩트가 보관됨 |
| QA-ADMIN-40 | 파일럿 종료 후 `npm run pilot:evidence -- --require-retro --out=.data/pilot-evidence.post-pilot.json`, `npm run pilot:evidence -- --require-retro --format=markdown --out=.data/pilot-evidence.md`, `npm run pilot:field-evidence:draft -- --source-csv=docs/pilot-templates/pilot-data-intake.csv --pre-pilot-evidence=.data/pilot-evidence.json --post-pilot-evidence=.data/pilot-evidence.post-pilot.json --post-pilot-markdown=.data/pilot-evidence.md --out=.data/pilot-field-evidence.json`, `npm run pilot:field-evidence -- --file=.data/pilot-field-evidence.json --report=.data/pilot-evidence.post-pilot.json`, `npm run pilot:closeout-package -- --field=.data/pilot-field-evidence.json --preflight=.data/pilot-preflight.post-pilot.json --report=.data/pilot-evidence.post-pilot.json --markdown=.data/pilot-evidence.md --out=.data/pilot-closeout-package.json`, `npm run pilot:artifact-manifest -- --preflight-pre=.data/pilot-preflight.pre-pilot.json --evidence-pre=.data/pilot-evidence.json --evidence-pre-markdown=.data/pilot-evidence.pre-pilot.md --readiness-evidence=.data/pilot-readiness-evidence.csv --launch-package=.data/pilot-launch-package.json --password-rotation=.data/pilot-password-rotation.csv --preflight-post=.data/pilot-preflight.post-pilot.json --evidence-post=.data/pilot-evidence.post-pilot.json --evidence-markdown=.data/pilot-evidence.md --field=.data/pilot-field-evidence.json --closeout=.data/pilot-closeout-package.json --out=.data/pilot-artifact-manifest.json`, `npm run pilot:archive-artifacts -- --manifest=.data/pilot-artifact-manifest.json --archive-dir=.data/pilot-archives/final-judo-pilot-20260715`, `npm run pilot:storage-receipt:draft -- --archive=.data/pilot-archives/final-judo-pilot-20260715/pilot-archive-manifest.json --out=.data/pilot-storage-receipt.json`, `npm run pilot:storage-receipt -- --file=.data/pilot-storage-receipt.json --archive=.data/pilot-archives/final-judo-pilot-20260715/pilot-archive-manifest.json`, `npm run pilot:final-handoff -- --artifact-manifest=.data/pilot-artifact-manifest.json --archive-manifest=.data/pilot-archives/final-judo-pilot-20260715/pilot-archive-manifest.json --storage-receipt=.data/pilot-storage-receipt.json --out=.data/pilot-final-handoff.json`, `npm run pilot:status -- --strict --out=.data/pilot-status.json` 실행 | source CSV 파일과 dry-run/import 명령 참조, 자동 생성된 field evidence 초안, manifest 지점/계정 email·role·branchScope와 source CSV 일치, pre-pilot evidence count와 source CSV 예상 수치 일치, 지점/계정/기기/스크린리더 기대·실제 낭독/14일 출석·결제·공지 후속·공지 운영/환경 플래그 증빙 manifest, 시작 전 ready evidence 리포트, pre-pilot Markdown/launch package와 readiness/password rotation CSV 보관, post-pilot 런타임 고유 운영일/검증 운영일/운영 수치, Markdown 회고 파일 존재와 post-pilot/ready/운영 수치, JSON/Markdown 생성 시각, 총괄 PM signoff 순서, closeout ready 판단 패키지, SHA-256 아티팩트 manifest, archive manifest, 자동 생성된 장기 보관 receipt 초안과 최종 receipt, final handoff, 최종 status JSON이 모두 검증되면 통과 |

## 8. 접근성 체크

| ID | 절차 | 기대 결과 |
| --- | --- | --- |
| QA-A11Y-01 | Tab 키로 주요 화면 이동 | 포커스 순서가 헤더, 메뉴, 본문 액션 순서로 이동 |
| QA-A11Y-02 | 버튼/링크 이름 확인 | 아이콘 버튼은 `aria-label` 또는 텍스트 이름 보유 |
| QA-A11Y-03 | 상태 배지 확인 | 색상만이 아니라 텍스트로 상태 구분 |
| QA-A11Y-04 | `npm run test:a11y-static` 실행 | 네이티브 버튼 이름, 폼 컨트롤 라벨, 이미지 alt, 양수 tabIndex 확인 기준 통과 |
| QA-A11Y-05 | 390px 모바일 뷰포트 확인 | 텍스트 겹침 없음, 하단 nav와 sticky bar가 주요 액션을 가리지 않고 현재 메뉴가 `aria-current="page"`로 노출됨 |

## 9. 수동 테스트 실행 기록 양식

| 일시 | 테스터 | 빌드 | 통과 | 실패 | 메모 |
| --- | --- | --- | --- | --- | --- |
| 2026-06-13 | Codex | local | P0 smoke/browser 확인 통과 | 없음 | `npm run lint`, `npm run build`, `npm run test:smoke`, Browser DOM checks, Lighthouse `/login` 접근성 100점 |
| 2026-06-13 | Codex | local | 출석 수정 사유 회귀 확인 통과 | 없음 | `npm run lint`, `npm run build`, `npm run test:smoke`, `npm run test:routes`, `npm run test:db`, Browser 390px `/app/classes` |
| 2026-06-13 | Codex | local | 변경 기록 검색/필터 회귀 확인 통과 | 없음 | `npm run lint`, `npm run build`, `npm run test:smoke`, `npm run test:routes`, `npm run test:db`, Browser `/app/admin/audit-logs` 권한 차단/390px 확인 |
| 2026-06-13 | Codex | local | 대표/어드민 전용 IA 경로 강화 | 없음 | `/app/owner/branches`, `/app/owner/reports`, `/app/admin/users`, `/app/admin/settings` route smoke 대상 추가 |
| 2026-06-13 | Codex | local | 요청 기능 삭제 전 정원 검증 이력 | 없음 | `npm run lint`, `npm run build`, `npm run test:smoke`, `npm run test:routes`, `npm run test:db`, Browser 390px `/app/requests` |
| 2026-06-13 | Codex | local | 상담/주의 메모 공개 범위 통과 | 없음 | `npm run lint`, `npm run build`, `npm run test:smoke`, `npm run test:routes`, `npm run test:db`, Browser 390px `/app/members` |
| 2026-06-14 | Codex | local | 결제 할인/환불/취소 처리 통과 | 없음 | `npm run lint`, `npm run build`, `npm run test:smoke`, `npm run test:routes`, `npm run test:db`, Browser 390px `/app/payments` |
| 2026-06-14 | Codex | local | 보호자-자녀 연결 관리 통과 | 없음 | `npm run lint`, `npm run build`, `npm run test:smoke`, `npm run test:routes`, `npm run test:db`, Browser 390px `/app/members` |
| 2026-06-14 | Codex | local | 회원/학부모 긴급 연락처 수정 권한 통과 | 없음 | `npm run lint`, `npm run build`, `npm run test:smoke`, `npm run test:routes`, `npm run test:db`, Browser 390px `/app/members` |
| 2026-06-14 | Codex | local | 대표 운영 리포트 CSV 통과 | 없음 | `npm run lint`, `npm run build`, `npm run test:smoke`, `npm run test:routes`, Browser 390px `/app/owner/reports` |
| 2026-06-15 | Codex | local | 대표 리포트 우선 점검 지표 추가 | 기간별 실제 추세 차트는 이력 데이터 확정 후 후속 | `npm run test:smoke`, 운영 CSV `attendance_gap_slots`, `payment_risk_count`, `pilot_priority_score` 헤더 검증 |
| 2026-06-15 | Codex | local | 현장 운영 관제 패널 추가 | 실제 파일럿 지점/증빙 입력은 운영 단계에서 필요 | `npm run test:pilot-operator-support`, `/app/admin/settings` 다음 액션/모바일 출석 증빙 검증 |
| 2026-06-15 | Codex | local | 대표 기간별 운영 추세 추가 | 회원 생성일/퇴관일/상태 변경일 기반 신규·이탈·순증과 변경 기록 변경 이벤트를 분리 표시 | `npm run test:owner-report-trends`, 운영 CSV `trend_period`/`new_members`/`withdrawn_members`/`net_member_change`/`member_change_events` 헤더 검증 |
| 2026-06-23 | Codex | local | 대표 리포트 내보내기 문구 정리 | `/app/owner/reports`의 다운로드 버튼은 운영/결제 내보내기로 표시하고 CSV 파일 생성, export API, 감사 기록은 유지 | `npm run test:owner-report-trends`, `npm run test:role-csv-export-gates` |
| 2026-06-16 | Codex | local | 대표 오늘 우선순위 추가 | 대표가 출석 미처리/결제 위험/요청 기능 삭제 전 항목/휴면 회원을 오늘 처리할 지점별 항목으로 확인 | `npm run test:owner-report-trends`, Browser 390px `/app/owner/reports` |
| 2026-06-16 | Codex | local | 비개발자 대표 진행 보고서 추가 | 실제 운영 오픈/APK/결제사/푸시 운영 완료로 과장하지 않고 외부 준비 대기를 명시 | `npm run test:owner-progress-report`, [docs/P1_OWNER_PROGRESS_REPORT.md](docs/P1_OWNER_PROGRESS_REPORT.md)의 현재 단계/완료/남은 외부 준비/대표 결정 필요/과장 금지/secret 미노출 검증 |
| 2026-06-16 | Codex | local | 대표 진행 보고서 초안 생성기 추가 | 실제 외부 증빙이나 대표 의사결정 등록표 상태가 바뀔 때마다 `.data` 산출물에서 대표 공유용 초안을 재생성해야 함 | `npm run test:owner-progress-report-draft`, `owner:progress-report:draft` pending/ready/secret-like fixture와 `.data/p1-owner-progress-report.md` 생성 흐름 검증 |
| 2026-06-16 | Codex | local | 대표 의사결정 등록표 추가 | 대표가 외부 blocker 담당자/기한/증빙 책임자를 결정하지 않으면 P1 strict-ready로 진행할 수 없음 | `npm run test:owner-decision-register`, `owner:decision-register` pending/ready/missing/secret-like fixture와 `.data/p1-owner-decision-register.*`, `.data/p1-owner-decision-register.guide.md` 생성 흐름 검증 |
| 2026-06-16 | Codex | local | 대표 의사결정 등록표 CSV 적용 | 대표가 채운 담당자/기한/증빙 책임자 CSV를 completed 산출물로 적용하되, 이것만으로 P1 ready로 과장하지 않음 | `npm run test:owner-decision-register-apply-csv`, `owner:decision-register:apply-csv` completed JSON/Markdown, 고정 열 변조/placeholder/date/evidence URL/secret-like fixture 검증 |
| 2026-06-16 | Codex | local | 대표 공유 패키지 추가 | 대표에게 공유 전 최신 `.data` 산출물과 정적 보고서, 의사결정 등록표, CSV 작성 guide, 패키지 README, 한국어 대표 요약 Markdown이 모두 생성되어 있어야 함 | `npm run test:owner-briefing-package`, `owner:briefing-package` manifest/package-dir 생성, optional `.data/p1-owner-decision-register.completed.*` 복사, `.data/p1-owner-decision-register.guide.md`, 패키지 README, 한국어 대표 요약 Markdown, SHA-256/byte size, 누락/secret-like fixture 검증 |
| 2026-06-18 | Codex | local | 대표 공유 패키지 6인 팀 목표 프롬프트 포함 | P1 ready 외부 증빙은 계속 대기 | `npm run test:owner-briefing-package`, `owner:briefing-package`가 [docs/TEAM_AGENT_PROMPTS.md](TEAM_AGENT_PROMPTS.md)를 필수 산출물과 패키지 README 읽기 순서에 포함하고 SHA-256/byte size manifest와 공유 폴더 복사를 검증 |
| 2026-06-15 | Codex | local | 결제 상태 이력 추가 | 외부 PG/정기결제 provider 연동은 운영 provider 확정 후 필요 | `npm run test:payment-lifecycle`, `npm run test:smoke`, 결제 CSV `status_history_count`/`last_status_changed_at`/`last_status_reason` 헤더 검증 |
| 2026-06-16 | Codex | local | 확인할 결제 추가 | 대표/총괄이 미납/만료 예정/온라인 결제/정기결제 실패 후속 조치를 현재 필터 기준으로 우선 확인 | `npm run test:payment-lifecycle`, Browser 390px `/app/payments` |
| 2026-06-15 | Codex | local | 온라인 결제 provider-neutral 골격 추가 | 실제 PG/VAN 승인, provider event ID 필드/서명 매핑, 실 영수증 URL 검증은 운영 provider 확정 후 필요 | `npm run test:online-payments`, `npm run test:smoke`, 결제 CSV `online_payment_status`/`receipt_url` 헤더와 webhook event ID idempotency 검증 |
| 2026-06-15 | Codex | local | 정기결제 약정 provider-neutral 골격 추가 | 실제 PG/VAN 자동청구 승인, billing key/mandate 보관 정책, provider event ID 필드/서명 매핑, 실 영수증 URL 검증은 운영 provider 확정 후 필요 | `npm run test:recurring-billing`, `npm run test:smoke`, 결제 CSV `recurring_status`/`recurring_next_billing_date` 헤더 검증 |
| 2026-06-14 | Codex | local | 결제 상태 위험 필터 통과 | 없음 | `npm run lint`, `npm run build`, `npm run test:routes`, Browser 390px `/app/payments` |
| 2026-06-14 | Codex | local | 공지 반/개인 대상 지정 통과 | 없음 | `npm run lint`, `npm run build`, `npm run test:smoke`, `npm run test:routes`, Browser 390px `/app/notices` |
| 2026-06-14 | Codex | local | 통합 릴리즈 검증 통과 | 없음 | `npm run test:release` |
| 2026-06-14 | Codex | local | 관리자 파일럿 준비 게이트 통과 | 없음 | `npm run lint`, `npm run build`, `npm run test:routes`, `npm run test:a11y-static`, `npm run test:release`, Browser 390px `/app/admin/settings` |
| 2026-06-14 | Codex | local | 스모크 테스트 데이터 격리 통과 | 없음 | `npm run test:smoke`, `npm run test:attendance-speed`, `npm run test:release`, 실행 후 `.data` 지점 2개/출석 3건/변경 기록 0건 확인 |
| 2026-06-14 | Codex | local | 도메인 규칙 단위 테스트 통과 | 없음 | `npm run test:unit`, 역할/RBAC 매핑, 메뉴 접근, 지점/회원 스코프, 공지 대상 지정 검증 |
| 2026-06-14 | Codex | local | 모바일 출석 브라우저 E2E 통과 | 없음 | `npm run test:e2e`, 390px 코치 운영 계정 로그인/전체 출석 일괄 저장/출석 변경/새로고침 후 오프라인 대기 큐 복구/재동기화/sticky 저장/변경 기록/결제 금액 마스킹 검증 |
| 2026-06-15 | Codex | local | 모바일 하단 네비게이션 safe area 검증 강화 | 없음 | `npm run test:e2e`, fixed 하단 메뉴, `safe-area-inset-bottom`, 현재 메뉴 `aria-current`, 48px 이상 활성 메뉴 터치 영역, main bottom padding 검증 |
| 2026-06-15 | Codex | local | 모바일 미읽음 공지 배지 강화 | 없음 | `npm run test:e2e`, `npm run test:notification-readiness`, 상단 공지 버튼 미읽음 배지 및 접근성 이름 검증 |
| 2026-06-16 | Codex | local | 모바일 출석 처리율/미처리 인원 표시 강화 | 없음 | `npm run test:e2e`, 수업별 progress aria 값, 미처리 인원, 전체 출석 후 100%/0명 갱신 검증 |
| 2026-06-16 | Codex | local | 모바일 미처리 출석 필터 강화 | 없음 | `npm run test:e2e`, 미처리만 보기 토글, 완료 회원 숨김, 미처리 회원 유지, 완료 수업 빈 상태, 40px 이상 터치 영역 검증 |
| 2026-06-16 | Codex | local | 모바일 공지 필터 강화 | 없음 | `npm run test:e2e`, `npm run test:notification-readiness`, `/app/notices` 전체/미읽음/중요 필터, 표시 건수, pressed 상태, 40px 이상 터치 영역 검증 |
| 2026-06-16 | Codex | local | 모바일 공지 보이는 공지 읽음 처리 강화 | 없음 | `npm run test:e2e`, `npm run test:notification-readiness`, `npm run test:smoke`, 보이는 공지 읽음 처리 버튼, 미읽음 빈 상태, 배지 해제, bulk-read 스코프 차단 검증 |
| 2026-06-16 | Codex | local | 확인할 공지 추가 | 운영자가 중요 미읽음/미읽음 읽음 처리/중요 공지 푸시 점검/읽음 반응 점검을 현재 필터 기준으로 우선 확인 | `npm run test:notification-readiness`, Browser 390px `/app/notices` |
| 2026-06-16 | Codex | local | 모바일 출석 최근 변경 되돌리기 강화 | 없음 | `npm run test:e2e`, 390px 코치 출석 변경 후 하단 sticky 되돌리기 버튼 터치 영역, 이전 상태 서버 원복, 변경 기록, horizontal overflow 없음 검증 |
| 2026-06-16 | Codex | local | 모바일 출석 명단 검색 강화 | 없음 | `npm run test:e2e`, 390px 코치 출석 명단 검색 입력/표시 건수/검색 빈 상태/검색어 지우기/터치 영역/horizontal overflow 없음 검증 |
| 2026-06-16 | Codex | local | 모바일 출석 상태 빠른 필터 강화 | 없음 | `npm run test:e2e`, 390px 코치 출석 상태 칩 필터의 터치 영역/pressed 상태/상태별 건수/필터 결과/상태별 빈 상태/horizontal overflow 없음 검증 |
| 2026-06-16 | Codex | local | 모바일 출석 빠른 메모 강화 | 없음 | `npm run test:e2e`, 390px 코치 출석 빠른 메모 칩 터치 영역, 기존 메모와 조합, 저장 후 출석 메모/변경 기록 유지, horizontal overflow 없음 검증 |
| 2026-06-15 | Codex | local | 중요 공지 표시 강화 | 없음 | `npm run test:notification-readiness`, `npm run test:smoke`, 중요 공지 발행/저장/변경 기록/배지/우선 정렬/푸시 제목 prefix 검증 |
| 2026-06-15 | Codex | local | 모바일 설치 메타데이터 및 오프라인 앱 셸 가드 통과 | 없음 | `npm run test:mobile-install`, iOS/Android PWA manifest, standalone `/app` start URL, 192/512 아이콘, apple web app, viewport/theme color, production 서비스 워커 등록, API 캐시 금지 검증 |
| 2026-06-15 | Codex | local | 계정 화면 PWA 설치 액션 강화 | 없음 | `npm run test:mobile-install`, `/app/account` 설치 액션, `beforeinstallprompt` 프롬프트, standalone/iOS 설치 상태, 거절/수동 설치 상태, `aria-live` 검증 |
| 2026-06-16 | Codex | local | Android TWA build 환경 가드 강화 | 실제 Java Runtime/Android SDK/운영 HTTPS 웹앱 도메인 준비 전 APK/AAB 미생성 | `npm run test:android-packaging`, `npm run android:twa:prepare -- --origin=<확정 운영 HTTPS 웹앱 origin> --sha256=<fingerprint> --out-dir=<tmp>`, Java/keytool/sdkmanager/adb/npx/Android SDK home `buildReady/buildBlockers`, Android handoff draft/manifest 검증 |
| 2026-07-21 | Codex | local | Android Play 비공개 테스트 업로드 파일 갱신 | Desktop AAB/APK가 최신 Play release report와 다른 파일이면 업로드 전 실패 | `npm run test:android-play-release-artifacts`, `.data/mobile-builds/android-play-release-20260721064936/google-play-release-report.json`, `/Users/sny0219/Desktop/final-judo-play-release.aab`, `/Users/sny0219/Desktop/final-judo-release.apk`, `versionCode 40`/`versionName 1.0.39` |
| 2026-07-05 | Codex | Browser 390px + iOS Simulator | 결제 정보 재사용 명시 동의 | `/app/payments/checkout`에서 `결제수단과 입력정보를 다음에도 사용`이 기본 해제되고, 사용자가 직접 체크한 뒤에만 다음 납부 재사용 문구가 표시됨 | `npm run test:payment-checkout-method-flow`, `npm run test:p5-p10-internal-readiness`, Browser `saveMethodChecked=false`, iPhone 16e 증빙 `.data/mobile-builds/ios/payment-checkout-save-opt-in-20260705/payment-checkout-save-opt-in-ios-sim.png` |
| 2026-07-05 | Codex | Browser 390px + iOS Simulator | 우리WON페이 모달 결제 오인 문구 정리 | `/app/payments/checkout` 우리카드 모달이 실제 결제 완료처럼 보이지 않고 선택 확인/도장 안내 후 진행 문구로 표시됨 | `npm run test:payment-checkout-method-flow`, `npm run test:p5-p10-internal-readiness`, Browser modal copy, iPhone 16e 증빙 `.data/mobile-builds/ios/payment-checkout-woori-copy-ios-20260705/payment-checkout-woori-copy-ios-sim.png` |
| 2026-07-05 | Codex | Browser 390px + iOS Simulator | 결제 확인 피드백 live region | `/app/payments/checkout`에서 `납부 정보 확인` 후 나타나는 피드백을 `role=status`와 `aria-live=polite`로 전달해 보조기술도 확인 상태를 받을 수 있게 함 | `npm run test:payment-checkout-method-flow`, Browser `confirmationFeedbackA11y.role=status`, `ariaLive=polite`, iPhone 16e `.data/mobile-builds/ios/payment-checkout-feedback-a11y-20260705/payment-checkout-feedback-a11y-ios-sim.png`, summary `.data/mobile-builds/ios/payment-checkout-feedback-a11y-20260705/summary.json` |
| 2026-07-05 | Codex | Browser 390px + iOS Simulator | 공지 액션 피드백 잔상 정리 | `/app/notices`에서 읽음, 알림 발송, 삭제, 작성 피드백이 동시에 남지 않도록 액션 시작 시 이전 피드백을 정리 | `npm run test:notice-delete-ui`, Browser `feedbackResetState.readFeedbackCount=0`, iPhone 16e `.data/mobile-builds/ios/notice-feedback-reset-20260705/notice-feedback-reset-ios-sim.png`, summary `.data/mobile-builds/ios/notice-feedback-reset-20260705/summary.json` |
| 2026-07-14 | Codex | Browser API/390px, isolated `next start` | 공지 수정 읽음·권한·감사 계약 | 실제 내용 변경은 읽음 초기화, 동일 내용 재저장은 읽음 유지, 코치 audience 변경은 403, 반·개인 대상 변경/잘못된 타입은 400, 읽음·수정 동시 요청 직렬화, 감사 로그 본문 원문은 미저장 | `npm run test:notice-delete-ui`, `npm run test:notification-readiness`, `.data/mobile-builds/ios/notice-update-contract-20260714/summary.json`, overflow 0, console/runtime 오류 없음 |
| 2026-06-16 | Codex | local | Android TWA doctor Markdown 리포트 추가 | 실제 Java Runtime/Android SDK/운영 HTTPS 웹앱 도메인 준비 전 APK/AAB 미생성 | `npm run test:android-packaging`, `npm run android:twa:doctor -- --origin=<확정 운영 HTTPS 웹앱 origin> --sha256=<fingerprint> --out=.data/android-twa-doctor.json --markdown=.data/android-twa-doctor.md`, JSON/Markdown doctor 리포트와 다음 액션 검증 |
| 2026-06-16 | Codex | local | Android 예시 origin 차단 가드 추가 | `localhost`, `.example`, `.test`, `.local`, TODO origin은 실제 운영 origin으로 인정하지 않음 | `npm run test:android-packaging`, `npm run test:android-release-handoff`, doctor/prepare/build/handoff가 placeholder origin을 blocked로 판정하는 fixture 검증 |
| 2026-06-18 | Codex | local | 모바일 앱 API origin 혼동 차단 가드 추가 | `https://api.finaljudo.co.kr` 같은 API-only 후보는 `/login`과 `/app/dashboard`를 서빙하는 웹앱 origin 확인 전 Android/iOS 앱 origin으로 인정하지 않음 | `npm run test:android-packaging`, `npm run test:ios-capacitor-connection`, `npm run test:ios-ipa-doctor`, Android TWA doctor/prepare와 iOS Capacitor/IPA doctor가 `api.*` origin을 blocked로 판정하고 `--allow-api-origin-webapp` 예외는 운영자 확인 후에만 사용 |
| 2026-06-16 | Codex | local | 운영/결제/푸시 예시 origin 차단 가드 추가 | 실제 운영 배포/PG/VAN/푸시 handoff에서 `localhost`, `.example`, `.test`, `.local`, TODO origin과 예시 mailto subject를 ready로 인정하지 않음 | `npm run test:deployment-handoff`, `npm run test:deployment-handoff-draft`, `npm run test:payment-provider-handoff`, `npm run test:payment-provider-handoff-draft`, `npm run test:notification-push-handoff`, `npm run test:notification-push-handoff-draft` placeholder origin fixture 검증 |
| 2026-06-15 | Codex | local | Android TWA 패키징 준비 가드 추가 | JDK/Android SDK/운영 HTTPS 도메인 준비 전 APK/AAB 미생성 | `npm run test:android-packaging`, `npm run android:twa:doctor`, `npm run test:android-release-handoff-draft`, `npm run test:android-release-handoff`, `.github/workflows/android-twa.yml`, TWA/Bubblewrap 전략, assetlinks 템플릿, PWA manifest 호환성, Android SDK/JDK blockers JSON, Android handoff draft/manifest 검증 |
| 2026-06-15 | Codex | local | PWA 알림 준비 상태 가드 추가 | 운영 VAPID 키와 실제 기기 푸시 수신 검증은 파일럿 환경에서 필요 | `npm run test:notification-readiness`, 알림 권한 UI, PushSubscription 저장/해지 API, 공지별 push dispatch API, 알림 표시/push/click 경로 검증 |
| 2026-06-15 | Codex | local | 운영 푸시 handoff 가드 추가 | 운영 VAPID secret store와 Android 실기기 push 수신/클릭 증빙은 파일럿 환경에서 필요 | `npm run test:notification-push-handoff-draft`, `npm run test:notification-push-handoff`, 원문 VAPID private key 미기록, Android 기기 필수, 공지 발송 변경 기록, P1 readiness/release package 연결 검증 |
| 2026-06-14 | Codex | local | 의존성 보안 audit 통과 | 없음 | `npm audit --audit-level=moderate`, Next 내부 `postcss` override 후 취약점 0건 |
| 2026-06-14 | Codex | local | 파일럿 2지점 샘플 데이터 통과 | 없음 | `npm run test:pilot`, 강남 본관/송파 도장 샘플과 5개 역할/수업/회원권/공지 검증 |
| 2026-06-14 | Codex | local | 파일럿 준비 계약 정합성 통과 | 없음 | `npm run test:pilot-readiness-contract`, 기본 준비 항목/import/preflight 필수 항목의 ID/문구/담당자 정합성 확인 |
| 2026-06-14 | Codex | local | JSON 저장소 백업/복구 게이트 통과 | 없음 | `npm run test:store`, 기본 데이터 생성/원자적 쓰기/동일 키 FIFO 잠금/백업 pruning/깨진 JSON 및 잘못된 shape 복구 검증 |
| 2026-06-21 | Codex | local | stale demo seed 날짜 보정 | 오래된 `.data/final-judo-db.json` seed 날짜가 남아 회원 화면이 과거 일정에 묶이지 않도록 known demo seed ID의 수업/결제 날짜만 오늘 기준으로 보정 | `npm run test:store`, 임시 stale seed DB를 읽어 수업/결제 날짜 rolling 확인 |
| 2026-06-21 | Codex | local/browser | 관리자 변경 기록 앱용 문구 recheck | 오래된 runtime 기록에 남은 `변경 기록을 조회했습니다.` 표시를 `변경 기록을 조회했습니다.`로 보정하고, 관리자 변경 기록/코치 회원 화면이 빈 화면 없이 실제 서비스 화면으로 렌더링되는지 확인 | `npm run test:store`, `npm run test:p5-p10-internal-readiness`, `.data/mobile-builds/ios/visible-text-audit-20260621-recheck/recheck.json` |
| 2026-06-14 | Codex | local | 파일럿 데이터 import dry-run 통과 | 없음 | `npm run test:pilot-import`, intake CSV를 지점/계정/회원/수업/회원권/공지/보호자 연결이 포함된 운영 DB shape로 변환 검증 |
| 2026-06-14 | Codex | local | 파일럿 준비 초안 workspace 통과 | 없음 | `npm run test:pilot-prelaunch-draft`, readiness/password CSV 초안, audit launch package, pre-pilot evidence/preflight/status, blocked next action summary 생성 검증 |
| 2026-06-14 | Codex | local | 파일럿 status prelaunch 다음 액션 통과 | 없음 | `npm run test:pilot-status`, runtime waiting과 readiness evidence missing 리포트가 `pilot:prelaunch-draft`를 다음 액션으로 안내하는지 검증 |
| 2026-06-14 | Codex | local | 파일럿 런치 패키지 통과 | 없음 | `npm run test:pilot-launch-package`, CSV 검증/import dry-run/pre-pilot evidence JSON·Markdown/password rotation/manifest 템플릿/운영 문서 명령 검증 |
| 2026-06-14 | Codex | local | 파일럿 런치 명령 검증 통과 | 없음 | `npm run test:pilot-launch-command`, `pilot:launch-package` audit/strict 종료 코드와 pre-pilot evidence/preflight/readiness evidence/password rotation/launch package 산출물 및 PostgreSQL URL 비밀번호 redaction 검증 |
| 2026-06-14 | Codex | local | 파일럿 준비 증빙 CSV 검증 통과 | 없음 | `npm run test:pilot-readiness-evidence`, shared readiness ID 기반 CSV 초안, pre-pilot retro 제외, verified 증빙, placeholder/unknown/missing ID 차단 검증 |
| 2026-06-14 | Codex | local | 파일럿 준비 증빙 적용 검증 통과 | 없음 | `npm run test:pilot-readiness-evidence-apply`, 준비 증빙 CSV dry-run/write, pre-pilot readiness 반영, `pilot_readiness.update` 변경 기록, idempotent 재실행, pending/non-admin/invalid checkedAt 차단 검증 |
| 2026-06-14 | Codex | local | 파일럿 비밀번호 교체 증빙 검증 통과 | 없음 | `npm run test:pilot-password-rotation`, 계정별 CSV 초안, 기본 임시 비밀번호 잔존, 변경 기록 누락, 계정 누락, 원문 임시 비밀번호 노출 차단 검증 |
| 2026-06-14 | Codex | local | PostgreSQL 런타임 저장소 통과 | 없음 | `npm run test:postgres-store`, Docker PostgreSQL `app_runtime_state` JSONB 저장소 연결/쓰기/읽기/revision/row, 서로 다른 store 인스턴스의 동일 키 advisory lock, 파일럿 CSV import, 준비 증빙 CSV apply, 비밀번호 교체 CSV, `pilot_readiness.update` 변경 기록, PostgreSQL 파일럿 prelaunch draft/launch package/evidence/status와 DB URL 비밀번호 redaction 검증 |
| 2026-06-15 | Codex | local | PostgreSQL prelaunch draft smoke 통과 | 없음 | `npm run test:postgres-store`, `pilot:prelaunch-draft -- --driver=postgres`가 summary/status/launch package를 생성하고 stdout/summary/status/launch package에 원문 DB 비밀번호를 남기지 않는지 검증 |
| 2026-06-14 | Codex | local | 파일럿 준비 증빙 트래커 통과 | 없음 | `/api/v1/admin/pilot-readiness`, `/app/admin/settings` 상태/담당자/증빙 저장, `pilot_readiness.update` 변경 기록을 `npm run test:smoke`에 포함 |
| 2026-06-14 | Codex | local | 운영 데모 로그인 가드 통과 | 없음 | `npm run test:auth-production-guard`, production 기본 차단과 `FINAL_JUDO_ENABLE_DEMO_LOGIN=1` 명시 허용, secure/httpOnly session cookie 검증 |
| 2026-06-14 | Codex | local | 운영 reset API 가드 통과 | 없음 | `npm run test:dev-reset-guard`, production 기본 차단과 `FINAL_JUDO_ENABLE_DEV_RESET=1` 명시 허용 검증 |
| 2026-06-15 | Codex | local | 환경 변수 매트릭스 검증 추가 | 실제 운영 secret 값 입력과 배포 플랫폼 secret store 설정은 운영 단계 필요 | `npm run test:env-readiness`, `docs/ENVIRONMENT_MATRIX.md`, `.env.example`, `.env.production.example` 안전 기본값/placeholder/release gate 정합성 검증 |
| 2026-06-15 | Codex | local | 운영 배포 handoff 검증 추가 | 실제 운영 secret 값과 production preflight ready 리포트는 배포 직전 필요 | `npm run test:deployment-handoff-draft`, `npm run test:deployment-handoff`, `docs/deployment-handoff.template.json`, 원문 secret 값 차단, 필수 env/secret store/blocked preflight fixture 검증 |
| 2026-06-14 | Codex | local | 초대 가입 초기 비밀번호 보안 통과 | 없음 | `npm run test:smoke`, 초대 수락 시 12자 이상 초기 비밀번호 필수, 기본 임시 비밀번호 차단, 사용된 초대 링크 재사용 차단, `auth.invite.accept` 변경 기록 원문 미저장 검증 |
| 2026-06-14 | Codex | local | 운영 데이터 점검 통과 | 없음 | `npm run test:preflight-runtime`, 현재 `.data`에서 정확한 데모 blocker/warning/count와 참조 오류/민감정보/운영 로그 오류 없음 확인 |
| 2026-06-14 | Codex | local | 파일럿 증빙 리포트 통과 | 없음 | `npm run test:pilot-evidence`, runtime/preflight/준비 항목/운영 로그 요약과 post-pilot `--require-retro` 차단 조건 검증 |
| 2026-06-14 | Codex | local | 현장 증빙 manifest 초안 검증 통과 | 없음 | `npm run test:pilot-field-evidence-draft`, source CSV 지점/계정과 post-pilot 운영 수치 자동 반영, placeholder 초안 strict 차단, 운영자 입력 manifest validator 통과 검증 |
| 2026-06-14 | Codex | local | 현장 증빙 manifest 검증기 통과 | 없음 | `npm run test:pilot-field-evidence`, 템플릿 shape와 실제 파일럿 현장 증빙 manifest 통과/실패 조건, source CSV 검증과 지점/계정 email·role·branchScope 대조 및 pre-pilot evidence count 대조, 스크린리더 기대·실제 낭독 품질 기록, pre-pilot evidence readiness, 고유 운영일/검증 운영일/공지 후속/공지 누락, post-pilot 리포트 수치/경로/freshness/signoff chronology 불일치 차단 검증 |
| 2026-06-14 | Codex | local | 파일럿 종료 패키지 검증 통과 | 없음 | `npm run test:pilot-closeout-package`, post-pilot preflight/JSON/Markdown/field manifest ready 패키지와 blocked preflight/Markdown 누락 차단 검증 |
| 2026-06-14 | Codex | local | 파일럿 아티팩트 manifest 검증 통과 | 없음 | `npm run test:pilot-artifact-manifest`, pre/post preflight·evidence·Markdown·launch package·password rotation CSV·field manifest·closeout 파일 해시와 변조/누락 차단 검증 |
| 2026-06-14 | Codex | local | 파일럿 아티팩트 archive 검증 통과 | 없음 | `npm run test:pilot-archive-artifacts`, ready manifest 원본 파일 해시 재검증, fresh archive 복사, 복사본 해시 검증, 변조 파일/blocked manifest/기존 archive 폴더/placeholder 경로 차단 검증 |
| 2026-06-14 | Codex | local | 파일럿 storage receipt 초안 검증 통과 | 없음 | `npm run test:pilot-storage-receipt-draft`, archive manifest 해시/byte size와 산출물별 archivedPath/sha256/sizeBytes 자동 반영, placeholder 차단, 운영자 입력 receipt validator 통과 검증 |
| 2026-06-14 | Codex | local | 파일럿 장기 보관 receipt 검증 통과 | 없음 | `npm run test:pilot-storage-receipt`, archive manifest와 업로드 receipt의 보관 URI/해시/byte size/보존 정책/산출물 coverage 대조, placeholder/누락/시간순서/변조 차단 검증 |
| 2026-06-14 | Codex | local | 파일럿 최종 handoff 검증 통과 | 없음 | `npm run test:pilot-final-handoff`, artifact manifest/archive manifest/storage receipt의 ready 상태와 경로/해시/byte size 정합성, 누락 archive 항목/변조/blocked manifest 차단 검증 |
| 2026-06-14 | Codex | local | 파일럿 상태 요약 검증 통과 | 없음 | `npm run test:pilot-status`, 런타임/증빙/보관/final handoff 단계별 ready 판정, missing/waiting/blocked non-ready blocker 목록, status JSON 출력 저장, strict final handoff 재검증/변조 archive 차단 fixture 검증 |
| 2026-06-15 | Codex | local | P1 final evidence intake draft 검증 추가 | 실제 owner/status/evidence 입력은 외부 담당자 확인 필요 | `npm run test:p1-evidence-intake-draft`, P1 readiness 7개 최종 요구사항을 6인 팀+iOS lane별 JSON/CSV/Markdown 입력표로 생성하고 secret-like 값 redaction 검증 |
| 2026-06-15 | Codex | local | P1 final evidence intake CSV 적용 검증 추가 | 실제 owner/evidence/signoff 입력은 외부 담당자 확인 필요 | `npm run test:p1-evidence-intake-apply-csv`, 운영자가 채운 CSV의 operator 필드만 completed JSON으로 적용하고 strict verifier 연계, TODO/row 변조/secret-like 값 차단 검증 |
| 2026-06-15 | Codex | local | P1 final evidence intake strict 검증 추가 | 실제 owner/status/evidence/signoff 입력은 외부 담당자 확인 필요 | `npm run test:p1-evidence-intake`, ready readiness 기반 intake의 owner/evidence URL/checkedAt/signoff, row 계약, readiness 경로, secret-like 값 차단 검증 |
| 2026-06-15 | Codex | local | P1 release archive 검증 추가 | 실제 외부 P1 package ready 산출물 입력은 외부 담당자 확인 필요 | `npm run test:p1-release-archive`, ready P1 release package 원본 파일 해시 재검증, fresh archive 복사, 복사본 해시 검증, 변조/blocked/existing/placeholder/secret fixture 차단 |
| 2026-06-15 | Codex | local | P1 release storage receipt 검증 추가 | 실제 장기 보관소 업로드 위치와 receipt 입력은 외부 담당자 확인 필요 | `npm run test:p1-release-storage-receipt`, P1 release archive upload receipt 초안/strict 검증, 업로드 coverage, 보존 정책, 로컬 archive copy hash, secret-like 값 차단 검증 |
| 2026-06-16 | Codex | local | P1 GitHub connector repo/access readiness 검증 추가 | 실제 owner acknowledgement 후 publishReady=true 이슈 생성은 외부 담당자 확인 필요 | `npm run test:p1-github-connector-readiness -- --connector-repo-receipt=.data/p1-github-connector-access.json`, connector payload repo와 local origin 일치, `_create_issue` tool name, blocked publish guard, issue payload 계약, `_get_repo` 접근 repo/default branch/권한 검증 |
| 2026-06-16 | Codex | local | 관리자 설정 GitHub connector access 안내 검증 추가 | 실제 owner acknowledgement 후 publishReady=true 이슈 생성은 외부 담당자 확인 필요 | `npm run test:pilot-operator-support`, `/app/admin/settings`의 P1 최종 배포 준비 안내에 `.data/p1-github-connector-access.json`, `_get_repo`, default branch/권한 검증 명령, `GitHub connector access ready` 상태판 항목 노출 확인 |
| 2026-06-16 | Codex | local | 관리자 설정 상태판 섹션 안내 검증 추가 | 실제 외부 handoff와 evidence intake가 ready 전이면 release package/archive/storage 보관은 대기 | `npm run test:pilot-operator-support`, `/app/admin/settings`의 P1 최종 배포 준비 안내에 운영자 상태판 Markdown GitHub Connector 섹션, Release Custody 섹션, readiness/evidence intake blocked 상태의 release custody prerequisite 안내가 표시되는지 확인 |
| 2026-06-16 | Codex | local | P1 이슈 등록 상태판 한국어 안내 검증 추가 | 실제 owner acknowledgement 후 publishReady=true 이슈 생성은 외부 담당자 확인 필요 | `npm run test:p1-operator-status`, issue registration blocked next action이 GitHub connector/CSV/JSON/수동 workflow 절차를 한국어로 안내하고 영어 fallback 문구를 차단하는지 확인 |
| 2026-06-16 | Codex | local | P1 dispatch 상태판 한국어 안내 검증 추가 | 실제 owner delivery/acknowledgement 입력은 외부 담당자 확인 필요 | `npm run test:p1-operator-status`, dispatch blocked next action이 담당자 전달/acknowledgement CSV 작성과 strict 검증 절차를 한국어로 안내하고 영어 fallback 문구를 차단하는지 확인 |
| 2026-06-16 | Codex | local | 구현 백로그 문서 정합성 검증 추가 | 실제 외부 증빙 대기 항목은 계속 대기 상태 | `npm run test:implementation-backlog`, IMPLEMENTATION_BACKLOG ID 중복/섹션 불일치/순번 누락/status drift와 `바로 다음 작업` 번호 중복/누락 차단 |
| 2026-06-16 | Codex | local | QA 계획 문서 정합성 검증 추가 | 실제 외부 증빙 대기 항목은 계속 대기 상태 | `npm run test:qa-plan`, QA_TEST_PLAN scenario ID 중복/빈 기대 결과/npm script 오타 차단 |
| 2026-06-16 | Codex | local | P1 외부 handoff 초안 한국어 안내 검증 추가 | 실제 운영 secret/Android SDK/PG/VAN/VAPID/owner acknowledgement는 외부 증빙 대기 | `npm run test:p1-handoff-draft`, `npm run test:p1-handoff-issues`, `npm run test:p1-github-connector-readiness`, deployment/Android/payment/notification/issue registration 초안과 connector payload blocked publish guard가 한국어 운영 안내를 유지하는지 확인 |
| 2026-06-16 | Codex | local | P1 handoff Android doctor Markdown 보관 검증 추가 | 실제 Java Runtime/Android SDK/운영 HTTPS 도메인 준비 전 APK/AAB 미생성 | `npm run test:p1-handoff-draft`, `p1:handoff-draft` workspace의 `.data/android-twa-doctor.md` 제목/체크 테이블/다음 액션/secret 미노출 확인 |
| 2026-06-16 | Codex | local | P1 operator status Android doctor 노출 검증 추가 | 실제 Java Runtime/Android SDK/운영 HTTPS 도메인 준비 전 APK/AAB 미생성 | `npm run test:p1-operator-status`, 운영자 상태판 JSON/Markdown support artifacts에 `.data/android-twa-doctor.md`가 표시되고 제목/체크 테이블/다음 액션/secret 미노출 형식이 유지되는지 확인 |
| 2026-06-18 | Codex | local | P1 operator status iOS IPA doctor 노출 검증 추가 | 실제 iPhone UDID/provisioning profile/운영 HTTPS 웹앱 origin 준비 전 IPA 미생성 | `npm run test:p1-operator-status`, `npm run test:pilot-operator-support`, 운영자 상태판 JSON/Markdown support artifacts와 관리자 모바일 배포 상태에 `.data/mobile-builds/ios/ios-ipa-doctor.md`와 Provisioning Hints가 표시되는지 확인 |
| 2026-06-18 | Codex | local | 관리자 보류 중인 외부 준비 표시 | 운영 웹앱 origin과 iOS 실제 iPhone/provisioning profile은 사용자 보류이며 ready 아님 | `npm run test:pilot-operator-support`, `/app/admin/settings`의 `보류 중인 외부 준비` 섹션이 운영 웹앱 origin 확정, iOS 실제 iPhone/provisioning profile, profile inventory 0, 출시 판단은 보류 유지 문구를 표시하는지 확인 |
| 2026-06-18 | Codex | local | P1 operator status 보류 외부 준비 섹션 추가 | 운영 웹앱 origin과 iOS 실제 iPhone/provisioning profile은 사용자 보류이며 ready 아님 | `npm run test:p1-operator-status`, `.data/p1-operator-status.json/.md`의 Deferred External Prep 섹션이 사용자 보류 2개, `/login`, `/app/dashboard`, profile inventory 0, 출시 판단은 보류 유지 문구를 보관하는지 확인 |
| 2026-06-18 | Codex | local | P1 completion evidence doctor 분류 강화 | 실제 운영 origin/release signing/실제 iPhone provisioning profile 준비 전 Android/iOS 배포 ready 아님 | `npm run test:p1-completion-evidence`, Android TWA doctor와 iOS IPA doctor blocked 진단은 support artifact로 남기고 외부 blocker가 있는 완료 기준은 외부 증빙 대기로 집계되는지 확인 |
| 2026-06-18 | Codex | local | 관리자 P1 상태판 doctor 안내 정합화 | 실제 운영 origin/release signing/실제 iPhone provisioning profile 준비 전 Android/iOS 배포 ready 아님 | `npm run test:pilot-operator-support`, `/app/admin/settings` operator status/완료 기준 매트릭스 안내에 iOS IPA doctor JSON/Markdown, Provisioning Hints support artifact, Android/iOS doctor 외부 blocker 분류 기준이 표시되는지 확인 |
| 2026-06-18 | Codex | local | 회원/학부모 CSV export API 403 smoke 증거 추가 | 대표/총괄 CSV export는 유지, 회원/학부모는 API 직접 호출도 차단 | `npm run test:role-csv-export-gates`, `npm run test:smoke`, `npm run test:release`, 회원/학부모 `/api/v1/exports/payments`와 `/api/v1/exports/operations` 403 및 CSV content-type 미반환 확인 |
| 2026-06-18 | Codex | local | externalBlockers CSV 7개 row 문서 정합화 | iOS IPA 실제 기기/provisioning profile 준비 전 externalBlockers CSV는 7개 row 유지 | `npm run test:pilot-operator-support`, `npm run test:p1-operator-status-apply-external-blockers-csv`, README/QA/릴리즈 체크리스트의 externalBlockers CSV row 수가 운영 배포, Android, iOS IPA, 결제, 푸시, 이슈 등록, 파일럿 7개와 일치하는지 확인 |
| 2026-06-18 | Codex | local | iOS IPA doctor 산출물 경로 정합화 | 실제 iPhone UDID/provisioning profile/운영 HTTPS 웹앱 origin 준비 전 IPA ready 아님 | `npm run test:pilot-operator-support`, `npm run test:qa-plan`, README/QA/관리자 화면의 `ios:ipa:doctor` strict 명령이 `.data/mobile-builds/ios/ios-ipa-doctor.json/.md` 산출물을 기준으로 일치하는지 확인 |
| 2026-06-18 | Codex | local | iOS IPA doctor profile inventory 강화 | 실제 iPhone UDID/provisioning profile/운영 HTTPS 웹앱 origin 준비 전 IPA ready 아님 | `npm run test:ios-ipa-doctor`, 임시 provisioning profile fixture로 Local Profile Inventory의 profile 파일 수, Team ID/bundle id 매칭 수, 등록 기기 포함 profile 수를 확인하고 실제 기기 UDID 원문이 JSON/Markdown에 기록되지 않는지 확인 |
| 2026-06-18 | Codex | local | iOS IPA provisioning 런북 검증 추가 | 실제 iPhone UDID/provisioning profile/운영 HTTPS 웹앱 origin 준비 전 IPA ready 아님 | `npm run test:ios-provisioning-runbook`, [docs/IOS_IPA_PROVISIONING_RUNBOOK.md](docs/IOS_IPA_PROVISIONING_RUNBOOK.md)의 Team ID `5GWZ792DWH`, bundle id `kr.co.finaljudo.multigym`, `FINAL_JUDO_IOS_SERVER_URL`, matching provisioning profile, strict doctor/build 순서, UDID 원문 미기록 규칙 검증 |
| 2026-06-18 | Codex | local | P1 6인 팀 목표 프롬프트 정합화 | P1 ready 외부 증빙은 계속 대기 | `npm run test:team-agent-prompts`, [docs/TEAM_AGENT_PROMPTS.md](TEAM_AGENT_PROMPTS.md)의 P0 완료 후 P1 운영/모바일 배포 기준 프롬프트, Android APK artifact/iOS Simulator와 IPA ready 분리, 7개 외부 blocker 검증 |
| 2026-06-16 | Codex | local | P1 operator status GitHub connector Markdown 섹션 추가 | 실제 owner dispatch/acknowledgement 입력 후 publishReady=true 이슈 발행은 외부 담당자 확인 필요 | `npm run test:p1-operator-status`, 운영자 Markdown의 GitHub Connector 섹션에 repo, `_get_repo` checkedAt, default branch, 권한, issue payload/publish 상태가 표시되는지 확인 |
| 2026-06-16 | Codex | local | P1 operator status release custody 순서 강화 | 실제 외부 handoff와 evidence intake가 ready 전이면 release package/archive/storage 보관은 대기 | `npm run test:p1-operator-status`, readiness/evidence intake blocked 상태에서 release package/archive/storage 명령 대신 prerequisite 안내가 표시되고 ready fixture에서는 보관 prerequisite가 true로 요약되는지 확인 |
| 2026-06-16 | Codex | local | P1 operator status Release Custody 섹션 추가 | 실제 외부 handoff와 evidence intake가 ready 전이면 release package/archive/storage 보관은 대기 | `npm run test:p1-operator-status`, 운영자 Markdown의 Release Custody 섹션에 prerequisite/package/archive/storage receipt 상태와 한국어 다음 액션이 표시되는지 확인 |
| 2026-06-16 | Codex | local | 운영자 지원 문서 게이트 강화 | 실제 외부 handoff와 evidence intake가 ready 전이면 release package/archive/storage 보관은 대기 | `npm run test:pilot-operator-support`, README/QA 계획/릴리즈 체크리스트 모두에서 현장 운영 관제, P1 최종 배포 준비, Android doctor Markdown, `externalBlockers`, `github-connector-runbook.md`, GitHub Connector 섹션, Release Custody 섹션, release custody prerequisite 안내가 유지되는지 확인 |
| 2026-06-16 | Codex | local | P1 외부 증빙 수집 확인 추가 | 실제 운영 secret/Android SDK/PG/VAN/VAPID/owner acknowledgement는 외부 증빙 대기 | `npm run test:pilot-operator-support`, `/app/admin/settings` P1 최종 배포 준비 패널의 외부 증빙 수집 확인가 lane별 초안, blocked 리포트, 수집 필드, 먼저 실행할 명령, strict 검증 명령을 유지하는지 확인 |
| 2026-06-16 | Codex | local | P1 증빙 형식 가드 추가 | 실제 외부 증빙 입력은 운영 담당자 확인 필요 | `npm run test:pilot-operator-support`, `/app/admin/settings` P1 최종 배포 준비 패널의 증빙 형식 가드가 HTTPS/provider URI, ISO timestamp, 승인 시간순서, TODO placeholder 제거, 원문 secret 금지를 안내하는지 확인 |
| 2026-06-16 | Codex | local | P1 operator status 증빙 형식 가드 보관 | 실제 외부 증빙 입력은 운영 담당자 확인 필요 | `npm run test:p1-operator-status`, `.data/p1-operator-status.json`과 Markdown의 Evidence Format Guardrails 섹션이 HTTPS/provider URI, ISO timestamp, 승인 시간순서, TODO placeholder 제거, 원문 secret 금지 규칙을 보관하는지 확인 |
| 2026-06-16 | Codex | local | GitHub connector runbook 산출물 검증 추가 | 실제 owner dispatch/acknowledgement 입력 후 publishReady=true 이슈 발행은 외부 담당자 확인 필요 | `npm run test:p1-handoff-issues`, `npm run test:p1-handoff-draft`, `github-connector-runbook.md`가 pending 상태에서는 connector 호출 금지, ready 상태에서는 `_create_issue` 호출/응답 저장/acknowledgement/receipt 검증 순서와 body hash 표를 표시하는지 확인 |
| 2026-06-16 | Codex | local | GitHub connector runbook readiness 정합성 검증 추가 | 실제 owner dispatch/acknowledgement 입력 후 publishReady=true 이슈 발행은 외부 담당자 확인 필요 | `npm run test:p1-github-connector-readiness`, `github-connector-runbook.md`의 repo, publishReady, publishGuard, response target, results template, payload body SHA-256 표와 blocked/ready 안내가 connector payload와 일치하는지 확인 |
| 2026-06-16 | Codex | local | GitHub connector 실제 접근 확인 | 실제 owner dispatch/acknowledgement 입력 후 publishReady=true 이슈 발행은 외부 담당자 확인 필요 | GitHub connector로 `antoe-prog/ant` 최근 PR 조회 정상 응답, local origin repo와 connector access receipt repo/권한 일치 확인 |
| 2026-06-16 | Codex | local | 관리자 설정 P1 상태판 모바일 렌더링 확인 | 실제 외부 handoff와 evidence intake가 ready 전이면 release package/archive/storage 보관은 대기 | Browser `/app/admin/settings`, `github-connector-runbook.md`와 runbook publishReady/repo/body hash 정합성 문구 반영, 390x844 가로 오버플로 없음, 하단 네비게이션과 터치 타겟 확인 |
| 2026-06-16 | Codex | local | P1 통합 릴리즈 검증 재통과 | Android APK/AAB 실 산출은 JDK/Android SDK/운영 HTTPS 웹앱 origin/release SHA-256 준비 전까지 대기 | `npm run test:release`, lint/build/audit/unit/store/환경/Android/알림/파일럿/P1 handoff/GitHub connector/모바일 E2E/smoke/DB/PostgreSQL store 전체 통과 |
| 2026-06-16 | Codex | local | P1 issue registration publish 전 next action 가드 추가 | 실제 owner dispatch/acknowledgement 입력 후 publishReady=true 이슈 발행은 외부 담당자 확인 필요 | `npm run test:p1-handoff-issue-receipt`, `npm run test:p1-operator-status`, blocked issue draft receipt/report/operator status가 외부 이슈 등록 대신 dispatch acknowledgement CSV/receipt와 `publishReady=true` 재생성을 먼저 안내하는지 확인 |
| 2026-06-20 | Codex | local | 공지 알림 API 피드백 기술명 제거 | 공지별 알림 발송 보류/실패 피드백이 `VAPID 공개키/비밀키` 대신 `알림 발송 설정` 기준으로 안내되며 운영 환경 알림 설정 전에는 실제 발송 ready가 아님 | `npm run test:notification-readiness`, `npm run test:p5-p10-internal-readiness`, 공지 알림 dispatch API 사용자 피드백과 앱 노출 문구 회귀 차단 |
| 2026-06-21 | Codex | local | 공지 알림 미지원/실패 문구 앱용 안정화 | `/app/notices`의 알림 미지원/등록 실패 피드백에서 브라우저/권한 구현 기준을 제거하고 기기/알림 설정 기준으로 안내. 관리자 알림 준비 설명도 클릭 흐름 중심으로 정리 | `npm run test:notification-readiness`, `npm run test:p5-p10-internal-readiness`, `npm run lint`, `npm run build` |
| 2026-06-20 | Codex | local | 운영 빌드 계정 전환 안정화 | production `/select-role`은 역할 바로 시작 버튼 대신 이메일 로그인 안내를 보여주며 개발/테스트 역할 선택 QA는 유지 | `npm run test:p5-p10-internal-readiness`, `npm run test:auth-production-guard`, production shortcut guard와 demo login 차단 정책 확인 |
| 2026-06-20 | Codex | local | 비밀번호 재설정 예시 이메일 제거 | `/reset-password` 입력 placeholder를 한국어 안내로 바꿔 사용자 화면에 더미 이메일 도메인이 보이지 않게 함 | `npm run test:p5-p10-internal-readiness`, `/reset-password` visible copy에서 `name@example.com` 재유입 차단 |
| 2026-06-20 | Codex | local | 회원 초대 폼 더미/권한 문구 정리 | `/app/members` 초대 이메일 placeholder와 실패 피드백에서 예시 도메인, 지점 권한 표현을 제거하고 담당/선택 지점 표현으로 정리 | `npm run test:p5-p10-internal-readiness`, members visible copy에서 `name@example.com`, `지점 권한`, `자기 지점 범위` 재유입 차단 |
| 2026-06-20 | Codex | local | 회원 연락처 더미 placeholder 제거 | `/app/members` 신규 회원/회원 프로필 연락처 입력에서 `010-0000-0000` 더미 번호를 제거하고 `연락 가능한 번호` 안내로 정리 | `npm run test:p5-p10-internal-readiness`, members visible copy에서 더미 연락처 재유입 차단 |
| 2026-06-20 | Codex | local | 로그인 개발용 계정 채우기 도구 제거 | `/login`의 개발용 계정 이메일/공통 비밀번호 채우기 UI를 제거하고 `quickLogin=1` 역할 바로 시작만 유지 | `npm run test:p5-p10-internal-readiness`, login client copy에서 `.test` 이메일과 기본 비밀번호 재유입 차단 |
| 2026-06-20 | Codex | iOS Simulator | 로그인 quickLogin 빈 화면 안정화 | `/login?quickLogin=1` iOS WebView 빈 화면 원인이 hydration mismatch임을 확인하고 mount 이후 역할 버튼 활성화로 수정 | `npm run test:p5-p10-internal-readiness`, iPhone 16e Simulator 재검증 |
| 2026-06-20 | Codex | local | 앱 안내문/내부 실행 문구 추가 정리 | 코치/대표/결제/공지/관리자 사용자 화면에서 반복 소개문과 내부 실행 명령형 데이터를 줄이고, Capacitor iOS 계정 화면의 PWA 설치 안내, 관리자 운영 이슈 로그의 내부 단계 코드/영문 placeholder, 계정 화면 지점명, 권한 화면 변경 기록 내부 코드를 정리. 학부모/코치/대표/계정/관리자 설정 iPhone 16e Simulator 캡처를 `.data/mobile-builds/ios/dummy-cleanup-stability`에 보관 | `npm run lint`, `npm run build`, `npm run test:unit`, `npm run test:store`, `npm run test:p3-operations`, `npm run test:p5-p10-internal-readiness`, `npm run ios:ipa:doctor`, `npm run p1:readiness` |
| 2026-06-20 | Codex | local | 총괄 초대 폼 예시 도메인/지점 범위 문구 정리 | `/app/admin/users`, `/app/admin/roles` 초대 폼에서 예시 이메일 placeholder와 `전체 지점 권한`, `지점 범위` 표현을 제거하고 모든/담당/선택 지점 표현으로 정리 | `npm run test:p5-p10-internal-readiness`, admin users/roles visible copy에서 예시 도메인과 지점 범위 표현 재유입 차단 |
| 2026-06-20 | Codex | local | 공지 작성 실패 지점 권한 문구 정리 | `/app/notices` 공지 작성 실패 피드백을 대상/지점 확인 문구로 바꿔 사용자에게 내부 권한 표현이 보이지 않게 함. `/app/requests`에는 공지 작성 흐름을 두지 않음 | `npm run test:p5-p10-internal-readiness`, notices/requests visible copy에서 `지점 권한` 재유입 차단 |
| 2026-06-20 | Codex | local | 보호 화면/사용자 목록 권한 문구 정리 | 직접 접근 차단 화면과 `/app/admin/users` 사용자 목록 설명에서 `접근 권한`, `지점 접근 범위`, `권한 코드` 표현을 사용자 친화적인 계정/담당 지점/관리 코드 표현으로 정리 | `npm run test:p5-p10-internal-readiness`, protected route와 admin users visible copy에서 이전 표현 재유입 차단 |
| 2026-06-20 | Codex | local | 요청 기능과 공지 중복 동선 분리 | 회원/학부모 요청 기능 삭제 전 공지함은 상단 공지 버튼과 `/app/notices`로 분리 | `npm run test:e2e`, `npm run test:notification-readiness`, `npm run test:p5-p10-internal-readiness` |
| 2026-06-21 | Codex | local | 회원/학부모 홈 소개문 재유입 가드 강화 | 회원/학부모 `/app/dashboard`에 `회원 홈`, `학부모 홈`, `오늘 요약`, 내부 운영 큐/브리프/리듬 안내가 다시 노출되지 않도록 화면 전체와 정적 소스 기준을 함께 확인 | `npm run test:e2e`, `npm run test:p3-operations`, `npm run test:role-csv-export-gates`, `npm run test:p5-p10-internal-readiness` |
| 2026-06-23 | Codex | local | 대표 운영 보드 장문 소개 제거 | `/app/owner/reports`의 대표 운영 판단/회원 유지/회수/그룹/지점 비교/조기 경보 패널에서 반복 설명 문장을 제거하고 제목, 지표, 다음 확인 중심으로 유지 | `npm run test:p5-p10-internal-readiness`, `npm run test:p3-operations` |
| 2026-06-23 | Codex | local | 공통 대시보드 역할명 홈 제목 제거 | 총괄/코치 공통 `/app/dashboard` 상단 제목을 동적 `역할명 홈` 대신 `대시보드`로 단순화해 첫 화면 소개문 밀도를 낮춤 | `npm run test:p5-p10-internal-readiness`, `npm run lint`, `npm run build` |
| 2026-06-23 | Codex | local | 요청 기능 혼합 제목 제거 | 요청 기능 삭제 전 `/app/requests`와 공지 확인 동선을 분리 유지 | `npm run test:p5-p10-internal-readiness`, 모바일 브라우저 `/app/requests` |
| 2026-06-24 | Codex | local | 회원/학부모 요청 흐름 행 압축 | 요청 기능 삭제 전 `/app/requests` 요청 내역을 단일 흐름 행으로 표시하고 카드 높이 가드를 낮춰 모바일 첫 화면 밀도를 줄임 | `npm run test:visible-app-copy-stability`, `npm run test:p5-p10-internal-readiness`, 390px 모바일 스크린샷 |
| 2026-06-24 | Codex | local | 회원/학부모 결제 필터 한 줄 압축 | `/app/payments` 가족 역할 필터에서 visible `보기` 제목과 큰 필터 카드 느낌을 제거하고 `결제 N건 + 전체/주의/완료` 한 줄 44px 필터로 압축. 금액/CSV 비노출은 유지 | `npm run lint`, `npm run build`, `npm run test:visible-app-copy-stability`, `npm run test:p5-p10-internal-readiness`, iPhone 16e Simulator |
| 2026-06-23 | Codex | local | 관리자 지점 선택 시스템 문구 제거 | `/app/admin/settings` 운영 로그/이슈 지점 선택과 기록 배지가 `전체 지점`으로 표시되고 `전체/시스템` 문구가 앱 화면에 남지 않음 | `npm run test:p5-p10-internal-readiness`, 관리자 설정 모바일 확인 |
| 2026-06-23 | Codex | local | 관리자 설정 제목 앱용 정리 | `/app/admin/settings` 상단 제목과 요약 영역이 `운영 설정`으로 표시되고 `시스템 설정` 제목이 앱 화면에 남지 않음 | `npm run test:p5-p10-internal-readiness`, 관리자 설정 모바일 확인 |
| 2026-06-23 | Codex | local | 코치 수업 화면 내부 운영 용어 정리 | `/app/classes` 코치 화면의 `레일/큐/보드/리듬/기록 압축` 제목이 빠른 처리, 준비, 확인, 후속 조치, 기록 정리 중심의 앱 문구로 표시됨 | `npm run test:p5-p10-internal-readiness`, `npm run test:p3-operations`, 코치 수업 모바일 확인 |
| 2026-06-23 | Codex | local | 대표 리포트 상단 내부 운영 용어 정리 | `/app/owner/reports` 상단 운영 패널 제목이 `보드/큐/리듬/코호트` 대신 운영 판단, 그룹, 경보, 약속, 흐름, 결정, 실험, 리뷰 중심으로 표시됨 | `npm run test:p5-p10-internal-readiness`, `npm run test:p3-operations`, 대표 리포트 모바일 확인 |
| 2026-06-23 | Codex | local | 대표 리포트 하단 내부 운영 용어 정리 | `/app/owner/reports` 하단 표준화/확산/재교육/성과 패널의 `보드/큐/코호트/확인 기준` 표현이 마감 점검, 표준화, 표준 확산 확인, 재교육, 성과 액션 확인, 대상 그룹, 확인 기준으로 표시됨 | `npm run test:p5-p10-internal-readiness`, `npm run test:p3-operations`, 대표 리포트 모바일 확인 |
| 2026-06-23 | Codex | local | 회원/학부모 결제 화면 압축 | `/app/payments` 회원/학부모 화면은 금액 요약 카드와 운영 보드 없이 회원권 상태 리스트만 표시하고 CSV 내보내기, 운영 액션 큐, 원화 금액을 노출하지 않음 | `npm run test:role-csv-export-gates`, `npm run test:p5-p10-internal-readiness`, Browser 390px member/guardian, iPhone 16e Simulator guardian |
| 2026-06-23 | Codex | local | 코치 저장 상태 패널 모바일 압축 | `/app/classes` 모바일 하단 저장 상태 패널은 기본 상태에서 한 줄 출석 요약과 저장 대기/처리율만 보여 출석 필터와 명단을 덜 가리고, 저장 대기 없음 상태는 `정상`으로 표시하며, 대기 저장/되돌리기 상황에서만 버튼 영역이 확장됨 | `npm run test:p5-p10-internal-readiness`, Browser 390px coach classes, iPhone 16e Simulator coach |
| 2026-06-23 | Codex | local | 대표 리포트 내부 약어 정리 | `/app/owner/reports` 표준 확산/재교육/정착 패널이 `lifecycle`, `SLA`, `예시`, `캡처`, `템플릿` 대신 결제 상태 변경 기록, 처리 기한, 미처리 없음 확인, 표준 절차 같은 앱용 문구를 사용함 | `npm run test:p5-p10-internal-readiness`, `npm run test:p3-operations` |
| 2026-06-23 | Codex | local | 관리자 설정 내부 명령어 노출 순화 | `/app/admin/settings` 모바일 화면에 `npm run`, `doctor`, `handoff`, `P1 release blocked`, `검증 명령` 같은 내부/릴리즈 문구가 보이지 않고 운영 설정, 자동 검증, 점검, 외부 준비 대기 문구로 표시됨. P1 외부 blocker 7개는 그대로 blocked 유지 | `npm run test:p5-p10-internal-readiness`, Browser 390px 관리자 설정, `.data/mobile-builds/ios/admin-settings-copy-cleanup-20260623/admin-settings-mobile-copy-cleanup.png` |
| 2026-06-23 | Codex | local | 관리자 설정 요약 카드 모바일 압축 | `/app/admin/settings` 390px 모바일 첫 화면에서 역할/지점/기록 정책/출석 사유 정책 요약이 2열로 보이고, 역할/권한 기준 섹션이 같은 첫 화면에 진입함. 가로 overflow와 내부 릴리즈 문구 노출 없음 | `npm run test:p5-p10-internal-readiness`, Browser 390px 관리자 설정, `.data/mobile-builds/ios/admin-settings-summary-compact-20260623/admin-settings-summary-compact-browser.png` |
| 2026-06-23 | Codex | local | 코치 수업 흐름 문구 압축 | `/app/classes` 코치 빠른 조치와 수업 흐름 카드가 사유 입력 대기, 상담/주의 건수, 저장 대기, 미처리/사유 메모 상태 중심으로 짧게 표시되고 결제 금액은 노출되지 않음 | `npm run test:p5-p10-internal-readiness`, Browser 390px 코치 수업, `.data/mobile-builds/ios/coach-classes-flow-copy-20260623/coach-classes-flow-copy-browser.png` |
| 2026-06-23 | Codex | local + iOS Simulator | 회원/학부모 결제 운영 필터 숨김 강화 | `/app/payments` 회원/학부모 화면의 보기 선택지에서 부분 환불, 전액 환불, 취소가 보이지 않고 숨김 필터 상태는 전체 보기로 복구됨. 대표/총괄 운영 필터와 환불/취소 기능은 유지 | `npm run test:role-csv-export-gates`, `npm run test:p5-p10-internal-readiness`, Browser 390px member/guardian, iPhone 16e Simulator member, `.data/mobile-builds/ios/member-guardian-payment-filter-20260623` |
| 2026-06-23 | Codex | local | 회원 결제 빈 상태 증빙 정합성 갱신 | `/app/payments` 회원 화면에서 결제 완료 보기의 빈 상태가 `결제 0건`, `다른 보기를 선택해 주세요.`로 보이고 부분 환불/전액 환불/취소, 회원권 요약 카드, 납부 예정/만료 금액 카드가 보이지 않음 | `npm run test:p5-p10-internal-readiness`, Browser 390px member payments empty filter, `.data/mobile-builds/ios/compact-empty-copy-20260621/member-payments-filter-empty-compact-copy-browser.png` |
| 2026-06-23 | Codex | local | 코치 회원 상담/주의 증빙 정합성 갱신 | `/app/members` 코치 화면의 상담/주의 메모 작성 기능은 44px `작성` 토글 뒤에 접혀 있고, 기본 목록은 상담/주의 메모 헤더와 최근 메모 요약을 표시한다. `공개 범위`, `저장 후 선택한 대상에게 공유됩니다.`, `공개된 상담/주의 메모가 없습니다.`는 보이지 않음. 회원 검색 입력도 모바일에서 작동 | `npm run test:p5-p10-internal-readiness`, Browser 390px coach members, `.data/mobile-builds/ios/visible-text-audit-20260621-recheck/coach-app_members.png` |
| 2026-06-23 | Codex | local | 관리자 설정 주간/피드백 내부 제목 정리 | `/app/admin/settings` 주간 리뷰/피드백 영역이 `P3 ... 보드`, `SLA 레인`, `증거 패킷`, `P3 백로그 후보` 대신 주간 리뷰 액션 추적, 주간 리뷰 효과 확인, 운영 자동화 실패 복구, 현장 피드백 분류, 피드백 정리 흐름, 피드백 접수, 피드백 QA 반영 일정, 피드백 증거 묶음, 피드백 회고 액션, 피드백 운영 반영, 피드백 실행 확인, 피드백 재발 방지, 피드백 응답 기준, 피드백 운영 지표, 분류별 운영 지표, 노화 창, 백로그 신호, 운영 판단, 개선 후보 중심으로 표시됨 | `npm run test:p5-p10-internal-readiness`, `npm run test:p3-operations`, Browser 390px 관리자 설정 |
| 2026-06-24 | Codex | local + iOS Simulator | 회원/학부모 계정 상태 카드 압축 | `/app/account` 회원/학부모 활성 계정에서 `상태: 활성` 카드를 숨기고 유형/지점 2칸 요약과 계정 전환/로그아웃 44px 액션만 유지. 초대 대기처럼 조치가 필요한 상태만 상태 카드로 표시 | `npm run test:visible-app-copy-stability`, `npm run test:p5-p10-internal-readiness`, iPhone 16e Simulator `.data/mobile-builds/ios/account-status-compact-20260624/member-account-ios-sim.jpg` |
| 2026-06-24 | Codex | local | 대표 리포트 내부 운영 패널 비노출 가드 | `/app/owner/reports` 대표 화면에서 지점별 운영 그래프는 유지하되 내부 P3 운영 패널과 코드형 운영 확인 문구가 실제 앱 DOM에 렌더링되지 않는지 visible copy scan에 추가 | `npm run test:visible-app-copy-stability`, `npm run test:p5-p10-internal-readiness` |
| 2026-06-24 | Codex | local + iOS Simulator | 관리자 사용자 수정 비밀번호 딥링크 확인 | `/app/admin/users#edit-user-owner`가 대표 사용자 수정 패널을 바로 열고, 이름/이메일 아래 `비밀번호 변경` 섹션과 새 비밀번호/확인 입력을 iPhone 16e 화면에서 표시. visible-copy 스캔이 같은 해시 딥링크와 `.test` 이메일 blank 상태를 자동 검증 | `npm run lint`, `npm run build`, `npm run test:admin-user-management-api`, `npm run test:visible-app-copy-stability`, `npm run test:p5-p10-internal-readiness`, iPhone 16e Simulator `.data/mobile-builds/ios/admin-users-password-edit-20260624-latest/admin-users-password-edit-ios-sim.jpg` |
| 2026-06-24 | Codex | Browser 390px + iOS Simulator | 관리자 사용자 초대 카드 압축 | `/app/admin/users` 닫힌 초대 카드를 목록 헤더의 `초대` 버튼으로 압축하고 역할 필터를 1줄 4칩으로 줄여 첫 사용자 행의 수정/삭제/재발급 세로 액션이 하단 내비 위에 보임. 초대 폼은 버튼을 열 때만 표시되고 이름/이메일/역할 입력은 유지 | Browser `mobile-bottom-navigation` 겹침 0건, iPhone 16e `.data/mobile-builds/ios/admin-users-invite-compact-20260624/admin-users-invite-compact-ios-sim.jpg`, `npm run lint`, `npm run build`, `npm run test:visible-app-copy-stability`, `npm run test:p5-p10-internal-readiness` |
| 2026-06-24 | Codex | Browser 390px + iOS Simulator | 회원/학부모 수업 카드 압축 | `/app/classes` 회원/학부모 수업 카드를 136px compact 카드로 낮추고 자녀/본인 출석 칩을 48px로 정리. 날짜, 시간, 장소, 코치, 단계, 출석 상태는 유지하고 코치/관리자 출석 조작 UI는 변경하지 않음 | Browser member/guardian `familyClassCardMaxHeight=136`, `familyAttendanceChipMaxHeight=48`, iPhone 16e `.data/mobile-builds/ios/family-class-card-dense-20260624/guardian-classes-card-dense-ios-sim.jpg`, `npm run lint`, `npm run test:visible-app-copy-stability`, `npm run test:p5-p10-internal-readiness` |
| 2026-06-24 | Codex | Browser 390px | 관리자 사용자 수정 저장바 폭 제한 | `/app/admin/users` 사용자 수정 폼의 저장/취소 바를 전체 화면 fixed overlay에서 폼 내부 sticky 액션바로 바꿔 비밀번호 변경 섹션이 열린 모바일 화면에서도 하단 내비를 가리지 않음 | Browser `adminUserEditActionBarWidth=298`, `adminUserEditFormWidth=324`, `adminUserEditActionButtonMaxBottom=752`, `adminUserMobileBottomNavTop=767`, `npm run test:visible-app-copy-stability`, `npm run test:p5-p10-internal-readiness` |
| 2026-06-24 | Codex | Browser 390px + iOS Simulator | 학부모 학습 리포트 상태값 정리 | `/app/dashboard` 학부모 학습 리포트의 단계/피드백/심사/대회 값을 `목표 주황띠`, `최근 1건`, `통과 안내`, `참가 안내`처럼 보호자가 바로 이해할 수 있는 상태 문구로 표시하고 `2개 반`, `공지 있음` 같은 애매한 값은 노출하지 않음 | `npm run test:visible-app-copy-stability`, `npm run test:p5-p10-internal-readiness`, iPhone 16e Simulator `.data/mobile-builds/ios/guardian-learning-status-values-20260624/guardian-dashboard-learning-status-ios-sim.jpg` |
| 2026-06-24 | Codex | Browser 390px + iOS Simulator | 관리자 사용자 삭제 보호 문구 압축 | `/app/admin/users` 보호 계정 행의 긴 `삭제 제한` 사유 문장을 기본 목록에서 `삭제 보호 N개` 칩으로 줄이고, 상세 사유는 삭제 버튼 접근성 설명과 title에 유지 | `npm run test:visible-app-copy-stability`, `npm run test:p5-p10-internal-readiness`, iPhone 16e Simulator `.data/mobile-builds/ios/admin-users-delete-protection-chip-20260624/admin-users-delete-protection-chip-ios-sim.jpg` |
| 2026-06-24 | Codex | Browser 390px + iOS Simulator | 관리자 사용자 검색/역할 필터 복구성 | `/app/admin/users` 검색어와 역할 필터를 URL `q`/`role`에 반영하고, 검색 결과가 없을 때 빈 목록 대신 `조건에 맞는 사용자가 없습니다.`와 전체 보기 액션을 표시 | `npm run test:visible-app-copy-stability`, `npm run test:p5-p10-internal-readiness`, Browser `.data/mobile-builds/ios/visible-app-copy-stability/admin-users-empty-filter-state.png`, iPhone 16e `.data/mobile-builds/ios/admin-users-filter-state-20260624/admin-users-empty-filter-ios-sim.jpg` |
| 2026-06-24 | Codex | Browser 390px + iOS Simulator | 관리자 변경 기록 필터 접힘형 | `/app/admin/audit-logs`의 검색 필터를 기본 접힘 패널로 바꾸고 적용 조건 칩과 44px `필터 열기` 버튼만 첫 화면에 표시해 조회 결과와 변경 기록 목록이 먼저 보이도록 정리 | Browser `panelCount=1`, `fieldsVisible=0`, `chipCount=4`, console error 0, iPhone 16e `.data/mobile-builds/ios/admin-audit-filter-compact-20260624/admin-audit-filter-collapsed-ios-sim.jpg`, `npm run test:visible-app-copy-stability`, `npm run test:p5-p10-internal-readiness` |
| 2026-06-24 | Codex | Browser 390px + iOS Simulator | 관리자 지점 요약 카운트 정리 | `/app/admin/branches` 상단 요약의 `운영 현황 6/5`처럼 회원/수업 총량으로 읽히는 값을 `운영 중 2` 지점 상태 카운트로 바꿔 전체 지점/대표 미배정/비활성 지점과 같은 기준으로 정렬 | Browser `activeSummaryText=운영 중2`, slash count 0, 지점 생성 토글 닫힘→열림→닫힘, console error 0, iPhone 16e `.data/mobile-builds/ios/admin-branch-summary-20260624/admin-branches-active-summary-ios-sim.jpg`, `npm run test:visible-app-copy-stability`, `npm run test:p5-p10-internal-readiness` |
| 2026-06-24 | Codex | Browser 390px | 총괄 권한 최근 기록 노이즈 제거 | `/app/admin/roles`의 요약 카운트와 `최근 계정 변경` 목록을 계정·권한 관련 로그로 한정해 `변경 기록을 조회했습니다.` 같은 조회 로그가 반복 노출되지 않게 정리 | Browser `recentReadLogCount=0`, `최근 계정 변경` 목록 확인, console error 0, `npm run test:visible-app-copy-stability`, `npm run test:p5-p10-internal-readiness` |
| 2026-06-24 | Codex | Browser 390px + iOS Simulator | 관리자 사용자 목록 가로 액션 압축 | `/app/admin/users` 모바일 사용자 카드의 수정/삭제/재발급 버튼을 가로 3버튼으로 바꾸고, 카드 하단의 담당 지점 안내와 `삭제 보호` visible chip을 제거해 행 높이와 빈 여백을 줄임 | Browser `firstStackWidth=140`, `buttonTops=523/523/523`, `mobileScopeSummaryCount=0`, `visibleDeleteHelperCount=0`, console error 0, iPhone 16e `.data/mobile-builds/ios/admin-users-horizontal-actions-20260624/admin-users-horizontal-actions-ios-sim.jpg`, `npm run test:visible-app-copy-stability`, `npm run test:p5-p10-internal-readiness` |
| 2026-06-24 | Codex | Browser 390px + iOS Simulator | 관리자 역할 목록 행 압축 | `/app/admin/roles` 모바일 사용자/역할 목록에서 역할 배지와 변경 버튼을 가로 compact row로 정리하고, 담당 지점/관리범위 설명과 긴 계정 보호 안내는 모바일 첫 흐름에서 제거해 카드 여백을 줄임 | Browser `adminRoleActionRowMaxWidth<=140`, `managementScopeVisibleCount=0`, `branchScopeVisibleCount=0`, `protectionLongCopyPresent=false`, console error 0, iPhone 16e `.data/mobile-builds/ios/admin-roles-compact-row-20260624/admin-roles-compact-row-ios-sim.jpg`, `npm run test:visible-app-copy-stability`, `npm run test:p5-p10-internal-readiness` |
| 2026-06-24 | Codex | Browser 390px + iOS Simulator | 관리자 변경 기록 행 압축 | `/app/admin/audit-logs` 기본 조회를 전체 지점으로 고정하고 모바일 로그 행에서 반복 `공통`/대상 종류 메타를 숨김. 결과 배지는 시간 줄에 배치하고 `변경 전후`는 payload가 있는 로그에만 표시해 목록 스캔 속도를 높임 | Browser `adminAuditNoDetailRowMaxHeight<=150`, `adminAuditListRowMaxHeight<=190`, `branchMetaVisible=0`, `targetMetaVisible=0`, console error 0, iPhone 16e `.data/mobile-builds/ios/admin-audit-compact-rows-20260624/admin-audit-compact-rows-ios-sim.jpg`, `npm run test:visible-app-copy-stability`, `npm run test:p5-p10-internal-readiness` |
| 2026-06-24 | Codex | Browser 390px + iOS Simulator | 관리자 지점 요약 상태 바 압축 | `/app/admin/branches` 상단 4개 요약 카드를 한 줄 4분할 상태 바로 바꿔 전체/운영 중/미배정/비활성 지점 수는 유지하면서 모바일 첫 화면에서 목록 도달 시간을 줄임 | Browser `adminBranchSummaryGridHeight<=84`, `activeSummaryText=운영 중2`, slash count 0, console error 0, iPhone 16e `.data/mobile-builds/ios/admin-branch-summary-bar-20260624/admin-branches-summary-bar-ios-sim.jpg`, `npm run test:visible-app-copy-stability`, `npm run test:p5-p10-internal-readiness` |
| 2026-06-24 | Codex | Browser 390px + iOS Simulator | 관리자 지점 상단 카드 추가 압축 | `/app/admin/branches` 상단 요약 상태 바와 `지점 생성` 패널을 약 2/3 높이로 줄이고, 생성 버튼은 44px 터치 높이를 유지해 목록 도달성과 조작성을 같이 보존 | Browser `summaryHeight=59`, `createPanelHeight=58`, `createToggleHeight=44`, console error 0, iPhone 16e `.data/mobile-builds/ios/admin-branch-card-compact-20260624/admin-branches-card-compact-ios-sim.jpg`, `npm run test:visible-app-copy-stability`, `npm run test:p5-p10-internal-readiness` |
| 2026-07-04 | Codex | Browser 390px + iOS Simulator | 관리자 지점 생성 폼 열림 상태 압축 | `/app/admin/branches`의 `지점 생성` 열림 폼에서 보이는 라벨을 접근성 라벨로 남기고 입력 placeholder 중심의 2행 폼으로 정리해 목록이 더 빨리 보이게 함. 생성 토글과 생성 버튼은 44px 터치 기준을 유지하고 지점명/지역 입력은 서버와 같은 80/100자 상한을 사용 | Browser `adminBranchCreateOpenPanelHeight<=160`, `adminBranchCreateOpenFormHeight<=112`, `adminBranchCreateOpenInputMaxLengths=[80,100]`, `adminBranchCreateOpenSubmitButtonHeight=44`, `.data/mobile-builds/ios/visible-app-copy-stability/visible-app-copy-stability-report.json`, iPhone 16e `.data/mobile-builds/ios/admin-branch-create-compact-20260704/admin-branches-create-compact-ios-sim.png`, `npm run test:visible-app-copy-stability`, `npm run test:p5-p10-internal-readiness` |
| 2026-07-05 | Codex | Browser 390px + iOS Simulator | 관리자 지점 운영 설정 터치 안정화 | `/app/admin/branches`의 운영 설정 폼에서 지점명/지역/상태/시간대 입력과 출석 수정 정책 토글을 44px 터치 높이로 맞추고, 저장 버튼과 하단 내비 clearance를 유지 | Browser `fieldGridMinControlHeight=44`, `policyControlHeight=44`, `saveHeight=44`, `formHeight=201`, `cardHeight=279`, iPhone 16e `.data/mobile-builds/ios/admin-branch-settings-touch-20260705/admin-branches-settings-touch-ios-sim.png`, `npm run test:visible-app-copy-stability`, `npm run test:p5-p10-internal-readiness` |
| 2026-07-05 | Codex | Browser 390px + iOS Simulator | 인증 계정 전환 터치 안정화 | `/login`의 로그인된 계정 전환 액션과 `/select-role`의 로그인 화면 링크를 44px 터치 높이로 맞추고, 쿠키만 남은 앱 재시작 상태에서도 `/login`이 현재 세션을 복원하도록 함 | Browser `accountSwitchLayout.buttonHeight=44`, `cookieOnlyAccountSwitch.layout.buttonHeight=44`, `authSelectRoleLoginLinkHeight=44`, iPhone 16e `.data/mobile-builds/ios/auth-account-switch-touch-20260705/login-account-switch-ios-sim.png`, `npm run test:login-keep-signed-in`, `npm run test:visible-app-copy-stability`, `npm run test:auth-production-guard`, `npm run test:p5-p10-internal-readiness` |
| 2026-06-24 | Codex | Browser 390px + iOS Simulator | 코치 출석 메모 기본 접힘 | `/app/classes` 코치 출석 명단의 회원별 `현장 메모` 입력과 빠른 메모 칩을 기본 접힘으로 두고 `메모` 토글을 눌렀을 때만 열리게 정리. 지각/결석 등 사유가 필요한 상태는 메모 영역을 자동으로 열어 현장 입력 흐름을 유지 | Browser `coachClassAttendanceNoteToggleCount>0`, `coachClassAttendanceNoteEditorCount < toggleCount`, 입력칸은 열린 editor 안에서만 표시, iPhone 16e `.data/mobile-builds/ios/coach-attendance-note-collapse-20260624/coach-classes-note-collapse-ios-sim.jpg`, `npm run test:e2e`, `npm run test:visible-app-copy-stability`, `npm run test:p5-p10-internal-readiness` |
| 2026-06-26 | Codex | Browser 390px + iOS Simulator | 운영자 공지 작성 폼 기본 접힘 | `/app/notices` 대표/총괄 화면에서 공지 목록과 읽음 처리가 먼저 보이고 `공지 작성` 폼은 44px `작성 열기` 토글 뒤에 접힘. 폼을 열면 제목/내용/대상/발행 입력이 44px 터치 기준과 가로 overflow 0을 유지 | Browser `noticeCreateFormCount=0 -> 1`, `noticeCreateToggleHeight=44`, iPhone 16e `.data/mobile-builds/ios/notice-create-collapse-20260626/admin-notices-create-panel-collapsed-ios-sim.jpg`, `npm run test:visible-app-copy-stability`, `npm run test:notification-readiness`, `npm run test:p5-p10-internal-readiness` |
| 2026-06-26 | Codex | Browser 390px + iOS Simulator | 코치 회원 상담/주의 목록 압축 | `/app/members` 코치 화면에서 빈 주의사항/빈 상담 문구를 반복하지 않고, 최근 상담/주의 메모는 기본 1건만 표시하며 복수 메모는 44px `최근 메모 N건 더보기`로 확장 | Browser `coachMemberNoteListMaxItems=1`, `coachMemberNoteListToggleMinHeight=44`, `coachMemberEmptyAlertCopyCount=0`, `coachMemberEmptyNoteCopyCount=0`, iPhone 16e `.data/mobile-builds/ios/coach-members-compact-notes-20260626/coach-members-compact-notes-ios-sim.jpg`, `npm run test:visible-app-copy-stability`, `npm run test:p5-p10-internal-readiness` |
| 2026-07-04 | Codex | Browser 390px + iOS Simulator | 회원 상세 빈 상태 문구 절제 | `/app/members` 관리자/대표/코치/회원/학부모 상세 카드에서 주의사항/상담/코치 피드백 데이터가 없을 때 별도 빈 문구를 반복하지 않고 `0건` 요약과 작성/수정 액션만 유지 | Browser `admin-members`, `owner-members`, `coach-members`, `member-members`, `guardian-members`의 `memberProfileEmptyAlertCopyCount=0`, `memberProfileEmptyNoteCopyCount=0`, iPhone 16e `.data/mobile-builds/ios/member-empty-copy-quiet-20260704/member-empty-copy-quiet-ios-sim.png`, `npm run test:member-profile-guardian-edit`, `npm run test:visible-app-copy-stability`, `npm run test:p5-p10-internal-readiness` |
| 2026-07-04 | Codex | Browser 390px + iOS Simulator | 결제 상세 납부 요약 압축 | `/app/payments/checkout` 성인 회원/학부모 결제 상세 상단의 금액/상태/대상/기간 요약을 2열 compact grid로 바꿔 결제자 정보가 첫 모바일 viewport 안에 더 빨리 보이게 함 | Browser `summaryHeight=235<=245`, `summaryGridHeight=143<=150`, `summaryGridRowMinHeight=65`, `payerInfoTop=441<=480`, iPhone 16e `.data/mobile-builds/ios/payment-checkout-payer-compact-20260704/payment-checkout-payer-compact-ios-sim.png`, `npm run test:payment-checkout-method-flow`, `npm run test:p5-p10-internal-readiness` |
| 2026-07-04 | Codex | Browser 390px + iOS Simulator | 결제자 추가 정보 기본 접힘 | `/app/payments/checkout` 성인 회원/학부모 결제 상세의 주소·일반전화·이메일 입력을 `주소·이메일 추가` 토글 뒤로 접어 이름/휴대전화 입력 후 결제수단이 더 빨리 보이게 함. 펼침 상태에서는 기존 주소검색/이메일/일반전화 입력을 유지 | Browser `payerInfoHeight=271<=280`, `methodSectionTop=720<=730`, optional details 기본 0건, optional toggle 44px 이상, iPhone 16e `.data/mobile-builds/ios/payment-checkout-payer-compact-20260704/payment-checkout-payer-compact-ios-sim.png`, `npm run test:payment-checkout-method-flow`, `npm run test:p5-p10-internal-readiness` |
| 2026-07-05 | Codex | Browser 390px + iOS Simulator | 결제 주소 검색 안내 구현어 제거 | `/app/payments/checkout` 주소검색 피드백에서 `연동 전` 같은 구현 상태 표현을 제거하고 `우편번호와 주소를 직접 입력해 주세요.`로 안내 | `npm run test:payment-checkout-method-flow`, `npm run test:p5-p10-internal-readiness`, iPhone 16e `.data/mobile-builds/ios/payment-checkout-address-copy-20260705/payment-checkout-address-copy-ios-sim.png` |
| 2026-07-05 | Codex | Browser 390px + iOS Simulator | 결제 계좌 안내 구현어 제거 | `/app/payments/checkout` 가상계좌/계좌이체 안내에서 `운영 결제 설정`, `전용 화면`처럼 내부 설정으로 보이는 문구를 제거하고 `도장 안내 후 입금·인증 절차`로 안내. `method=accountTransfer#payment-account-method-panel` 딥링크로 해당 안내 위치를 바로 확인 | Browser `virtualAccountPanel`/`accountTransferPanel` copy, `accountTransferDeepLinkPanel.top=404`, iPhone 16e `.data/mobile-builds/ios/payment-checkout-account-method-copy-20260705/payment-checkout-account-transfer-ios-sim.png`, `npm run test:payment-checkout-method-flow`, `npm run test:p5-p10-internal-readiness` |
| 2026-07-04 | Codex | Browser 390px + iOS Simulator | 결제 준비 문구 오인 방지 | `/app/payments/checkout` 마지막 액션을 `결제 진행하기`에서 `납부 정보 확인`으로 바꾸고 하단 안내를 `납부 정보 접수`로 낮춰 실 PG/VAN 연결 전 화면이 실제 승인 완료처럼 보이지 않게 함 | Browser `confirmButtonText=납부 정보 확인`, provider status에 `납부 정보 접수`/`담당자가 확인 후 안내`, iPhone 16e `.data/mobile-builds/ios/payment-checkout-preapproval-copy-20260704/payment-checkout-preapproval-copy-ios-sim.png`, `npm run test:payment-checkout-method-flow`, `npm run test:p5-p10-internal-readiness` |
| 2026-07-05 | Codex | Browser 390px + iOS Simulator | 결제 접수 문구 최종 정리 | `/app/payments/checkout` 하단 안내와 확인 피드백을 `납부 정보 접수`/`담당자가 확인 후 안내` 기준으로 맞추고, 우리WON페이 모달은 앱 선택 참고 안내로 낮춰 실제 결제 승인처럼 보이지 않게 함 | Browser summary `.data/mobile-builds/ios/payment-checkout-receipt-copy-20260705/summary.json`, iPhone 16e `.data/mobile-builds/ios/payment-checkout-receipt-copy-20260705-ios/payment-checkout-receipt-copy-ios-sim.png`, `npm run test:family-payment-checkout`, `npm run test:payment-checkout-method-flow`, `npm run test:visible-app-copy-stability`, `npm run test:p5-p10-internal-readiness`, `npm run lint`, `npm run build` |
| 2026-07-05 | Codex | Browser 390px + iOS Simulator | 알림함 결제 pending 문구 정리 | `/app/notifications` 결제 알림의 pending 상태를 실제 결제 진행처럼 보이지 않게 `납부 정보 확인 필요`/`납부 확인 중` 톤으로 정리 | Browser summary `.data/mobile-builds/ios/notification-payment-copy-20260705/summary.json`의 guardian case `paymentTitles=한유나 납부 정보 확인 필요`, `paymentActionLabels=납부 확인 중`, iPhone 16e `.data/mobile-builds/ios/notification-payment-copy-ios-20260705/guardian-notifications-payment-copy-ios-sim.png`, `npm run test:mobile-notification-inbox`, `npm run test:notification-readiness`, `npm run test:family-payment-checkout`, `npm run test:p5-p10-internal-readiness` |
| 2026-07-05 | Codex | Browser 390px + iOS Simulator | 가족 결제 준비 CTA 문구 통일 | 성인 회원/학부모가 열 수 있는 결제 준비 CTA를 `납부 정보 확인`으로 통일해 실 PG/VAN 연결 전 `결제하기`처럼 보이는 live-payment 오인을 제거 | Browser summary `.data/mobile-builds/ios/notification-payment-copy-20260705/summary.json`의 `paymentActionLabels=납부 정보 확인, 납부 확인 중`, iPhone 16e `.data/mobile-builds/ios/notification-payment-copy-ios-20260705/guardian-notifications-payment-copy-ios-sim.png`, `npm run test:family-payment-checkout`, `npm run test:mobile-notification-inbox`, `npm run test:notification-readiness`, `npm run test:p5-p10-internal-readiness` |
| 2026-07-05 | Codex | Browser 390px + iOS Simulator | 결제 목록 CTA 문구 증빙/가드 | `/app/payments` 회원/학부모 결제 목록 카드와 `/app/notifications` 결제 알림 링크가 `결제하기` 대신 `납부 정보 확인` 계열 문구를 쓰는지 실제 DOM으로 검증 | Browser report `.data/mobile-builds/ios/visible-app-copy-stability/visible-app-copy-stability-report.json`의 `memberPaymentCheckoutActionTexts=납부 정보 확인...`, iPhone 16e `.data/mobile-builds/ios/payment-list-cta-copy-ios-20260705/member-payments-cta-copy-ios-sim.png`, `npm run test:family-payment-checkout`, `npm run test:visible-app-copy-stability`, `npm run test:p5-p10-internal-readiness` |
| 2026-07-05 | Codex | Browser 390px + iOS Simulator | 초대 링크 복사 실패 안내/초대 폼 진입점 | `/app/admin/roles?invite=1`로 총괄 권한 화면의 사용자 초대 폼을 바로 열 수 있고, 초대 링크 복사 실패 시 주소창 복사 안내 대신 `복사를 완료하지 못했습니다. 링크 열기로 확인해 주세요.`를 표시 | Browser summary `.data/mobile-builds/ios/invite-link-copy-feedback-20260705/summary.json`의 `inviteQueryOpen=true`, `hasOldAddressCopy=false`, iPhone 16e `.data/mobile-builds/ios/invite-link-copy-feedback-ios-20260705/admin-role-invite-open-ios-sim.png`, `npm run test:invite-link-copy-feedback`, `npm run test:p5-p10-internal-readiness` |
| 2026-07-05 | Codex | Browser 390px + iOS Simulator | 총괄 초대 폼 제출 버튼 하단 여백 | `/app/admin/roles?invite=1#admin-role-invite-submit`로 진입하면 사용자 초대 폼이 열린 뒤 제출 버튼 위치까지 안정적으로 스크롤되고, 버튼이 모바일 하단 고정 내비게이션 위에 남음 | Browser summary `.data/mobile-builds/ios/admin-role-invite-bottom-safe-area-20260705/summary.json`의 `hashSubmitLayout.submitBottomNavClearance=51`, iPhone 16e `.data/mobile-builds/ios/admin-role-invite-bottom-safe-area-ios-20260705/admin-role-invite-submit-ios-sim.png`, `npm run test:admin-role-invite-bottom-safe-area`, `npm run test:p5-p10-internal-readiness` |
| 2026-07-05 | Codex | Browser 390px + iOS Simulator | 알림함 필터 툴바 단순화 | `/app/notifications` 상단 필터의 중첩 테두리 프레임을 제거하고 평평한 wrapping toolbar로 정리해 목록 카드 안 카드처럼 보이는 밀도를 낮춤 | Browser summary `.data/mobile-builds/ios/notification-filter-toolbar-20260705/summary.json`의 `filterToolbarNestedFrameCount=0`, `filterToolbarOverflow<=0`, iPhone 16e `.data/mobile-builds/ios/notification-filter-toolbar-ios-20260705/guardian-notifications-filter-toolbar-ios-sim.png`, `npm run test:mobile-notification-inbox`, `npm run test:notification-readiness`, `npm run test:p5-p10-internal-readiness` |
| 2026-07-05 | Codex | Browser 390px + iOS Simulator | 공지/알림 하단 탭 라벨 분리 | `/app/notices`에서는 active 하단 탭이 `공지`와 `/app/notices`로 보이고, `/app/notifications`와 일반 가족 홈에서는 `알림`과 `/app/notifications`로 보여 공지 화면과 알림함 의미가 섞이지 않게 함 | Browser report `.data/mobile-builds/ios/visible-app-copy-stability/visible-app-copy-stability-report.json`의 `admin/owner/coach/member/guardian-notices noticeLabel=공지 noticeHref=/app/notices`, `member/guardian-notifications noticeLabel=알림 noticeHref=/app/notifications`, iPhone 16e 증빙 `.data/mobile-builds/ios/notice-notification-nav-label-20260705/`, `npm run test:visible-app-copy-stability`, `npm run test:p5-p10-internal-readiness` |
| 2026-07-04 | Codex | Browser 390px + iOS Simulator | 결제 상세 카드 안내/저장 피드백 안정화 | `/app/payments/checkout` 신용카드 안내 버튼을 44px로 맞추고 `다음에도 사용` 체크 상태를 확인 피드백에 반영해 저장 여부가 모호하지 않게 함 | Browser `cardGuideButtonMinHeight=44`, `다음 납부에도 사용할 정보로 표시했습니다`, `이번 납부 확인에만 사용합니다`, iPhone 16e `.data/mobile-builds/ios/payment-checkout-save-feedback-20260704/payment-checkout-save-feedback-ios-sim.png`, `npm run test:payment-checkout-method-flow`, `npm run test:p5-p10-internal-readiness` |
| 2026-07-05 | Codex | Browser 390px + iOS Simulator | 결제 운영 액션 터치 안정화 | `/app/payments` 대표/총괄 모바일에서 결제 내보내기/상태 필터/표시 건수와 결제 행 재등록/온라인 요청/정기결제 약정/전액 입력/환불/취소 액션을 44px 터치 높이로 맞춤 | Browser `topControlHeights` 모두 44, `actionControlHeights` 모두 44, iPhone 16e `.data/mobile-builds/ios/payment-create-actions-touch-20260705/payment-actions-touch-ios-sim.png`, `npm run test:payment-create-touch-targets`, `npm run test:p5-p10-internal-readiness` |
| 2026-07-05 | Codex | Browser 390px + iOS Simulator | 수업 생성/수정 터치 안정화 | `/app/classes` 대표 모바일에서 수업 생성/수정 입력과 생성/저장 버튼을 44px로 맞추고, 코치 출석 메모 토글/입력/빠른 메모/저장도 44px 기준으로 통일 | Browser `class create/edit`와 `attendance note` 높이 모두 44 이상, iPhone 16e `.data/mobile-builds/ios/class-management-touch-targets-20260705/`, `npm run test:class-management-touch-targets`, `npm run test:p5-p10-internal-readiness` |
| 2026-07-05 | Codex | Browser 390px + iOS Simulator | 회원 관리 터치 안정화 | `/app/members` 대표 모바일에서 계정 초대/회원 등록 폼 기본 접힘을 유지하고, 초대/등록/상태/기본정보/보호자 검색/상담 메모 입력과 저장 버튼을 44px 기준으로 통일 | Browser `invite/create/status/profile/guardian/note` 높이 모두 44 이상, iPhone 16e `.data/mobile-builds/ios/member-management-touch-targets-20260705/`, `npm run test:member-management-touch-targets`, `npm run test:p5-p10-internal-readiness` |
| 2026-07-05 | Codex | Browser 390px + iOS Simulator | 총괄 사용자 관리 터치 안정화 | `/app/admin/users` 총괄 모바일에서 초대 폼 기본 접힘을 유지하고, 목록 액션과 초대/수정/삭제/비밀번호 재발급 입력 및 저장 버튼을 44px 기준으로 통일 | Browser `invite/edit/delete/password reset` 높이 모두 44 이상, iPhone 16e `.data/mobile-builds/ios/admin-user-management-touch-targets-20260705/`, `npm run test:admin-user-management-touch-targets`, `npm run test:p5-p10-internal-readiness` |
| 2026-06-26 | Codex | Browser 390px + iOS Simulator | 요청 기능/공지 전용 메뉴 회귀 방지 | 요청 기능 삭제 전 코치 `/app/requests`와 코치 `/app/notices`가 전용 화면으로 렌더링되게 visible-copy 검증을 강화 | Browser `requestsScreenCount=1`, `noticesScreenCount=0` on requests, `noticesScreenCount=1`, `requestsScreenCount=0` on notices, `mobileBottomNavRouteIds=dashboard|classes|members|notices|requests`, iPhone 16e `.data/mobile-builds/ios/notice-request-menu-split-20260626/coach-requests-separated-menu-ios-sim.jpg`, `npm run test:visible-app-copy-stability`, `npm run test:p5-p10-internal-readiness` |
| 2026-06-26 | Codex | Browser 390px | 요청/공지 하단 active 메뉴 가드 | 요청 기능 삭제 후 `/app/requests`와 `/requests`는 404이고, `/app/notices`와 `/app/notifications`는 `notices` route만 active/current로 기록된다. 실제 탭 라벨은 `/app/notices`에서 `공지`, `/app/notifications`에서 `알림`으로 분리한다 | Browser `coach notices active=notices`, `member notifications active=notices`, `mobileBottomNavRequestsBadgeCount=0`, deleted request route 404, `npm run test:routes`, `npm run test:visible-app-copy-stability`, `npm run test:p5-p10-internal-readiness` |
| 2026-06-26 | Codex | Browser 390px + iOS Simulator | 코치 대시보드 오늘 운영 추가 압축 | `/app/dashboard` 코치 화면에서 `오늘 운영` compact graph를 156px 높이로 유지해 첫 화면 안에 `오늘 수업` 패널과 수업 항목이 함께 보임. 결제 금액/민감 운영정보는 계속 비노출 | Browser `coachDashboardFlowGraphHeight=156`, `coachDashboardFlowRowMaxHeight=46`, `coachDashboardClassesPanelTop=426`, iPhone 16e `.data/mobile-builds/ios/coach-dashboard-flow-density-20260626/coach-dashboard-flow-density-ios-sim.jpg`, `npm run test:visible-app-copy-stability`, `npm run test:p5-p10-internal-readiness` |
| 2026-06-26 | Codex | Browser 390px + iOS Simulator | 코치 수업 빠른 조치 밀도 압축 | `/app/classes` 코치 화면의 빠른 조치를 44px 버튼 3개 중심으로 줄여 첫 수업 카드가 모바일 첫 화면에 더 빨리 보임. 버튼에는 미처리/사유/주의 숫자가 직접 표시되고 결제 금액은 계속 비노출 | Browser `coachMobileSpeedPanelHeight=58`, `coachMobileSpeedActionButtonMinHeight=44`, `coachFirstClassCardTop=422`, iPhone 16e `.data/mobile-builds/ios/coach-classes-speed-density-20260626/coach-classes-speed-density-ios-sim.jpg`, `npm run test:visible-app-copy-stability`, `npm run test:p5-p10-internal-readiness` |
| 2026-07-04 | Codex | Browser 390px + iOS Simulator | 코치 출석 저장 패널 액션 터치 안정화 | `/app/classes` 코치 출석 변경 후 하단 fixed 저장 상태 패널의 저장 상태 chip, `되돌리기`, pending `재시도` 액션을 44px 이상으로 유지해 수업 현장 한 손 조작 오작동을 줄임. 패널은 하단 내비 위 clearance와 z-index를 유지 | Browser `statusChipHeight>=44`, `undoButtonHeight>=44`, optional `retryButtonHeight>=44`, `panelBottomClearance>=24`, `rosterToggleBottomNavOverlapCount=0`, iPhone 16e `.data/mobile-builds/ios/coach-classes-bottom-safe-area-20260701/ios-sim-summary.json`, `npm run test:coach-classes-bottom-safe-area`, `npm run test:p5-p10-internal-readiness` |
| 2026-06-26 | Codex | Browser 390px + iOS Simulator | 대표 리포트 추세 요약 그래프 레일화 | `/app/owner/reports` 기간별 운영 추세의 4개 요약 카드를 매출/출석/위험/순증 그래프 레일로 바꿔 카드형 반복을 줄임. 기간 필터와 상세 추세 그래프, 내보내기 기능은 유지 | Browser `ownerReportTrendSummaryGridHeight=106`, `ownerReportTrendSummaryRowCount=4`, `ownerReportTrendSummaryRowMaxHeight=20`, `ownerReportGraphBoardHeight=205`, iPhone 16e `.data/mobile-builds/ios/owner-reports-trend-summary-rail-20260626/owner-reports-trend-summary-rail-ios-sim.jpg`, `npm run test:visible-app-copy-stability`, `npm run test:p5-p10-internal-readiness` |
| 2026-07-04 | Codex | Browser 390px + iOS Simulator | 대표 내 지점 하단 내비 안전 여백 | `/app/owner/branches` 지점 목록 끝에 모바일 safe-area spacer를 추가해 마지막 지점 카드의 `결제`/`수업`/`더 보기` 액션이 하단 고정 내비 뒤에 숨지 않게 함 | Browser `ownerBranchBottomSafeAreaHeight=112`, `ownerBranchActionBottomNavClearanceAtScrollEnd=148`, action min height `44`, iPhone 16e `.data/mobile-builds/ios/owner-branches-bottom-safe-area-20260704/summary.json`, `npm run test:visible-app-copy-stability`, `npm run test:p5-p10-internal-readiness` |
| 2026-06-29 | Codex | Browser 369px + iOS Simulator | 결제 등록 회원 검색 선택 | `/app/payments` 대표/총괄 수기 결제 등록에서 회원을 select 스크롤 대신 이름/연락처/보호자 검색으로 찾고, 선택 후 결과 목록을 접어 하단 내비와 겹치지 않게 함. 재등록 prefill은 선택 회원 검색어까지 채움 | `npm run test:payment-lifecycle`, `npm run test:unit`, `npm run test:admin-settings-gates`, lint/build, iPhone 16e `.data/mobile-builds/ios/payment-member-search-20260629/payment-screen-ios-sim-entry.jpg`, 모바일 상호작용 `.data/mobile-builds/ios/payment-member-search-20260629/payment-create-member-selected-mobile-web.jpg`, `playwright-result.json` |
| 2026-06-29 | Codex | Browser 390px + iOS Simulator | 공지 개인 대상 회원 검색 선택 | `/app/notices` 대표/총괄/코치 공지 작성에서 개인 대상 회원을 select 스크롤 대신 이름/연락처/보호자 검색으로 찾고, 선택 후 결과 목록을 접어 하단 내비와 겹치지 않게 함. 검색어를 수정하면 기존 선택을 비우고 다시 결과를 표시 | `npm run test:notification-readiness`, `npm run test:visible-app-copy-stability`, `npm run test:p5-p10-internal-readiness`, Browser/iOS 증빙 `.data/mobile-builds/ios/notice-member-search-20260629` |
| 2026-06-29 | Codex | local | visible-copy 공지 읽음 테스트 격리 | `test:visible-app-copy-stability`가 알림함의 공지 `확인` 버튼을 누르더라도 시작 전/종료 후 개발 DB reset과 공지 읽음 스냅샷 비교로 runtime DB에 `readByUserIds`나 `notice.read` 로그를 남기지 않음 | `npm run test:visible-app-copy-stability`, `npm run test:p5-p10-internal-readiness`, 리포트 `devDataReset.noticeMutationSnapshotRestored=true` |
| 2026-06-29 | Codex | local | visible-copy CLI 실행 안전장치 | `test:visible-app-copy-stability -- --help`가 브라우저 스캔이나 개발 DB reset 없이 도움말만 출력하고, 지원하지 않는 인자는 즉시 실패해 긴 상태 변경 테스트가 실수로 시작되지 않음 | `npm run test:visible-app-copy-stability -- --help`, unknown-arg smoke, `npm run test:p5-p10-internal-readiness`, `git diff --check` |
| 2026-06-14 | Codex | local | 릴리즈 문서 정합성 통과 | 없음 | `npm run test:release-docs`, `package.json` scripts와 README/QA 계획/릴리즈 체크리스트가 `test:release` 실행 목록을 누락 없이 반영하고 README `검증` 명령 블록 순서가 release runner와 일치하는지 확인 |
| 2026-06-14 | Codex | local | 관리자 자동 검증 게이트 정합성 통과 | 없음 | `npm run test:admin-settings-gates`, `/app/admin/settings` 자동 검증 명령과 `test:release` 실행 목록 정합성 확인 |
| 2026-07-14 | Codex | Playwright 390px + iOS Simulator | 회원 상세 결제 도메인 연결 | `/app/members` 상세에서 현재 회원권 상태·납부일·만료일을 결제 도메인에서 표시하고 대표만 금액을 확인한다. 회원·학부모 금액 비노출, 코치 요약 비노출, 퇴회 자녀 결제 이동 차단, 정확한 학부모 자녀·결제 딥링크와 이후 수동 자녀 선택 유지를 확인 | `npm run test:payment-lifecycle`, `npm run test:member-profile-guardian-edit`, `npm run test:member-management-touch-targets`, `.data/mobile-builds/ios/member-payment-summary-20260714/summary.json`, iPhone 16e `member-payment-summary-ios-sim.png` |

## 10. 자동화 후보

- 배포 플랫폼에 맞춘 pipeline upload 단계에서 `.data/pilot-archives/<파일럿명>/pilot-archive-manifest.json`과 archive 폴더를 장기 보관소에 업로드하고, 검증된 `.data/pilot-storage-receipt.json`과 archive manifest를 함께 보관
- 회원/학부모 앱 QA 현재 기준: 회원 `/app/dashboard`는 보조 제목 없이 핵심 상태를 단일 패널로 보여주고, 학부모 `/app/dashboard`는 자녀 학습 리포트를 우선 보여준다. 학부모 리포트의 긴 공지/피드백 문구는 모바일 카드 안에서 축약하고, 다음 행동 큐/오늘 확인 브리프/주간 확인 리듬/복귀 안내 레일/수업 전 준비 보드/확인 리마인드 큐 같은 내부 운영 안내는 노출하지 않는다.
