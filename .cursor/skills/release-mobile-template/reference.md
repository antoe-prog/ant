# 릴리스 — 스토어·내부 배포 전 스모크 (수동)

1. **게이트웨이:** `GET /health`, 변경된 주요 `GET`/`POST` 한 번씩.  
2. **앱:** 콜드 스타트 → 채팅 한 턴(또는 템플릿 응답) → 백그라운드 복귀.  
3. **환경:** 스테이징/프로덕 API URL이 빌드 플래버와 일치하는지 설정 화면에서 확인.  
4. **비용/한도:** 테스트 계정으로 예산·레이트리밋 메시지가 의도대로인지(해당 시).
