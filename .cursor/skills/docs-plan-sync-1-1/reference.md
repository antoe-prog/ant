# 1-1 동기화 — 보조 표

## `doc_paths` 키와 실제 파일

`product_vision.DOC_PATHS`의 값이 `docs/product/*.md` 경로와 일치하는지 릴리스 전에 가끔 확인한다.

## PR 설명에 넣을 한 줄 예시

> SSOT: `app/product_vision.py` — md는 동기화만, 비즈니스 변경은 코드 먼저.
