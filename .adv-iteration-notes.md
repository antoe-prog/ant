## 적대적 검토 → 개선 반복(현재 트랙)

### 핵심 고위험 영역 (최우선)
1. 알림 권한/푸시 등록 실패의 원인 분해가 섞여 있음
- `denied`(OS 권한 거부), `provider unavailable`(서버측 APNs/FCM 미등록), `error`(네트워크/토큰 저장 실패)는 UI 메시지/액션이 같아야 한다고 보기 어려움.
- 목표: 상태별 액션을 분리해 잘못된 해결 경로로 유도하지 않도록 고정.

2. 관리자/운영 반영 상태 추적 불가
- 어떤 단계에서 막혔는지 코드/로그에서 판독 가능해야 함.
- 목표: 실패 시점별 로그 라벨과 사용자 안내 동기 일치.

### 다음 반복의 단일 패치 후보 (고정)
- `notifications` 관련 화면에서:
  - `blocked`: 설정 열기(운영체제 알림 권한)
  - `unavailable`: “알림 제공자 미설정(운영자 점검 필요)” + 지원 연결
  - `error`/`prompt`: 재시도 + 상태 메시지 재요청

### 실행 체크리스트
- [ ] `권한 거부`: 앱 설정으로 이동 버튼 노출
- [ ] `provider unavailable`: 앱 설정 이동 버튼 제거 또는 비활성화, 운영자 가이드 텍스트 표시
- [ ] 동일 사용 시나리오: 신규 설치 직후 / 알림 거절 후 / 백그라운드 재시도 3개
- [ ] 개선 결과: 오탐 경고(설정 열기 잘못 노출) 0건

### 회귀 검증 기준
- 상태별 텍스트/버튼 조합이 분기되어야 함(동일 문구/동일 액션 금지)
- 앱 설치/권한 토글/앱 재시작 이후에도 상태 해석이 일관됨
- 사용자 오퍼레이션이 실제 필요한 단계(권한 변경 vs 운영 점검)에 일치해야 함

### 사용자에게 바로 요청할 다음 액션
- 셸 권한이 복구되면 패치 적용+검증 실행:
  - 코드 수정 1건
  - 단위/릴리즈 체인 최소 smoke
  - 모바일에서 재현 시나리오 3건 확인 후 다음 항목(회원/수업/대회 플로우)로 순환
