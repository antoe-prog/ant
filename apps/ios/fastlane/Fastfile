# 이 저장소의 apps/ios는 Swift 스켈레톤이며 .xcodeproj가 없을 수 있습니다.
# Xcode 프로젝트를 생성한 뒤 lane을 활성화하세요.

default_platform(:ios)

platform :ios do
  desc "Fastlane 설치 확인 (빌드 없음)"
  lane :info do
    UI.important "apps/ios: Xcode 프로젝트가 준비되면 Appfile에 app_identifier 등을 채우고 beta/upload lane을 추가하세요."
  end

  # desc "TestFlight / 내부 배포 예시"
  # lane :beta do
  #   build_app(scheme: "YourScheme", project: "YourApp.xcodeproj")
  #   upload_to_testflight(skip_waiting_for_build_processing: true)
  # end
end
