# Changelog

## v0.1.0-template

- Initial monorepo scaffold for native mobile + AI gateway template.
- Added gateway endpoints (`/health`, `/v1/chat`, `/v1/models`, `/v1/usage`).
- Added prompt registry, provider adapter, context memory, rate limit, and budget guard.
- Added mobile feature skeleton for chat/settings/history.
- Added CI workflow, local infra compose file, and operations/release docs.
